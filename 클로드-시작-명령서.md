# 눈썹 밸런스 앱 — 클로드 시작 명령서

*(Perfect Brow · 반영구 눈썹 문신 밸런스 측정 도구)*

> 이 문서는 **AI 에게 직접 내리는 지시서**입니다.
> 원장님은 아무것도 첨부하지 않으십니다. **네가 직접 받아서 읽어라.**
>
> 최종 갱신 : 2026-08-25 · 기준 버전 **v1.75.0** · 회귀 테스트 **136/136**

---

# 0. 너는 지금 아무것도 첨부받지 못했다 — 스스로 찾아 읽어라

원장님이 새 창에서 **한 줄**만 말씀하셨을 것이다.
파일은 첨부되지 않는다. **아래를 순서대로 시도해서 이 문서를 직접 확보해라.**

```bash
# ① 1순위 — 깃허브 (항상 된다. 데스크톱 앱 없어도 됨)
git clone --depth 1 https://github.com/leahkshop/perfect-brow.git /home/claude/site-flat
cat /home/claude/site-flat/클로드-시작-명령서.md
```

①이 어떤 이유로든 실패하면 ②를 써라.

```bash
# ② 2순위 — 원장님 맥 (데스크톱 앱이 켜져 있고 폴더가 연결돼 있을 때만)
#    device_bash 로 실행한다. 일반 bash 로는 안 된다
cat "$HOME/mnt/perfect-brow-source/_출력/눈썹밸런스앱-클로드-시작명령서.md"
```

①②가 모두 실패하면 **원장님께 그대로 보고해라.** 추측으로 작업을 시작하지 마라.
①이 성공했다면 소스도 이미 다 받은 것이다. 아래 「읽는 즉시 할 일」의 clone 은 건너뛰고 테스트부터 하면 된다.

**원장님이 쓰시는 한 줄은 이것이다.**

> 「github.com/leahkshop/perfect-brow 클론해서 클로드-시작-명령서.md 읽고 그대로 해라」

이 한 줄만 보고도 위 절차가 전부 돌아가야 한다. 되묻지 마라.

---

# ⛳ 읽는 즉시 할 일 — 다른 말 없어도 아래를 그대로 수행해라

**너는 지금 눈썹 자수 밸런스 앱(Perfect Brow) 작업을 맡았다.
이 문서가 이 프로젝트의 작업 규칙 전부다. 끝까지 읽고 아래를 순서대로 실행해라.**

```bash
# 1. 새 창은 빈 컨테이너다. 소스부터 받아라
git clone --depth 1 https://github.com/leahkshop/perfect-brow.git /home/claude/site-flat

# 2. 시작점이 정상인지 확인 (고치기 전에 반드시)
npm i playwright
cd /home/claude/site-flat && \
PB_CHROME=/opt/pw-browsers/chromium-1194/chrome-linux/chrome node regression-test.mjs
# ※ npx playwright install 은 하지 마라. 크로미움은 /opt/pw-browsers 에 이미 있다
```

그 다음 **아래 6개 파일을 전부, 이 순서로 읽어라. 하나도 건너뛰지 마라.**
"양이 많다", "필요할 때 읽겠다" 는 허용되지 않는다. 여기서 아낀 시간은 반드시 사고로 돌아온다.

| 순서 | 파일 | 왜 읽어야 하나 |
| --- | --- | --- |
| 1 | `클로드-시작-명령서.md` | 지금 읽고 있는 이 문서. 작업 규칙 전부 |
| 2 | `BASELINE.md` | 🔒 잠금 사양. **어기면 조용히 깨지는 규칙**들이 여기 있다 (좌표계·대칭·getScreenCTM 금지 등) |
| 3 | `app.js` | 전 로직 약 1,900줄. 상단 주석에 섹션 지도가 있다 |
| 4 | `index.html` | 화면 구조 + CSS 전부 (별도 CSS 파일 없다) |
| 5 | `regression-test.mjs` | 133개 회귀 테스트. **무엇이 보호되고 있는지**가 여기 다 적혀 있다 |
| 6 | `README.md` | 현재 기능 목록과 사용법. 원장님이 무엇을 쓰고 계신지 |

`sw.js` · `manifest.webmanifest` 는 배포 단계에서만 열면 된다.

**읽었으면 원장님께 한 줄로 확인해 드려라.**
`"6개 파일 다 읽었습니다 · 회귀 테스트 136/136 · v1.75.0"`
(버전은 항상 **app.js 의 `APP_VERSION`** 을 읽어 그 값으로 말한다 — 이 문서의 숫자가 낡았을 수 있다)

**판정**

- 회귀 테스트가 **133/133 가 아니면** → 코드를 고치지 말고 **그것부터 보고**해라
- **133/133 면** → "준비됐습니다" 라고 말하고 **무엇을 할지 물어봐라.** 바로 고치지 마라

---

# 🔁 작업이 끝나면 — **묻지 말고 끝까지 수행해라**

원장님이 고칠 것을 지시하고 결과를 확인하셨다면, 아래 9단계는 **매번 자동으로 끝까지** 한다.
"배포할까요?" "백업할까요?" 라고 되묻지 마라. 이건 이 프로젝트의 상시 절차다.

```
1. 회귀 테스트 전 항목 통과 확인          ← 실패하면 여기서 멈추고 보고
2. 실제 화면 렌더링해서 눈으로 확인
3. BASELINE.md 에 규칙 추가 (반드시 "왜"와 함께)
4. sw.js 의 VERSION 올리기
5. 백업 zip + SHA-256 만들기              ← 배포보다 먼저
6. GitHub 커밋 (브라우저 자동화 · 5번 장 참고)
7. 릴리스 태그 발행 + 한국어 상세 노트
8. 배포된 sw.js 를 가져와 버전이 올라갔는지 확인
9. 백업 · 대시보드를 원장님께 전달하고 맥 _출력 폴더에 저장
```

**중간에 멈춰야 하는 경우는 딱 셋뿐이다.**

| 멈춰야 할 때 | 어떻게 |
| --- | --- |
| 회귀 테스트가 하나라도 실패 | 커밋하지 말고 **실패 항목을 먼저 보고** |
| 크롬 확장 · 데스크톱 앱이 끊겨 배포 불가 | **백업은 이미 만들어 뒀으니** 그 사실을 알리고, 연결되면 이어서 배포 |
| 방향이 두 갈래 이상 | `AskUserQuestion` 으로 물어라 (각 선택지에 결과와 대가를 적어서) |

---

# 🤖 스스로 하는 것 / 못 하는 것

| 항목 | 자동 | 비고 |
| --- | --- | --- |
| 소스 받기 · BASELINE 읽기 · 테스트 실행 | ✅ | 이 문서를 읽는 즉시 |
| 코드 수정 후 테스트 · 문서 · 버전 올리기 | ✅ | 묻지 않고 수행 |
| 백업 zip + SHA-256 | ✅ | 배포보다 먼저 |
| GitHub 커밋 · 릴리스 태그 | ⚠️ | **원장님 크롬이 켜져 있어야** 한다 (`git push` 는 인증이 없어 불가) |
| 맥 `_출력` 폴더 저장 · 대시보드 갱신 | ⚠️ | **데스크톱 앱이 켜져 있어야** 한다 |
| 앱을 실제 기기에서 켜보기 | ❌ | 원장님만 할 수 있다. 결과를 알려주셔야 한다 |
| MediaPipe 실제 정확도 검증 | ❌ | 테스트 환경에서 모델을 못 받는다. 가짜 랜드마크로 계산 경로만 검증 중 |

**⚠️ 표시는 원장님 컴퓨터가 꺼져 있으면 못 한다.** 그럴 때는
① 백업은 반드시 먼저 만들어 두고 ② 못 한 단계를 분명히 말하고 ③ 연결되면 이어서 해라.
**조용히 건너뛰지 마라.**

---

**그리고 아래 8번(원장님과 일하는 방식)을 특히 지켜라.**
한국어로 짧게 · 못 한 것은 숨기지 말고 먼저 · "답변 먼저"면 코드 건드리지 말 것 ·
스크린샷은 90° 돌아가 있으니 환산부터 · 빨간 표시는 "여기로 옮겨라"는 뜻 ·
큰 변경은 화면으로 먼저 보여주고 확인받을 것.

---

## 0. 제일 먼저 — 이것부터 실행해라

새 창은 **빈 리눅스 컨테이너**다. 이전 창의 파일은 전부 사라졌다. 소스부터 받아라.

```bash
git clone --depth 1 https://github.com/leahkshop/perfect-brow.git /home/claude/site-flat
```

그 다음 **반드시 이 순서로** 읽어라.

| 순서 | 파일 | 왜 |
| --- | --- | --- |
| 1 | `/home/claude/site-flat/BASELINE.md` | 🔒 **잠금 사양.** 규칙마다 "안 지키면 어떻게 깨지는지"가 적혀 있다. 안 읽으면 이미 고친 버그를 다시 만든다 |
| 2 | `/home/claude/site-flat/app.js` | 전 로직 (약 1,900줄) |
| 3 | `/home/claude/site-flat/index.html` | 화면 구조 + 전체 CSS (약 550줄) |
| 4 | `/home/claude/site-flat/regression-test.mjs` | 회귀 테스트 133항목 |
| 5 | `/home/claude/site-flat/README.md` | 사용법 (v1.75.0 기준으로 갱신됨) |

**고치기 전에 시작점이 정상인지 확인해라.**

```bash
npm i playwright
cd /home/claude/site-flat && \
PB_CHROME=/opt/pw-browsers/chromium-1194/chrome-linux/chrome node regression-test.mjs
```

→ **133/133 통과**가 나와야 정상이다. 안 나오면 **그것부터 보고**하고, 코드를 고치지 마라.
(`npx playwright install` 은 하지 마라. 크로미움은 `/opt/pw-browsers/` 에 이미 있다.)

---

## 1. 이 앱이 무엇인가

**반영구 눈썹 문신(PMU) 아티스트가 시술 중에 쓰는 눈썹 밸런스 측정 도구**다.

- 사용자는 **원장님 한 분**. 시술실에서 **한 손에 폰을 들고** 쓴다
- 고객 얼굴 사진을 올리면 **AI가 얼굴을 인식해 자동 정렬**하고,
  **원장님이 그려 둔 드로잉을 읽어 그 선 위로 자를 올린다** (BASELINE 1-19 · 이 앱의 핵심 흐름)
- 원장님이 고객 얼굴에 직접 눈썹을 그린 뒤, **좌우 밸런스가 맞는지 앱이 판정**한다 ← 이 앱의 최종 목표
- **가로 전용**이다. 폰 회전 잠금을 켜 둬도 앱이 스스로 가로로 돌아간다
- **오프라인 동작**. 고객 사진은 **기기 밖으로 절대 나가지 않는다**

### 지금 상태

| 항목 | 값 |
| --- | --- |
| 라이브 | https://leahkshop.github.io/perfect-brow/ |
| 저장소 | https://github.com/leahkshop/perfect-brow (**공개**) |
| 기준 버전 | v1.75.0 |
| 회귀 테스트 | 133항목 전 통과 |
| 복구 지점 | 릴리스 태그 16개 + 로컬 zip 16개 + 맥 `_출력/백업/` |
| 기술 | 순수 HTML + CSS + 바닐라 JS. **프레임워크·빌드 단계 없음** |
| AI | MediaPipe FaceLandmarker (478점) — 기기 안에서 실행 |

---

## 2. 화면 구조 (가로 기준)

```
┌──────┬───────────────────────────────────────────────┬─────────┐
│ 왼쪽 │                                               │ 오른쪽  │
│ 레일 │              사 진 (주인공)                   │  도크   │
│      │                                               │         │
│ 한/영│  [모든라인숨김][여러라인]  [밸런스][왼][오른] │ 초기화  │
│ 눈   │   ← 가이드 선들이 여기 얹힌다                 │ (간격)  │
│ 앞머리│                                              │ 사진변경│
│ 앞두께│                                              │ 사진저장│
│ 아치 │                                               │ ▲ 위아래│
│아치두께│                                             │ 드래그바│
│ 꼬리 │                                               │ ▼       │
│ ──── │                                               │         │
│ 센터 │                                               │         │
│ 이너 │                                               │         │
│아우터│                                               │         │
│ ──── │                                               │         │
│V피봇 │  [프리셋][★][★][★]    [사진잠금]  [다시][되돌]│ ◀ 좌우 ▶│
│V앵글 │   ← 즐겨찾기 3개      ↑ 잠금이 화면 정중앙    │ 드래그바│
└──────┴───────────────────────────────────────────────┴─────────┘
   왼손 = 선 고르기            오른손 = 미세 조절
```

**설계 핵심** — 가로 모드 양손 조작. 사진이 화면의 90% 이상을 차지한다.
왼쪽 레일은 **목록**이라 담백하고, 오른쪽은 **지금 조작 중인 것**이라 켜진다.

---

## 3. 절대 바꾸면 안 되는 것 — 요약

전문은 `BASELINE.md` 1장에 있다. **작업 전에 반드시 원문을 읽어라.** 아래는 지도일 뿐이다.

| # | 규칙 | 어기면 |
| --- | --- | --- |
| 1-1 | 좌표는 **0~1 정규화**로 저장. 픽셀 저장 금지 | 기기 바뀌면 다 어긋남 |
| 1-2 | 대칭은 **`setLine()`** 을 거친다. `g.v2 = …` 직접 대입 금지 | 좌우 대칭이 깨짐 |
| 1-3 | SVG 는 소문자 태그 + `createElementNS` | 네이티브에서 안 그려짐 |
| 1-4 | 제스처는 **기준값 + 델타**. 절대 위치 금지 | 선이 손가락으로 순간이동 |
| 1-5 | 프리셋 로드가 저장분을 덮어쓰지 않게 | 저장한 디자인이 사라짐 |
| 1-6 | **`getScreenCTM()` 사용 금지** · 가로 전용 | iOS 에서 손가락 방향 90° 어긋남 |
| 1-7 | 각 바는 자기 축으로만 · 조절자 2개 항상 표시 | 조작이 직관에서 벗어남 |
| 1-8 | 사진 잠금 = 미세조정 모드 스위치 | — |
| 1-9 | 되돌리기 = "손 뗄 때까지가 한 작업" | 되돌리기가 먹통처럼 보임 |
| 1-10 | 사진·키·비밀번호는 밖으로 안 나간다 | 사고 |
| 1-11 | **그리는 범위 = 잡는 범위**(`browX`) | 안 보이는 곳에서 선이 잡힘 |
| 1-12 | 버튼 이름은 **i18n 키**. 하드코딩 금지 | 언어 전환이 그 버튼만 안 먹음 |
| 1-13 | **발광·그라데이션은 켜진 모드에만** | 가이드 선과 UI 가 헷갈림 |
| 1-14 | 자는 **자기 세로선(anchor)** 위에 올라간다 · 길이는 눈썹 폭 비례 | frac 상수로 박으면 아치 자가 아우터를 따라 움직인다 (원장님이 두 번 잡아내심) |
| 1-15 | AI 두께는 **실측**. 고정 오프셋 금지 | 고객마다 안 맞음 |
| 1-16 | 자는 **짝끼리 길이·끝이 같다** | 두께를 눈으로 못 잼 |
| 1-17 | 프리셋은 **얼굴 기준틀**로 환산 | 다른 고객에게 안 맞음 |
| 1-18 | 밸런스는 **못 읽으면 조용히 건너뛴다** | 오판 → 잘못된 시술 |

---

## 4. 작업 절차 — 이 순서를 지켜라

```
1. BASELINE.md 읽기
2. 한 번에 한 가지만 고치기
3. 회귀 테스트 전 항목 통과 확인   ← 하나라도 실패하면 커밋 금지
4. 실제 화면을 렌더링해서 눈으로 확인 (스크린샷)
5. BASELINE.md 에 규칙 추가 (반드시 "왜"와 함께) + sw.js VERSION 올리기
6. 백업 zip + SHA-256 만들기        ← 배포보다 먼저
7. GitHub 커밋
8. 릴리스 태그 발행
9. 백업·대시보드를 맥 _출력 폴더에 저장
```

**6번이 7번보다 앞인 이유** — 배포 뒤에 백업을 만들면 중간에 세션이 끊겼을 때
"배포는 됐는데 백업이 없는" 상태가 남는다. 실제로 v1.16.0 · v1.20.1 이 그렇게 빠졌다.

### 배포 체크리스트

- [ ] 회귀 테스트 전 항목 통과
- [ ] `BASELINE.md` 기준 버전 · 규칙 · 테스트 표 갱신
- [ ] `sw.js` `VERSION` 증가 (**안 올리면 폰에 옛 화면이 남는다**)
- [ ] 백업 zip + SHA-256
- [ ] GitHub 커밋
- [ ] 릴리스 태그
- [ ] 맥 `_출력` 저장 · 대시보드 갱신

### 테스트 작성 규칙

- **버그를 하나 고칠 때마다 그 버그를 잡는 테스트를 하나 추가한다**
- 테스트 번호를 `BASELINE.md` 6장 표와 **1:1로 맞춘다**
- **값만 보지 말고 실제로 바뀌었는지도 확인해라** —
  아무 일도 안 일어났는데 통과하는 테스트는 없는 것보다 나쁘다
- **선 좌표를 하드코딩하지 마라.** 자의 길이는 피드백으로 자주 바뀐다.
  실제로 그려진 토막의 한가운데를 찾아서 찍어라
- 밸런스 테스트는 **반드시 가로 모드**에서 — 세로 폴백은 오른쪽 토막이 아예 안 그려진다

---

## 5. 배포 방법 (중요 — `git push` 는 안 된다)

이 컨테이너에는 **GitHub 인증 정보가 없다.** `git clone`(읽기)은 되지만 `push` 는 안 된다.
배포는 **브라우저 자동화로 GitHub 웹에 파일을 올리는 방식**이다.

```
1. 고친 파일을 /mnt/user-data/outputs/site/ 로 복사
2. mcp__claude-in-chrome__ 도구로 https://github.com/leahkshop/perfect-brow/upload/main 열기
3. find 로 파일 입력을 찾아 file_upload 로 올리기
4. 커밋 메시지에 "v1.XX.X — 무엇을 바꿨는지" (한국어)
5. Commit changes 클릭 → 커밋 확인
6. https://github.com/leahkshop/perfect-brow/releases/new?tag=v1.XX.X&title=v1.XX.X
   → 릴리스 노트 작성 → Publish release
7. 1분 뒤 배포된 sw.js 를 WebFetch 로 가져와 버전이 올라갔는지 확인
```

**알려진 걸림돌**
- `Publish release` 는 **한 번에 안 눌릴 때가 있다.** 스크린샷 찍고 다시 눌러라.
  주소가 `/releases/tag/vX` 로 바뀌면 성공이다
- 크롬 확장이 끊기면 배포가 멈춘다. `tabs_context_mcp` 로 새 탭 그룹을 만들어 다시 시도
- GitHub 장애로 Pages 빌드가 안 돌 때가 있다. `sw.js` 버전을 올려 재커밋하면 다시 트리거된다

---

## 6. 환경 정보 (이 컨테이너에서 되는 것 / 안 되는 것)

| 항목 | 상태 |
| --- | --- |
| `git clone` (공개 저장소) | ✅ 됨 |
| `git push` | ❌ 인증 없음 → 브라우저 자동화로 대체 |
| `curl` 로 임의 사이트 | ❌ 막힘 (`WebFetch` 도구를 써라) |
| `npm i` | ✅ 됨 |
| Playwright 크로미움 | ✅ `/opt/pw-browsers/chromium-1194/chrome-linux/chrome`. **`playwright install` 하지 마라** |
| 맥 파일 접근 | ✅ `mcp__remote-devices__*` (데스크톱 앱이 켜져 있을 때만) |
| 연결된 맥 폴더 | `/Users/leahsito/Downloads/perfect-brow-source` **만** |

### 맥에 저장하는 위치

```
/Users/leahsito/Downloads/perfect-brow-source/_출력/
├── Perfect-Brow-대시보드.html      ← 매 버전 갱신
└── 백업/
    ├── perfect-brow-v1.75.0.zip   ← 예시 — 최신 버전 번호를 따른다
    └── … (버전별 zip 계속)
```

---

## 7. ⚠️ 함정 모음 (실제로 당한 것들)

| 함정 | 내용 |
| --- | --- |
| **맥의 낡은 파일** | `_출력/BASELINE.md`(22KB · 8/17), `perfect-brow-app.zip`, `perfect-brow-web.zip`, `standalone.html`, `가이드.md` 는 **전부 옛날 것**이다. 최신은 **GitHub** 또는 `_출력/백업/` 의 **최신 zip** 뿐 |
| **캐시 버전** | `sw.js` `VERSION` 을 안 올리면 폰에 옛 화면이 남는다. 원장님이 "안 바뀌었다"고 하시면 이것부터 의심 |
| **iOS 좌표** | `getScreenCTM()` 은 iOS 사파리에서 조상 CSS transform 을 무시한다. **크로미움 테스트로는 절대 안 잡힌다** |
| **무의미한 테스트** | 아무 일도 안 일어났는데 통과하는 테스트가 실제로 있었다(42·57·79번). "실제로 바뀌었나"를 꼭 검사해라 |
| **세로 폴백** | 세로 모드는 작업 영역이 좁아 오른쪽 토막이 안 그려진다. 레이아웃·밸런스 테스트는 가로에서 |
| **토큰 뒤집기** | `:root` 색을 다크로 바꿨을 때 모달만 흰 바탕으로 남아 글자가 안 보였다. **토큰을 바꾸면 그 토큰을 쓰는 화면 전부**(홈·모달·토스트) 확인 |
| **시안 채움 위 흰 글자** | 대비가 2:1 도 안 나온다. `--on-accent`(어두운 글자)를 써라 |
| **MediaPipe 검증 불가** | 테스트 환경에서 모델을 못 받는다. **가짜 랜드마크로 계산 경로만** 검증 중. 실제 정확도는 원장님이 확인해야 함 |

---

## 8. 원장님과 일하는 방식 ← 이게 제일 중요하다

### 말투와 보고

- **한국어로, 짧게.** 무엇을 **왜** 바꿨는지 중심으로
- **못 한 것 · 확실하지 않은 것 · 검증 못 한 것은 숨기지 말고 먼저** 말해라.
  "테스트는 통과했지만 실제 사진으로는 확인 못 했습니다" 같은 문장을 반드시 넣어라
- 과정을 늘어놓지 말고 **결과와 판단**을 말해라
- 이모지는 쓰지 마라

### 지시를 읽는 법

- **"답변 먼저"** 라고 하시면 **코드를 건드리지 말고 답부터** 해라
- **"작업전 BASELINE.md를 읽고 시작해라"** 는 거의 매번 붙는 상시 지시다
- 지시가 짧아도 **뜻은 깊다.** "꼬리가 짤린다" = 자동 정렬 로직을 고치라는 뜻이다
- 원장님은 **PMU 시술 용어로** 말한다. 코드 용어로 되묻지 말고 그 뜻을 먼저 해석해라
  (앞머리 = 눈썹 머리, 아치 = 눈썹 산, 꼬리 = 눈썹 끝, 이너 = 내안각, 아우터 = 눈꼬리)

### 스크린샷을 보내오실 때

- **폰 화면은 세로로 찍히지만 앱은 가로**다. 화면이 90° 돌아가 보인다.
  앱의 가로선은 스크린샷에서 **세로로** 보인다. 이걸 먼저 환산하고 판단해라
- **빨간색으로 직접 표시**해 주실 때가 있다 — 그건 "여기로 옮겨라"는 뜻이다.
  픽셀을 재서 정확한 값으로 환산해라
- 화면을 보고 판단이 안 서면 **추측하지 말고 물어봐라**

### 결정이 갈릴 때

- 방향이 두 갈래 이상이면 **AskUserQuestion** 으로 물어라. 각 선택지에 **결과와 대가**를 적어라
- 디자인 변경은 **코드를 건드리기 전에 실제 앱 화면으로 시안을 렌더링**해서 보여드려라
  (CSS 만 덧씌워 스크린샷을 찍으면 된다)
- 원장님이 고른 방향이 기술적으로 문제가 있으면 **그 자리에서 말해라.** 나중에 말하면 늦다

### ⚖️ 판정·수치는 **원장님 감각이 기준이다** (v1.32.0에서 배운 것)

원장님이 "이 정도면 맞는 것 아니냐" 고 하시면 **앱이 틀린 것이다.** 원장님은 이 일을 하는 전문가이고,
앱은 그 감각을 재현하는 도구다. 그럴 때는

1. **먼저 지금 판정 기준을 그대로 설명해라** — 무엇을 무엇과 비교해 어떤 값을 넘으면 표시하는지
2. **왜 원장님 눈과 다른지 원인을 찾아라.** 상수만 바꾸지 말고 **기준 자체가 잘못 잡혔는지** 봐라
   (실제 사례: 허용 오차가 화면 px 고정이라 **확대할수록 앱이 예민해졌다.** 상수를 올리는 게 아니라
    기준을 얼굴 크기에 붙이는 것이 답이었다)
3. **원장님이 정하신 값은 BASELINE 에 "누가·언제·왜" 와 함께 적어라.** 다음 세션이 임의로 되돌리지 못하게

원장님 말씀: "너는 내가 가르쳐주는 것에 정보를 계속 쌓아가야 한다. 나의 스킬에 대한 것이기 때문이다."

### 원장님이 중요하게 여기는 것

1. **매 버전 백업 · 깃 · 문서** — 하나라도 빠지면 지적하신다
2. **시술 중 손이 덜 가는 것** — 버튼 하나 줄이는 게 기능 하나 늘리는 것보다 낫다
3. **직관** — "손의 방향과 선의 방향이 일치"하는지를 늘 본다
4. **정확도** — 밸런스가 이 앱의 존재 이유다. 애매하면 표시하지 않는 쪽이 낫다

---

## 9. 지금까지 온 길 (요약)

| 버전 | 무엇 |
| --- | --- |
| v1.8.0 | 가로 전용 · 회전 잠금 무시 |
| v1.10.0 | iOS 제스처 90° 어긋남 수정 (`getScreenCTM` 금지) |
| v1.11~1.17 | 레이아웃 전면 재배치 · 되돌리기 · 메인 작업 영역 |
| v1.18~1.19 | 여러라인 · 전체라인 · 한/영 · 다시 실행 |
| v1.20 | **디자인 시스템 개편** — 전면 다크 · SVG 아이콘 · 버튼 위계 |
| v1.21 | 눈썹 구간 좌표계 (자가 눈썹만큼만) |
| v1.22 | **AI 측정 배치** — 선을 켤 때 그 사진에서 잰 자리로 |
| v1.23~1.24 | 자 정렬 · 위치 재조정 |
| v1.25 | **프리셋 얼굴 맞춤** (얼굴 기준틀) |
| v1.26 | **밸런스 판정** — 기준 쪽 대비 다른 곳만 빨갛게 |
| v1.27 | **사진 넣기 동선** — 홈은 세로 · 편집기만 가로 · 작업 중 사진변경은 가로 유지 + 화면 딤 |
| v1.30 | **드로잉 자동 맞춤** — 사진을 넣으면 **그린 선 위로** 모든 자가 자동 배치 (앱의 실제 작업 순서 · BASELINE 1-19) |
| v1.29 | **원장님 기준 도입** — 밸런스 허용 오차를 **얼굴 크기 기준 0.8mm** 로 · 버튼 자리 재조정 · 프리셋 내보내기/가져오기 |
| v1.53 | **작업 영역 확장** — 오른쪽 여백 5%→2% · 값 라벨은 바 안쪽으로 · 꼬리 자 바깥 회색 참조선 |
| v1.52 | **방향 신호** — 세로선 조용=회색/잡으면=전체 색 · 자 색이 세로선을 50% 관통 (v1.51 토막 방식은 「ㄱ자로 보인다」로 하루 만에 대체) |
| v1.51 | **일하는 곳만 굵게** — 자는 바깥 절반만 · 세로선은 눈썹 속을 비움 · 꼬리 자 반 · 내장 프리셋 제거 · 사진잠금이 센터선 위 |
| v1.50 | **버튼 위계·대비 교정** — 잠금이 주요·저장은 조용 · 가이드 켜짐 신호 · 캔버스 칩과 초기화를 어두운 판 위로(밝은 이마에서 안 보이던 문제) |
| v1.49 | **가이드 중 밝은 선은 하나** (다음 차례로 선택도 이동) · 아치·꼬리 연한 색도 별도 색으로 (고유색은 유지) |
| v1.48 | **이너 색 두 갈래** — 강조 민트 / 연한 연회색 · 연한 상태를 알파가 아니라 별도 색으로 (원장님: 「눈이 아프고 톤다운된 이상한 색」) |
| v1.28 | **버튼 재배치** — 사진 3버튼(변경·저장·잠금) 가운데 · 잠금 정중앙 · 초기화는 오른쪽 도크 맨 위(짙은 빨강) · **프리셋 즐겨찾기 ★3** · 눈가이드 제거 |

## 10. 다음에 할 만한 것

- [ ] `README.md` 를 현재 버전으로 다시 쓰기 (v1.11.0 시점에 멈춰 있음)
- [ ] 밸런스 허용 오차를 **mm 단위로 사용자 조절** (지금은 1px 고정)
- [ ] 밸런스에서 차이 나는 곳을 하나씩 짚어보는 버튼
- [ ] 실제 고객 사진으로 밸런스 정확도 검증 후 상수 조정
- [ ] 시술 전/후 사진 비교 (좌우 분할 뷰)
- [ ] 고객별 기록 저장

---

## 11. 첫 응답에서 할 것

위 0번을 실행하고 133/133 를 확인한 뒤, **무엇을 할지 물어봐라.**
바로 코드를 고치지 마라.
