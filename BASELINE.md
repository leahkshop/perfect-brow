# Perfect Brow — 기준선 (BASELINE) · 잠금 사양

> **이 문서의 목적**
> 앞으로 이 앱을 고도화할 때 **이미 검증된 부분이 깨지지 않도록** 지키는 규칙과 복구 방법을 적어둔 문서입니다.
> 사람이 작업하든 AI에게 시키든, **작업 전에 이 문서를 먼저 읽히세요.**

| 항목 | 값 |
| --- | --- |
| 기준 버전 | **v3.2.0** |
| 확정일 | 2026-08-30 |
| 라이브 주소 | https://leahkshop.github.io/perfect-brow/ |
| 저장소 | https://github.com/leahkshop/perfect-brow |
| 상태 | ✅ 안정 (회귀 테스트 전 항목 통과) |

---

## 1. 🔒 절대 바꾸면 안 되는 것 (Invariants)

바꾸는 순간 앱이 조용히 망가지는 항목들입니다. **원본 Manus 버전이 실제로 여기서 깨졌습니다.**

### 1-1. 좌표계 규약 — 가장 중요

**모든 가이드 라인 값과 사진 오프셋은 0~1 정규화 값입니다. 절대 픽셀 값을 넣지 마세요.**

| 변수 | 단위 | 의미 |
| --- | --- | --- |
| `g.h1` ~ `g.h3`, `front`, `frontThickness`, `archThickness` | 0~1 | 캔버스 **높이** 대비 비율 (0=위, 1=아래) |
| `g.v1`~`g.v5`, `arch` | 0~1 | 캔버스 **너비** 대비 비율 (0=왼쪽, 1=오른쪽) |
| `p.ox`, `p.oy` | 캔버스 비율 | 렌더링에서 `ox * W`, `oy * H` 로 곱해짐 |
| `p.zoom` | 배수 | 0.5 ~ 8 |
| `p.rot` | 도(°) | -30 ~ +30 |
| `g.innerAngle` | 0~1 | Pivot 의 **Y 위치** (각도 아님, 이름 주의) |
| `g.outerAngle` | 0~1 | 0.5 = 수평. 각도 = `(outerAngle - 0.5) × 2 × 40°` |

> ⚠️ **원본의 실제 버그:** `photoOffsetX` 에 `(0.5 - eyeCenterX) * CANVAS_WIDTH` (픽셀)를 넣었는데
> 렌더링에서 다시 `* CANVAS_WIDTH` 를 곱해서 약 400배 어긋났습니다. 사진이 화면 밖으로 날아갑니다.
> **정규화 변수에 픽셀을 대입하는 코드는 무조건 버그입니다.**

### 1-2. 대칭 규칙

`setLine()` 함수 안의 규칙입니다. 이 로직을 우회해서 `g.v2` 등을 직접 대입하면 대칭이 깨집니다.

```
v1(Center) 이동  →  v2, v3, v4, v5, v6, v7 이 같은 양만큼 함께 이동
v2(Inner) 이동   →  v3 = 2 × v1 − v2   (Center 기준 거울상)
v4(Outer) 이동   →  v5 = 2 × v1 − v4
v6(아치선) 이동  →  v7 = 2 × v1 − v6          (v1.32.0)
```

**라인 값을 바꿀 때는 반드시 `setLine(key, value)` 를 거치세요.** `S.g.v2 = 0.4` 처럼 직접 대입 금지.

### 1-3. SVG 렌더링

- 가이드 오버레이 `<svg id="guides">` 는 **반드시 `pointerEvents: none`** 유지.
  터치는 그 위의 `#touch` 레이어가 전담합니다. (원본은 이걸 svg에 걸어놓고 드래그를 구현했다고 착각해서 드래그가 아예 안 됐습니다.)
- SVG 요소는 `createElementNS(SVGNS, ...)` 로만 생성. 소문자 `<circle>` 같은 HTML 태그로 만들면 나중에 네이티브 앱으로 감쌀 때 안 그려집니다.

### 1-4. 제스처 델타

- 핀치/회전/팬은 **제스처 시작 시점의 기준값(`gDrag.zoom0` 등)에 대한 절대 계산**으로만 갱신합니다.
- 매 프레임 `+=` 로 누적하면 가속되어 튑니다. (원본의 버그)

### 1-5. 프리셋 저장소

- localStorage 키: **`pb_presets_v1`** — 사용자가 저장한 프리셋만 들어갑니다.
- **기본 프리셋(`BUILTINS()`)은 절대 이 저장소에 쓰지 마세요.** 매번 코드에서 생성해 앞에 붙입니다.
  (원본은 앱을 켤 때마다 기본값으로 저장소를 덮어써서 사용자 프리셋이 전부 날아갔습니다.)
- 스키마를 바꿔야 하면 키를 `pb_presets_v2` 로 올리고 마이그레이션 코드를 넣으세요.

**즐겨찾기 ★ (v1.28.0)**

- localStorage 키: **`pb_favs_v1`** — **프리셋 id 배열만** 넣습니다. 최대 3개(`FAV_MAX`).
- **프리셋 내용을 복사해 두지 마세요.** 원본을 수정했을 때 즐겨찾기 버튼만 옛 값으로 남습니다.
- 지워진 프리셋을 가리키는 id 는 `buildFavBar()` 가 **조용히 걸러냅니다**(오류를 내지 않습니다).
  프리셋을 삭제할 때 `pb_favs_v1` 에서도 함께 빼야 목록이 깔끔합니다.
- 3개를 넘겨 누르면 **넣지 않고 토스트만** 띄웁니다(`fav_max`). 오래된 것을 밀어내면
  원장님이 방금 지정한 것이 사라진 줄 알고 다시 찾게 됩니다.
- 버튼 줄은 **지정한 개수만큼만** 그립니다 — 빈 자리를 만들지 않습니다. 회귀 84·85번.

**내보내기 · 가져오기 (v1.29.0)**

- 프리셋은 브라우저 localStorage 에만 있어 **기기를 바꾸거나 데이터를 지우면 사라집니다.**
  `프리셋` 모달의 `내보내기` 가 `{tag, v, presets, favs}` JSON 파일로 빼 줍니다.
- 가져오기는 **같은 id 는 덮어쓰고 새 것은 뒤에 붙입니다** — 기존 저장분을 지우지 않습니다.
- **내장 프리셋은 내보내지 않습니다** (매번 코드에서 만들므로 · 1-5 위 규칙).
- 원장님이 만드신 디자인을 **앱 기본 제공 프리셋으로 넣을 때 이 파일을 씁니다.**

### 1-6. 화면 방향 — 이 앱은 **가로 전용**입니다 (v1.8.0)

**세로 모드는 지원하지 않습니다.** 기기 회전 잠금이 켜져 있어도 항상 가로로 나와야 합니다.

가로를 만드는 방법은 두 단계이고, 위쪽이 성공하면 아래는 자동으로 꺼집니다.

| 순위 | 방법 | 동작 기기 |
| --- | --- | --- |
| 1 | **진짜 방향 잠금** `screen.orientation.lock("landscape")` (`tryOrientationLock()`) | 안드로이드 설치앱(PWA) — 폰 화면 자체가 돌아가므로 **시스템 UI 도 함께 가로** |
| 2 | **CSS 90° 회전** `body.rot90` (`applyLayout()`) | 아이폰·아이패드 — 사파리는 방향 잠금 API 를 아예 구현하지 않음 |

- 1번이 성공하면 뷰포트가 가로가 되어 `devPortrait` 가 false → **2번은 스스로 꺼집니다.** 따로 끄는 코드가 필요 없습니다.
- 1번은 사용자 제스처가 있어야 허용되는 브라우저가 있어 **첫 터치에도 재시도**합니다 (최대 4회, `orientLockTries`).

**판정식 — 바꾸지 마세요**

```js
const editing = $("editor").classList.contains("active");
const rot = devPortrait && editing
  && (mode === "on" || (mode === "auto" && isTouchDevice()));
```

> **`editing` 조건을 빼지 마세요 (v1.27.0).**
> 가짜 회전은 **작업 화면에서만** 겁니다. **홈(첫 사진 선택)은 기기 방향 그대로 세로**로 뜹니다.
> **이유:** iOS 사진 선택 시트는 웹페이지 **바깥에서 OS 가 그리므로** 앱이 회전시킬 수 없습니다.
> v1.26.0 까지는 홈까지 90° 돌려놨기 때문에 **앱은 가로인데 시트만 세로**로 떠서
> 앱을 켜고 첫 사진을 넣는 것부터 불편했습니다(원장님 스크린샷으로 확인).
> 회전 잠금을 켜 두고 쓰는 것이 이 앱의 정상 사용 방식이라
> "폰을 잠깐 세우세요" 안내로는 해결되지 않습니다.
> "가로 전용"은 **눈썹을 보는 작업 화면**에 대한 규칙입니다.

**작업 중 사진 선택 — 방향을 바꾸지 말고 화면만 낮춘다 (v1.27.0)**

> ⛔ **편집 중 `사진변경` 에서 앱을 세로로 되돌리지 마세요.**
> 시트와 방향은 맞지만 **화면이 통째로 돌아버려 "지금 가로냐 세로냐"가 헷갈립니다**
> (원장님 판단 · 실제로 한 번 그렇게 만들었다가 되돌림).
> 대신 `S.picking` → `body.picking` → **딤 레이어**(`.screen.active::after`, 검정 55%)만 깝니다.
> 레이아웃을 건드리지 않으므로 시트를 닫으면 그대로 돌아옵니다.

| 항목 | 내용 |
| --- | --- |
| 상태 | `S.picking` — `openPicker()` 에서 켜고 `endPicking()` 에서 끕니다 |
| 끝나는 것을 아는 법 | 웹은 시트가 닫힌 것을 직접 알 수 없습니다. **네 갈래로 감지**: `change`(사진 고름) · `window focus` · `visibilitychange` · 첫 `pointerdown`.<br>**하나만 믿지 마세요** — 취소했을 때 이벤트가 안 오는 기기가 있어 화면이 어두운 채로 남습니다 |
| 400ms 가드 | 시트를 여는 순간 곧바로 오는 focus·pointerdown 은 무시합니다(아직 열리는 중). 이 가드를 빼면 **시트가 뜨자마자 딤이 사라집니다** |

- `isTouchDevice()` (`pointer:coarse` 또는 `maxTouchPoints>0`) 로 거르는 이유:
  **데스크톱 브라우저 창을 세로로 좁혔을 때 화면이 통째로 돌아가지 않게** 하기 위함입니다.
  `isStandalone()` 로 거르면 **홈화면에 설치하지 않고 사파리에서 그냥 연 경우 세로로 떠서** 못 씁니다 (v1.7.0 의 문제).
- 방향 설정 키: `pb_orient` = `auto`(기본 — 터치 기기면 항상 가로) / `on`(데스크톱 포함 항상) / `off`(기기 따름, ⟳ 버튼으로만)
- 마이그레이션 키 `pb_orient_v = "2"` — v1.7.0 이 자동으로 써넣은 `off` 를 한 번 지웁니다. **지우지 마세요.**

> ⛔ **v1.7.0 의 "기기가 가로가 되면 `pb_orient` 를 `off` 로 영구 전환" 로직은 v1.8.0 에서 제거되었습니다.**
> 되살리지 마세요. 회전 잠금을 켜 둔 채로 쓰는 것이 이 앱의 정상 사용 방식입니다.

**남는 제약 — iOS 사진 선택 시트**

- `rot90` 상태에서는 **iOS 사진 선택 시트가 앱과 90° 어긋납니다.** 그 UI 는 웹페이지 바깥에서 OS 가 그리므로
  웹앱이 회전시킬 방법이 없습니다.
- v1.27.0 대응: **홈은 아예 세로**로 두고(첫 사진 넣기), 작업 중에는 **가로를 유지한 채 화면만 어둡게** 합니다.
  안내 HUD(`picker_rot`)는 더 이상 쓰지 않아 **문구까지 지웠습니다** — 되살리지 마세요.
  방향을 맞추라고 안내하는 것보다 **동선을 그렇게 만들어 두는 쪽**이 시술 중에 손이 덜 갑니다.
- 근본 해결은 **네이티브(Capacitor) 빌드** 뿐입니다 — 7번 로드맵 참고.

**포인터 좌표 변환 — `getScreenCTM()` 을 쓰지 마세요 (v1.10.0)**

> ⛔ **이전 버전(v1.5.0~v1.9.0)의 `getScreenCTM().inverse()` 방식은 아이패드에서 실제로 깨졌습니다.**
> **되돌리지 마세요.**

- **원인:** iOS 사파리(WebKit)는 **조상 요소의 CSS transform 을 `getScreenCTM()` 에 반영하지 않습니다.**
  그래서 `body.rot90` 상태에서 손가락을 위아래로 끌면 선이 **좌우로** 움직였습니다 (축이 90° 어긋남).
  크로미움은 반영하므로 **PC 자동 테스트로는 절대 잡히지 않습니다.**
- **해결:** `stagePoint()` 가 rot90 변환을 **직접 역으로 풉니다.**

  `.screen` 은 `transform-origin:0 0; transform: translateX(100dvw) rotate(90deg)` 이므로
  **로컬 +x → 뷰포트 +y**, **로컬 +y → 뷰포트 −x** 입니다. 역변환은:

  ```js
  // body.rot90 일 때
  x = e.clientY - stageRect.top;
  y = stageRect.right - e.clientX;
  // 아닐 때
  x = e.clientX - stageRect.left;
  y = e.clientY - stageRect.top;
  ```

- 회전 CSS(각도·`transform-origin`·`translateX`)를 바꾸면 **이 식도 같이 고쳐야 합니다.**
- 캔버스 크기는 여전히 `offsetWidth/offsetHeight` 로 잽니다 (`getBoundingClientRect` 는 회전 시 축정렬 bbox 를 돌려줌).
- **테스트에서도 `getScreenCTM` 을 쓰지 마세요.** 앱과 같은 함수를 공유하면 축이 뒤바뀌어도 통과합니다.
  회귀 테스트 14·34 는 뷰포트 좌표를 직접 계산해 검증합니다.
- 레이아웃은 `@media (orientation:…)` 이 아니라 **`body.land` / `body.compact` 클래스**로 결정합니다 (`applyLayout()`).
- `body:not(.land)` 세로 CSS 는 **`off` 폴백 전용**으로만 남겨둡니다. 지우지는 마세요(비상 탈출구).

### 1-7. 바의 이동 축 — 각 바는 자기 축으로만 움직인다

| 바 종류 | 이동 축 | 대상 |
| --- | --- | --- |
| **가로 바** | **위아래로만** | Eye · Front · F.T · Arch · A.T · Tail · V Center Pivot |
| **세로 바** | **좌우로만** | Center · Inner · Outer |

- 대각선으로 끌어도 **자기 축 성분만** 반영됩니다.
- 방향 버튼도 축에 맞춰 자동으로 바뀝니다 — 가로 바 `▼ ▲`, 세로 바 `◀ ▶` (`posConfig().axis`).
- **대칭은 그대로 유지됩니다** — 세로 바를 좌우로 옮기면 `setLine()` 이 반대편을 자동으로 맞춥니다 (1-2 참고).

**조절자 2개 — 앱을 열면 둘 다 항상 보인다 (v1.9.0) · 절대 숨기지 말 것**

조절자는 우측 패널이 아니라 **캔버스 위, 오른쪽 아래 모서리에 L 자로** 놓입니다. **하나만 보이게 전환하지 마세요.**

| 조절자 | DOM id | 위치 (v1.12.0) | 형태 | 조절 대상 |
| --- | --- | --- | --- | --- |
| **세로 조절자** | `#posCtlV` | **오른쪽 끝 · 세로 중앙** (`.rdock` 안, `↶ 되돌리기` 아래) | `▲` 위끝 / `▼` 아래끝 | `S.selUD` — 위아래로 움직이는 **가로선** (Eye·Front·F.T·Arch·A.T·Tail·V Center Pivot·V Angle) |
| **가로 조절자** | `#posCtlH` | **오른쪽 끝 · 맨 아래** (`.bdock` 안, 사진보정 버튼 아래) | `◀` 왼끝 / `▶` 오른끝 | `S.selLR` 세로선 **또는 사진 보정** (`S.hMode`) |

**오른쪽 컨트롤은 화면 오른쪽 끝에서 2% 떨어뜨린다 (v1.13.0 은 5% · v1.53.0 에서 축소)**

```css
.stage{ --redge: calc(2% + 8px) }
body.rot90 .stage{ --redge: calc(2% + 8px + env(safe-area-inset-bottom,0px)) }
.rdock, .bdock { right: var(--redge) }
```

> v1.53.0 원장님 지시: 「이 라인들 오른쪽으로 조금 더 이동하여 내부 화면 넓게 쓰게」 —
> 작업 영역이 아이폰 기준 약 23px 넓어졌습니다. **실기기에서 오른쪽 끝 버튼이 시스템
> 스와이프에 걸리면 3%로 한 칸 물리기로 합의** — 0%로는 만들지 마세요.
> 이 축소로 **세로 바의 값 라벨이 여백에 못 들어가게 되어 바 왼쪽(캔버스 안쪽)으로
> 옮겼습니다** — 아래 라벨 규칙 참고.

- 아이폰은 **화면 맨 오른쪽 가장자리가 시스템 스와이프 영역**(앱 전환·닫기)이라, 컨트롤을 끝에 붙이면
  눌러도 시스템 제스처가 먼저 먹습니다.
- 강제 가로(`rot90`)에서는 **앱의 오른쪽 끝 = 기기의 아래쪽 = 홈 인디케이터 자리**입니다.
  그래서 `env(safe-area-inset-bottom)` 을 추가로 뺍니다 (회전 때문에 축이 바뀐다는 점 주의).
- **가로 바의 값 라벨(`.plabel`)은 바 안쪽 왼쪽 끝**에 둡니다 (`position:static; order:1`).
  바 바깥 왼쪽에 두면 **왼쪽 아래 메뉴 행과 겹칩니다.**
- **세로 바의 값 라벨은 바 왼쪽(캔버스 안쪽)** 에 세로로 쌓습니다 (v1.53.0 —
  여백이 2%로 줄어 42px 라벨이 오른쪽에 들어갈 자리가 없어짐):
  `right:calc(100% + 6px); flex-direction:column; width:42px`(compact 34px).
  그 자리는 스크림이 깔린 가장자리라 사진을 실질적으로 가리지 않습니다.
  **`width` 를 빼고 `max-width` 만 주면 안 됩니다** —
  절대 배치 요소라 shrink-to-fit 이 걸려 글자가 한 자씩 세로로 쪼개집니다.
- 세로 조절자는 `.rdock` 안에 들어 있고, `.rdock` 은 `top:0; bottom:var(--bdock); height:46%; margin:auto 0`
  으로 **아래 도크를 뺀 영역의 세로 한가운데**에 놓입니다. `--bdock` 은 `syncDockSpace()` 가
  아래 도크의 실제 높이를 재서 넣습니다 — **이 변수를 지우면 두 도크가 겹칩니다** (회귀 테스트 10·32번).
- 축 클래스(`axis-v`/`axis-h`)는 **HTML 에 고정**돼 있습니다. `updatePanels()` 에서 서로 바꾸지 마세요 —
  바꾸는 순간 한쪽이 사라지거나 두 조절자가 같은 자리로 겹칩니다.
- 위로 밀면 선이 위로, 오른쪽으로 밀면 선이 오른쪽으로 — **손의 방향과 선의 방향이 항상 일치**합니다.
- **선택 상태는 3개**입니다: `S.sel`(드래그 대상) / `S.selUD`(세로 조절자 대상) / `S.selLR`(가로 조절자 대상).
  선을 고를 때는 반드시 `noteSel(key)` 또는 `setSel(key)` 를 거치세요 — `S.sel` 만 직접 대입하면
  **조절자가 엉뚱한 선을 잡고 있게 됩니다.**
- 지금 드래그 대상인 쪽만 `.active` 테두리로 강조합니다. **강조 ≠ 표시** — 나머지 한쪽도 계속 보여야 합니다.

**가로 조절자는 하나로 두 가지를 겸한다 (v1.11.0)**

`S.hMode` = `"line"` (기본) / `"photo"` — 사진 보정 슬라이더를 따로 두지 않고 **같은 바를 공유**합니다.

| 전환 | 동작 |
| --- | --- |
| 사진 보정 버튼(줌·위아래·좌우·밸런스) 클릭 | `hMode = "photo"`, 그 모드로 바가 사진을 조절 |
| 같은 버튼 다시 클릭 | `hMode = "line"` 로 복귀 |
| 세로선 선택(레일 버튼·화면 탭·값 변경) | `noteSel()` 이 자동으로 `hMode = "line"` |
| 사진 잠금 ON | `toggleLock()` 이 `hMode = "line"` 으로 되돌리고 보정 버튼을 `disabled`(반투명) 처리 |

- 값 읽기/쓰기는 반드시 `hConfig()` / `applyH()` 를 거치세요. `posConfig()` 나 `photoConfig()` 를
  직접 부르면 두 모드가 어긋납니다.
- 사진 보정 버튼은 잠금 시 `disabled` → 전역 `button:disabled{opacity:.38;pointer-events:none}` 로
  **반투명 + 눌리지 않음**이 됩니다. 이 규칙을 지우지 마세요.
- 세로 조절자는 가로 `range` 를 `rotate(-90deg)` 로 세운 것이라 **트랙 길이를 슬롯 높이에 맞춰야 합니다**
  (`sizePosSlider()` + `ResizeObserver` on `#pSlotV`). `writing-mode` 세로 range 는 구형 사파리에서 동작하지 않습니다.
- 회전 방향이 `-90deg` 인 이유: 값 1이 **위쪽**에 오고, 가로선은 `v = 1 - g[key]` 라 값 1 = 선이 맨 위.
  `+90deg` 로 바꾸면 **손과 선이 반대로 움직입니다.**
- 조절자는 캔버스 위 오버레이이므로 그 영역에서는 선 드래그가 되지 않습니다 (정상).

**세로선 라벨(Center·Inner·Outer)은 캔버스 맨 위 (v1.15.0)**

- `V_LABEL_Y = 6` — 위에서 6px 갭을 두고 **맨 위 줄**에 붙입니다.
- 가로로 겹치면 17px 씩 아래 줄로 내려갑니다(기존 규칙 유지).
- **왼쪽 위 칩(`all line` · AI 안내)의 사각형을 피해서** 줄을 고릅니다.
  칩 위치는 `offsetLeft/offsetTop` 으로 계산합니다 — `getBoundingClientRect` 는 회전 시 어긋납니다 (1-6 참고).
- 아래를 좌·우 도크가 모두 차지하므로 **AI 안내 칩(`.aistatus`)은 왼쪽 위**(`all line` 바로 아래)에 둡니다.

**선 선택 — 한 줄 모드 / 여러라인 모드 (v1.18.0)**

| 모드 | 라인 버튼 · 화면의 선을 탭 | 드래그 |
| --- | --- | --- |
| **한 줄** (기본) | 1탭 = 선택(움직일 수 있음) · **같은 것 다시 탭 = 숨김/표시** | 그 선 하나 |
| **여러라인** (`여러라인` 버튼 ON) | 누를 때마다 **선택에 추가** · **다시 누르면 선택 해제**(숨기지 않음) | **선택된 선 전부가 함께** |

> ⚠️ **레일 버튼과 화면(캔버스) 탭은 반드시 같은 규칙이어야 합니다** (v1.18.1 에서 고침).
> v1.18.0 은 레일 버튼에만 "다시 탭 = 숨김"이 들어 있었고 **화면의 선을 탭하면 선택만 되고 숨겨지지 않았습니다.**
> 캔버스 쪽은 `pointerdown` 에서 **`setSel()` 이 `S.sel` 을 덮어쓰기 전에** 판정값을 잡아둬야 합니다:
> ```js
> wasSel = !S.multi && S.sel === key && S.hMode === "line";   // pointerdown, setSel() 앞
> // endPointer: 데드존을 넘지 않았고(!moved) wasSel 이면 visKeyOf(key) 를 step() 으로 토글
> ```
> `S.hMode === "line"` 조건이 있는 이유는 레일 버튼과 같습니다 — **사진 보정 중이던 첫 탭은
> 선 조절로 되돌리는 역할만** 하고 숨기지 않습니다.
> 키 → 표시여부 키 변환은 **`visKeyOf()`** 를 쓰세요 (`innerAngle`/`outerAngle` → `baseStructureVisible`).
> 숨김은 실제 편집이므로 `step()` 으로 감싸 **되돌리기 1단계로 기록**합니다.
> 반면 **아직 선택돼 있지 않던 선을 처음 탭하는 것(선택만)은 기록하지 않습니다** (회귀 42번).

- 상태: `S.multi`(모드) / `S.selSet`(선택된 키 배열). `selSet` 이 비면 단일 선택 `S.sel` 로 폴백합니다.
- 그리기·버튼 강조는 모두 **`isSelected(key)`** 를 씁니다. `S.sel === key` 로 되돌리면 여러라인이 하나만 굵어집니다.
- 동시 이동은 **`dragManyBy(keys, baseAll, dxN, dyN, mirroredKey)`** — 제스처 시작 시점의 `baseAll = {...S.g}` 기준 델타(1-4).
  **`Center(v1)` 가 선택에 들어 있으면 세로선 그룹은 `v1` 하나만 움직입니다** — v1 이 나머지를 이미 끌고 가므로(1-2)
  같이 움직이면 2배로 밀립니다. 이 규칙을 빼지 마세요.
- 탭과 드래그 구분은 기존 **3px 데드존**을 그대로 씁니다. 데드존을 넘으면 잡은 선이 선택에 합류하고,
  넘지 않으면 `pointerup` 에서 선택 토글만 합니다.
- 여러라인 모드를 켜면 현재 선택(`S.sel`) 하나를 seed 로 넣고, 끄면 `selSet` 을 비웁니다.
- **두 드래그 바(위아래·좌우)는 언제나 자기 축의 대표 선 하나만** 움직입니다 (`S.selUD` / `S.selLR`).
  여러라인 동시 이동은 **화면 드래그** 전용입니다.
- 선택 상태는 되돌리기 스냅샷에 넣지 않습니다 (1-9) — 되돌려도 선택은 그대로.

**버튼 이름 (v1.19.0)**

| 버튼 | 하는 일 |
| --- | --- |
| `모든 라인 숨김` (`btnAllLine`) | 지금 보이는 선을 전부 숨김 / 다시 누르면 복원 |
| `여러라인` (`btnMulti`) | 여러라인 모드 ON/OFF. 캔버스 왼쪽 위, `모든 라인 숨김` 바로 옆 |
| `전체라인` (`btnAllSel`) | **여러라인의 후속 버튼** — 여러라인이 꺼져 있으면 `hidden`. 화면에 보이는 선을 **한 번에 전부 선택**, 다시 누르면 전체 해제 |

- `전체라인` 은 `visibleLineKeys()` 를 씁니다 — **숨긴 선은 넣지 않습니다** (안 보이는 선이 같이 끌려가면 안 됨).
- **`outerAngle`(V 앵글)은 전체 선택에 넣지 않습니다.** 위치가 아니라 **각도**라서 같이 끌면 V 모양이 일그러집니다.
  `innerAngle`(V 피봇)은 위치이므로 포함합니다.
- 전체라인이 켜져 있는지는 `updateButtons()` 가 `visibleLineKeys().every(선택됨)` 으로 판단해 `.on` 을 붙입니다.

**선 선택 (탭)**

- 화면의 선을 **탭하면 그 선이 선택**되고, 조절자가 그 선의 축으로 바뀝니다. 다른 선을 탭하면 즉시 전환됩니다.
- 탭과 드래그를 구분하려고 **3px 데드존**을 둡니다 — 손가락 떨림으로 선이 밀리지 않습니다.
  데드존을 넘으면 **처음 누른 지점 기준 전체 이동량**을 적용하므로 정확도는 그대로입니다
  (넘어선 시점으로 기준을 옮기면 이동량이 3px 부족해집니다 — 그렇게 바꾸지 마세요).

**드래그 규칙 (v1.11.0) — 사진 잠금이 모드 스위치다**

| 손가락 | 잡은 곳 | 사진 잠금 | 결과 |
| --- | --- | --- | --- |
| 1개 | **선 위** (`HIT_PX` 28px 이내) | 무관 | 그 선이 선택되고 손을 따라 이동 |
| 1개 | 빈 곳(사진) | **해제** | **사진을 사방으로 자유 이동** (팬) |
| 1개 | 빈 곳(사진) | **잠금** | 이미 선택된 선이 끌림 (미세조정) |
| 2개 | 어디든 | 해제 | 줌 · 회전 · 이동 |
| 2개 | 어디든 | 잠금 | 아무 일도 일어나지 않음 |

- **잠금 = 시술 중 미세조정 모드**입니다. 사진을 고정해 두고 화면 어디를 끌어도 선만 움직입니다.
- **해제 = 사진 배치 모드**입니다. 사진을 한 손으로 자유롭게 밀고 두 손으로 확대합니다.
- 팬 한계는 `panLimit() = OFFSET_MAX × max(1, zoom)` — **배율에 비례**합니다.
  고정값으로 되돌리면 확대했을 때 사진이 중간에 걸려 부자연스러워집니다.
- 어느 쪽이든 **손가락 이동량을 그대로 따라갑니다** (절대 위치 점프 없음 — `dragLineBy()` 는 델타 기반).
  데스크톱 보조: `Shift` + 드래그 = 사진 이동.
- 대칭선의 거울쪽(`v3`/`v5`)을 잡았을 때는 부호를 뒤집어(`mirrored`) 손가락 방향과 일치시킵니다.
- **손가락 방향 = 화면에서 보이는 방향**입니다. 강제 가로(`rot90`) 중에도 마찬가지 — 좌표 변환은 1-6 참고.

> ⚠️ **절대 위치 방식으로 되돌리지 마세요.** 선에서 떨어진 곳을 잡으면 선이 손가락 위치로
> 순간이동해서 미세조정이 불가능해집니다. 반드시 `기준값 + 델타` 로 계산하세요.

### 1-11. 메인 작업 영역 (v1.17.0)

**캔버스 왼쪽 끝 ~ 오른쪽 위아래 드래그 바 왼쪽 끝**이 실제 작업 공간입니다.

```js
const WORK_GAP = 8;
function workRight() {                       // 0~1 정규화
  const d = $("rightDock"), W = S.dim.W;
  if (!d || !d.offsetWidth || !W) return 1;  // 도크가 아직 없으면 전체
  return clamp((d.offsetLeft - WORK_GAP) / W, 0.5, 1);
}
const centerX = () => workRight() / 2;       // 자동 정렬의 가로 기준점
```

| 적용 대상 | 내용 |
| --- | --- |
| **가로 가이드 선** | `segs` 의 x 를 `W` 가 아니라 **`WR = workRight() * W`** 에 곱해 그립니다. 오른쪽 컨트롤·스크림 위로 넘어가지 않습니다 |
| **Base Structure 오른쪽 팔** | 끝점을 `WR` 로 |
| **세로선 라벨 배지** | `drawBadge()` 가 `S.wr` 로 클램프 |
| **선 히트테스트** | `hitTest()` 의 가로선 구간도 `S.wr` 기준 — 그리는 범위와 잡는 범위가 같아야 합니다 |
| **사진·라인 자동 배치** | `centerX()` 를 기준으로 (위 2번 표) |

- `rightDock.offsetLeft` 는 **레이아웃 좌표**라 회전(rot90) 에서도 정확합니다. `getBoundingClientRect` 로 바꾸지 마세요 (1-6 참고).
- `renderGuides()` 가 매 프레임 `S.wr` 을 갱신합니다. 이 갱신을 빼면 히트테스트가 옛 값을 씁니다.
- **세로선은 위아래 전체 높이 그대로** 둡니다 (얼굴 중심선이라 끝까지 보여야 함).

### 1-8. 사진 잠금의 정의

`🔒 사진잠금` 은 **사진만** 잠급니다. 선은 잠긴 상태에서도 계속 조절할 수 있습니다.

- 잠금 시 비활성: 사진 팬·핀치 줌·회전 제스처, 사진 보정 슬라이더·`−`/`+`·모드 버튼
- 잠금 중에도 동작: 라인 드래그, 위치 조절 슬라이더·방향 버튼, 라인 표시/숨김, 눈 가이드 토글
- 버튼은 **상단 바와 `사진 보정` 칸 두 곳**에 있고 서로 상태가 동기화됩니다 (`toggleLock()`).

### 1-9. 되돌리기 (undo) — 한 번에 한 작업 (v1.12.0)

`↶ 되돌리기` 는 **직전 작업 하나만** 되돌립니다. 다시 누르면 그 전 작업을 되돌립니다.

**"작업" 하나의 정의 — 사용자가 손을 뗄 때까지**

| 작업 | 시작 | 끝 |
| --- | --- | --- |
| 드래그 · 핀치 제스처 | `pointerdown` | 마지막 손가락 `pointerup` |
| 슬라이더 끌기 | 슬라이더 `pointerdown` | `change` / `pointerup` / `blur` |
| 버튼 한 번 (± · 숨김/표시 · 초기화 · 프리셋 적용 · all line · 눈가이드 · V버튼) | `step(fn)` 안 | `step(fn)` 밖 |

```js
beginEdit();   // 스냅샷 {g, p, hiddenSnapshot}
  …값 변경…
commitEdit();  // 값이 실제로 바뀐 경우에만 스택에 push
```

- **`commitEdit()` 은 값이 그대로면 기록하지 않습니다.** 그래서 선을 탭만 한 경우(선택만)는 되돌리기 단계가 생기지 않습니다.
  이 비교(`sameState`)를 빼면 탭 한 번마다 빈 단계가 쌓여 되돌리기가 먹통처럼 보입니다.
- 스냅샷에 **`S.sel` 은 넣지 않습니다** — 되돌려도 선택은 그대로여야 손이 헷갈리지 않습니다.
**다시 실행 (redo) — 되돌리기 바로 위 (v1.19.0)**

`↷ 다시 실행` 은 **되돌린 작업을 앞으로 되감습니다**. 두 스택이 서로 상태를 주고받습니다.

```js
undo()  : S.redo.push(현재)  →  S.g/p/hs = S.hist.pop()
redo()  : S.hist.push(현재)  →  S.g/p/hs = S.redo.pop()
commitEdit() : 새 작업이 기록되면 S.redo = []      // 갈래(branch)는 버린다
```

- **새 작업을 하면 다시 실행 스택을 반드시 비우세요.** 안 비우면 존재하지 않는 미래로 되감아 값이 뒤엉킵니다.
- 두 버튼 모두 스택이 비면 `disabled`(반투명) — `updateUndoBtn()` 이 둘을 같이 관리합니다.
- 스택 상한 `HIST_MAX = 60`. 새 사진을 불러오면 `clearHist()` 로 **둘 다** 비웁니다.
- 스택이 비면 버튼이 `disabled`(반투명) 됩니다 (`updateUndoBtn()`, `updateButtons()` 에서 호출).
- 값을 바꾸는 새 UI 를 추가하면 **반드시 `step()` 으로 감싸거나 `beginEdit`/`commitEdit` 쌍을 붙이세요.**
  빠뜨리면 그 작업만 되돌릴 수 없습니다.

### 1-13. 디자인 시스템 (v1.20.0) — 규칙은 딱 하나

> **그라데이션과 글로우는 "지금 켜져 있는 모드"에만 쓴다.**
> 왼쪽 레일(목록)과 **사진 위 가이드 선은 절대 발광하지 않는다.**

이 한 줄이 "액센트가 파란 계열인데 Arch 가이드 선도 파랗다"는 문제를 색을 바꾸지 않고 해결합니다.
빛나면 내가 누르는 것, 안 빛나면 눈썹 선 — **색이 아니라 재질로 구분**합니다.

**색 토큰 (`index.html` `:root`)**

| 토큰 | 값 | 쓰임 |
| --- | --- | --- |
| `--stage` / `--chrome` | `#0A0C12` / `#0D1018` | 캔버스 / 도구 영역. **레일을 흰색으로 되돌리지 마세요** — 화면에서 제일 밝은 건 사진이어야 합니다 |
| `--glass` / `--hair` | `rgba(255,255,255,.035)` / `.10` | 유리 컨트롤 바탕 / 테두리 |
| `--a1` / `--a2` / `--a2b` | `#22D3EE` / `#67E8F9` / `rgba(34,211,238,.42)` | 액센트 켜짐 / 글자·아이콘 / 흐린 테두리 |
| `--grad` | `#22D3EE → #7C8CF8 → #A78BFA` | **켜진 모드에만.** 레일에는 쓰지 않습니다 |
| `--glow` | `rgba(90,180,255,.42)` | 위와 같음 |
| `--danger` | `#FF6B7A` | 밸런스 어긋남 표시 · 삭제 |
| `--danger-d` | `#F0384C` | **초기화 전용 (v1.28.1)** — 위 색보다 한 단계 짙게. 같은 빨강이면 위계가 안 보입니다 |

**버튼 위계 — 다 똑같이 생기면 아무것도 안 중요해 보입니다**

| 등급 | 대상 | 모양 |
| --- | --- | --- |
| **주요** | `사진잠금` **하나** (모드 스위치·제일 자주 씀) | **꺼짐 = `--grad-deep` 채움 + 흰 글자 / 켜짐 = `--grad` 채움 + 어두운 글자.** 꺼져 있을 때도 채움을 유지해 늘 시선을 잡고, 상태는 **밝기**로 구분합니다 (v1.50.0 원장님 지시: 「사진 잠금이 더 행동에 필요한 시선을 잡아야 한다」) |
| **보조** | `사진저장` | ⛔ v1.45.0 의 그라데이션 채움을 되살리지 마세요. 시술을 시작하는 시점에 저장할 것은 없는데 화면에서 제일 밝았습니다 (v1.50.0 원장님 지시: 「시작시 사진저장에 색상 죽일것」) |
| **켜짐 신호** | `가이드` | 켜지면 `--grad` 채움 + 글로우 (v1.50.0 「이것이 켜져있다는 신호가 보여야 한다」) |
| **보조** | 사진변경 · 프리셋 · 즐겨찾기 ★ · 되돌리기 · 다시실행 | 유리(반투명 + 헤어라인). 되돌리기·다시실행은 **쓸 수 있을 때만** 액센트로 살아남 |
| **위험** | `초기화` | **`.dockbtn.danger`** — `--danger-d`(짙은 빨강) + 오른쪽 도크 맨 위, `다시 실행` 과 띄움 — 3장 참고 |

**아이콘 — 이모지 금지**

- 🖼 ☆ 👁 ⬇ 🔒 ↺ 같은 이모지는 **기기마다 모양이 다르고** 앱을 아마추어처럼 보이게 합니다.
  전부 `currentColor` 를 쓰는 **SVG 선 아이콘**(stroke-width 1.7, 19px)으로 교체했습니다. 회귀 62번이 지킵니다.
- **잠금 아이콘은 상태에 따라 모양이 바뀝니다** (`setLockIcon()` · `LOCK_SVG.on/off`).
  색만으로 구분하면 역광·화면 기울임에서 안 읽힙니다. 회귀 65번.

**라인 버튼 — 버튼 전체를 선 색으로 칠하지 마세요**

```js
b.style.setProperty("--dot", spec.dot || spec.color);   // 왼쪽 색 띠만 선 색
```

- 왼쪽 **색 띠** = 어느 선인지 / **채움 + 굵은 글씨** = 선택됨(지금 움직일 것).
  두 정보가 서로 다른 자리에 있어서 충돌하지 않습니다. 예전처럼 `b.style.background = spec.color` 로
  되돌리면 버튼이 빨강·파랑 덩어리가 되어 **실제 가이드 선 색과 싸웁니다.** 회귀 64번.
- 어두운 중성선(`front`·`F.T`·`Center`·`Inner`)은 **선은 어둡게, 띠는 밝은 회색**(`dot: "#C9D1E0"`)입니다.
  띠까지 어둡게 하면 다크 레일에서 안 보입니다.

**모달·홈도 같은 다크 테마 — `background:#fff` 로 남겨두지 마세요 (v1.20.1)**

> v1.20.0 에서 `:root` 토큰을 다크로 뒤집었는데 `.modal` 만 `background:#fff` 로 남아
> **밝은 글자 + 흰 바탕 = 아무것도 안 보이는** 사고가 났습니다. 토큰을 바꿀 때는
> **그 토큰을 쓰는 화면 전부**(홈 · 모달 · 토스트 · 프리셋 목록)를 같이 확인하세요.

| 규칙 | 내용 |
| --- | --- |
| 모달 | `background:var(--surface)` + `border:1px solid var(--hair)`. 글자는 `var(--fg)` |
| **시안 채움 위 글자** | **`var(--on-accent)` (`#04141B`) — 어두운 글자.** `color:#fff` 를 쓰면 대비가 2:1 도 안 나옵니다 |
| 흰 글자를 쓰고 싶을 때 | `--grad-deep` (더 진한 그라데이션) 위에서만. 홈의 `사진 선택` CTA 가 이 경우입니다 |
| **한 모달에 채운 버튼은 하나** | 주 동작(`+ 현재 설정 저장`)만 채우고 나머지(`로드`·`수정`·`삭제`·`닫기`)는 **테두리형**.<br>줄마다 채운 버튼이 있으면 화면이 시끄럽고 뭐가 주 동작인지 안 보입니다 |
| 가로 화면 모달 | 세로가 짧아 잘리므로 `body.land.compact .modal{max-width:560px;max-height:88dvh}` 로 넓게 |

- 회귀 66·67·68 번이 **실제 대비값을 계산해서** 검사합니다 (모달 ≥7:1, 액센트 버튼 ≥7:1, 채운 버튼 1개).

**가이드 선 색 — 원색 대신 발광색**

| 선 | 이전 | 지금 | 이유 |
| --- | --- | --- | --- |
| Eye | `#FF0000` | `#FF3B4E` | 채도 100% 원색은 피부톤 위에서 눈이 아픕니다 |
| Arch · A.T · Outer | `#0066FF` | `#2E8BFF` | 〃 |
| Tail | `#7B2CBF` | `#A855F7` | 〃 |
| Front · F.T · Center · Inner | `#111111` | `#14161B` | **어둡게 유지** — `drawLine()` 의 흰 헤일로 덕분에 밝은 피부에서도 어두운 피부에서도 보입니다. 밝은 색으로 바꾸면 흰 헤일로와 겹쳐 사라집니다 |

- 색을 바꾸면 회귀 테스트가 참조하는 `window.PB.LINE_COLORS` 도 같이 고치세요.

### 1-14. 세로선 ↔ 가로 자 묶음 (v1.32.0) — 자는 자기 기둥 위에 올라간다

> ⚠️ **원장님이 직접 찾아내신 문제입니다** (2026-08-20):
> 「아우터라인과 아치 아치두께 라인이 함께 움직여」
> v1.31.x 까지 자의 x 가 **frac 상수**로 박혀 있어, 아치 자가 **아우터**를 따라 움직였습니다.

**세 묶음** — 세로선을 옮기면 그 묶음의 가로 자가 같이 따라옵니다.

| 묶음 | 세로선 | 가로 자 | 색 |
| --- | --- | --- | --- |
| **꼬리** | 아우터 `v4/v5` | 꼬리 | **보라 `#A855F7`** |
| **아치** | **아치선 `v6/v7`** (v1.32.0 신설, 아우터보다 **얇게** `w 1.2`) | 아치 · 아치두께 | 파랑 `#2E8BFF` |
| **앞머리** | 이너 `v2/v3` | 앞머리 · 앞두께 | 검정 `#14161B` |

```js
const SEG_HALF = 0.19;   // 자 반폭 (눈썹 폭 = |v2 − v4| 기준)
const BROW_PAD = 0.022;  // 눈 기준선이 아우터 바깥으로 더 나가는 여유
segPx(sp)     →  그 가로선의 좌·우 토막 [x0px, x1px]. anchor 세로선을 **가운데** 두고 잡는다
browBandY()   →  모든 가로선 값의 min−VPAD ~ max+VPAD (세로선이 진하게 보이는 구간)
```

| 규칙 | 내용 |
| --- | --- |
| **자 위치는 `anchor` 로** | `H_SPECS[].anchor` = `"v2"` / `"v6"` / `"v4"`. ⚠️ **frac 상수(`segs`)를 되살리지 마세요** — 그게 이 버그의 원인이었습니다 |
| 자 폭 | **눈썹 폭에 비례** (`SEG_HALF × |v2 − v4|`). 확대하거나 얼굴이 바뀌어도 관자놀이·코까지 뻗지 않습니다 |
| 눈 기준선(h1) | `anchor` 가 없습니다 — 아우터 바깥(`BROW_PAD`)부터 거울상까지 **좌우 관통** |
| **히트테스트** | `hitTest()` 도 반드시 `segPx()` 를 씁니다 — 그리는 범위와 잡는 범위가 다르면 **보이지 않는 곳에서 선이 잡힙니다** (1-11) |
| 밸런스 | `runBalance()` 도 `segPx()` 로 잽니다. 세 곳(그리기·잡기·재기)이 늘 같아야 합니다 |
| 세로선 | **Center(v1) 만 위아래 전체 길이.** 이너(`long: true`)는 `browBandY(false)`(눈까지), **아치선·아우터는 `browBandY(true)`** — 눈썹 가로선만 감싸는 짧은 구간 (v1.33.0 원장님 지시: 「이너라인 빼고 더 얇게 짧게 · 눈 위치까지 내려오지 않아도 된다」). 라벨까지는 opacity 0.16 연결선 |
| 라벨 연결선 | 지우면 맨 위 라벨이 **허공에 뜬 것처럼** 보입니다 |
| 겹침 방어 | 토막 폭이 2px 미만이면 그리지 않습니다 |
| `browBandY()` 는 **표시 여부와 무관하게** 모든 가로선 값을 씁니다 | 선을 껐다 켤 때마다 세로선 길이가 흔들리면 안 됩니다 |

**자동 배치** — 아치선도 자동으로 올라갑니다.

| 세로선 | 얼굴 인식만 (전부 **기준쪽 실측** · v1.38.0) | 드로잉을 읽었을 때(드로잉 맞춤 버튼) |
| --- | --- | --- |
| 이너 `v2` | **기준쪽** 내안각 | 드로잉의 **안쪽 끝** |
| **아치선 `v6`** | **기준쪽** 눈썹 산 랜드마크 | 드로잉에서 **제일 높은 곳의 x** |
| 아우터 `v4` | **2단계** (v1.41.0 · 원장님 지시 2026-08-21: 「1. 사진에 보이는 꼬리에 선을 올린다 2. 보이지 않을경우 얇은 눈썹은 드로잉이라고 처리하지 않는다. 얼굴의 기본 꼬리위치를 계산하여 …」) — ① **보이는 꼬리**: 강한 대비(`DRAW_CONTRAST`)로만 잉크를 찾고, 표본 `TAIL_VIS_MIN`(6) 이상일 때 그 잉크의 끝 (잉크 30%·2열 연속) ② **안 보이면**: 콧볼(`NOSE_ALA` 64/294)–외안각 연장선 ∩ 꼬리 높이(h3). 랜드마크 꼬리점 대비 0.8~1.5배 제한. ⚠️ ①을 **약한 대비(SOFT)로 바꾸지 마세요** — v1.35~v1.39 에서 네 번 실패: 옅은 잔털이 눈썹과 끊김 없이 이어져 약한 대비로는 끝을 못 정합니다 | 드로잉의 **바깥 끝** |

> ⚠️ **아우터를 외안각(눈꼬리 `33/263`)으로 되돌리지 마세요.** v1.34.0 까지 그랬고,
> 눈썹 꼬리는 눈꼬리보다 훨씬 바깥이라 **꼬리 자가 눈썹 한가운데** 떨어졌습니다 —
> 원장님 지적(2026-08-21): 「꼬리는 눈썹의 끝 뾰족한 부분인데」. 회귀 97번이 잡습니다.
> 픽셀 연장은 반드시 **바깥 방향으로만 · 한계 안에서만** — 풀면 관자놀이 머리카락을 잡습니다.
> **꼬리 자(h3)의 높이는 랜드마크 그대로** — 원장님 확인(2026-08-21): 「꼬리 가로선은 맞다」.
> **화면 맞춤(fitBrowsInFrame)은 랜드마크 꼬리와 아우터 반폭(재계산) 둘 다** 보고,
> **수렴할 때까지(최대 4회) 반복**합니다 — 꼬리 잉크가 화면 밖까지 이어지면 측정이
> 화면 끝에서 잘려 한 번의 축소로는 부족합니다. g.v4 는 0~1 로 잘려 있어 그 값으로
> 판정하면 안 됩니다.

- 회귀 **93**번이 「아우터를 옮기면 꼬리만 따라오고 아치는 가만히 / 아치선을 옮기면 아치만 따라온다」를
  검사합니다. 색(아우터=보라, 아치=파랑)과 아치선이 더 얇은 것까지 봅니다.
- 회귀 48번이 **이너·아우터를 좁히면 가로 자도 같이 짧아지는지**를 검사합니다.

**라벨이 위쪽 칩과 겹치지 않게** — `renderGuides()` 의 `blocks` 계산

> ⚠️ `offsetLeft` 는 **CSS transform 을 반영하지 않습니다.** `밸런스`·`드로잉 맞춤` 묶음은
> `translateX(-50%)` 로 놓여 있어, 이걸 빼먹으면 좌표가 통째로 어긋나 라벨이 그 위에 겹칩니다.
> `offsetParent` 를 **끝까지** 거슬러 올라가면서 각 단계의 **transform 이동량까지** 더하세요.
> 회전(rot90) 때문에 앱 안에서는 `getBoundingClientRect` 를 쓸 수 없습니다 (1-6).
> 피해야 할 칩을 **하나라도 빼면** 라벨이 그 글자 위에 겹쳐 둘 다 안 읽힙니다.

### 1-15. AI 측정 배치 (v1.22.0) — 고정 오프셋을 쓰지 마세요

앱은 **MediaPipe FaceLandmarker**(478점)를 기기 안에서 돌립니다. 사진은 어디에도 올라가지 않습니다 (1-10).

**눈썹 윤곽 인덱스 — 위/아래가 따로 있어 두께를 실측할 수 있다**

| 부위 | 위 윤곽 | 아래 윤곽 | 쓰는 곳 |
| --- | --- | --- | --- |
| 꼬리 | `70` / `300` | `46` / `276` | `h3` |
| 산(아치) | `105` / `334` | `52` / `282` | `h2` / `archThickness` |
| 앞머리 | `107` / `336` | `55` / `285` | `front` / `frontThickness` |
| 홍채 | `468~472` / `473~477` | — | `h1` |
| 눈 끝 | `33·133·362·263` (x 순 정렬) | — | `v1` / `v2` / `v4` |

- **두께 선을 `g.h2 + 0.035` 같은 고정 오프셋으로 되돌리지 마세요.** 고객마다 눈썹 두께가 달라 안 맞습니다.
  아래 윤곽을 실측하는 `AI_LM` 을 씁니다. 회귀 69번.
- 이 인덱스는 MediaPipe FaceMesh 규약입니다. **모델을 바꾸면 반드시 같이 확인**하세요.

**선을 켤 때마다 그 사진에서 측정한 자리로 (v1.22.0)**

```js
aiValueFor(key)   // 지금 화면 변환(S.p) 기준으로 그 선이 있어야 할 값. 랜드마크 없으면 null
aiPlaceLine(key)  // setLine() 을 거쳐 스냅. 배치했으면 true
```

- 꺼져 있던 선을 **켤 때만** 스냅합니다. 이미 켜진 선을 손으로 맞춰둔 걸 덮어쓰면 안 됩니다.
- **랜드마크가 없으면 조용히 실패**하고 마지막 값을 그대로 씁니다 (얼굴 인식 실패 · 눈만 잘라낸 사진).
  토스트로 시끄럽게 알리지 마세요 — 시술 중입니다. 회귀 72번.
- `aiValueFor` 는 **매번 현재 `S.p`** 로 다시 계산합니다. 사진을 확대·이동해도 맞는 자리에 올라옵니다.

**중심축 = 양쪽 내안각의 중점 (동공 중점이 아님)**

```js
g.v1 = (내안각L + 내안각R) / 2
halfIn = (|내안각L − v1| + |내안각R − v1|) / 2     // 좌·우 실측의 평균
```

- 중심축을 내안각 중점으로 잡으면 **이너 바가 좌우 내안각에 정확히 둘 다 닿습니다** (거리가 같아지므로).
- 비대칭의 오차는 아우터로 옮겨가고, 평균을 쓰므로 **좌우에 똑같이 나뉩니다.**
  한쪽만 재서 거울상을 만들면 한쪽이 크게 뜹니다. 회귀 70번.
- 대칭 규칙(1-2)은 그대로입니다 — 값은 반드시 `setLine()` 을 거칩니다.

**눈썹이 잘리지 않게 (`fitBrowsInFrame`)**

`autoAlign()` 은 동공 간 거리를 캔버스 폭의 `EYE_FRAC`(44%)로 **고정 확대**합니다.
얼굴이 넓거나 눈 간격이 좁은 고객이면 **눈썹 꼬리가 프레임 밖으로 밀려납니다** (실제로 잘렸습니다).

- 정렬 직후 꼬리(`70`·`300`)가 `FRAME_PAD`(6%) 안쪽에 들어오는지 재고, 넘치면 **배율만** 낮춥니다.
- 배율이 바뀌면 위치·선을 `autoAlignRelayout()` 으로 한 번만 다시 계산합니다 (재귀 금지).
- 회귀 71번이 양쪽 꼬리가 화면 안에 있는지 검사합니다.

### 1-61. ⭐ 왼쪽 레일 정돈 · 위아래 바 30% 단축 (v1.98.0 · 원장님 지시 2026-08-29)

> 「왼쪽 선 이름 버튼들 전부 조금 더 얇게 · 꼬리와 센터, 아우터와 센터피봇 간의 간격을 없애고
>   붙여서 · 버튼들의 배치를 중앙을 기점으로 · 위아래 드래그바의 길이를 30% 짧게, 밑 배치된
>   위치 고정(위가 짧아져서 내려온다) · 드래그바를 누를 때 왼쪽 선 이름 버튼이 눌리니
>   조금 더 떨어뜨려 배치」

- **버튼 얇게**: `.lbtn` 25 → **21px**(compact 23 → 20px), 글자 9.5 → 9px.
- **붙여서**: 레일 그룹 간격 7 → **2px**(그룹 안과 동일) · 꼬리↔센터·아우터↔V센터피봇의
  여백과 구분선(`#hButtons::after` · `#railExtra::before`) 제거 → 12개가 한 덩어리.
  compact 의 `.linebar{padding:3px}` 는 **세로만 0** 으로 (가로 3px 유지).
- **중앙 기점**: 선 버튼 덩어리만 레일 정중앙에 온다. 실측 오차 0px(844×390 · 1180×820 둘 다).
- **설정 배지 자리** (v1.98.1 · 원장님 지시 「설정 버튼을 눈 버튼과 살짝 갭을 두고 눈버튼 위로」):
  `#railLang` 은 흐름 안에 두되 **음수 margin-top 으로 자기 높이를 상쇄**해 가운데 정렬
  계산에는 0 으로 잡힌다 → 눈 버튼 바로 위 **8px 갭**에 뜨면서 중앙 정렬은 그대로.
  `margin-top = −(박스높이 + 레일 gap)` = −32px (compact −30px). 구분선은 제거(갭으로 구분).
  ⛔ margin-top 을 지우면 선 버튼이 아래로 밀려 중앙이 깨집니다.
- 🐛 **`#lineRail #hButtons{position:relative}` 버그**: `.linebar{position:absolute;bottom:8px}`
  의 `bottom` 이 되살아나 가로 버튼 묶음이 **8px 위로** 밀려 설정 배지와 겹쳤습니다.
  구분선이 사라진 지금은 `position:static` 으로 못 박습니다. ⛔ relative 로 되돌리지 마세요.
- **위아래 바**: 높이 48% → **33.6%**(=×0.7 · compact 58% → 40.6%), min-height 185 → 130px.
  가운데 정렬 박스가 줄면 아래 끝이 0.15h 올라가므로 **자기 높이의 21.43%** 를 추가로 내려
  상쇄 → **아래 끝 고정, 위쪽만 짧아짐**(실측: 이전과 아래 끝 동일, 높이 정확히 70%).
- **왼쪽 간격**: `.rdock` left 4 → **16px** — 바를 잡다 레일 버튼이 눌리지 않게.
  `workLeft()` 도 함께 밀리므로 사진 기준점은 원장님 정의(드래그 바 옆 ~ 화면 끝 정중앙) 그대로.
- 회귀 **150** 이 위 전부를 두 기기 폭에서 잠급니다.

### 1-60. ⭐⭐ **예비 동공 정렬** — 인식 실패 사진도 같은 크기·자리 (v1.97.0 · 원장님 지시 2026-08-29)

> 「첫 사진 크기가 모두 달라 동공 위치가 달라져 사용감이 나쁘다 — 동공 위치를 파악해
>   처음부터 동공이 비슷한 위치·비슷한 크기에 오도록 고도화」

- 얼굴 전체 사진은 MediaPipe → `autoAlign` 이 이미 동공 간격을 EYE_FRAC(44%)으로 통일한다.
  문제는 **눈 부위만 확대 촬영한 사진** — 얼굴 인식이 실패해 보정이 아예 안 걸렸다 (실기기 확인).
- `findPupilsFallback()`: 사진 픽셀에서 어두운(하위 6%) 연결 덩어리 중 **동그란 것**만 후보로
  (aspect 0.35~2.4 · 채움 45%↑ · 크기 상한) — 눈썹(길쭉)·머리카락(큼)·콧구멍 쌍(간격 좁음) 배제.
  한 쌍 조건: 가로 간격 12~80% · 기울기 ±22° · 크기비 3.2배 이내.
- 찾으면 `alignFromPupils()` 로 성공 경로와 **같은 규칙**(간격 44% · 중점 = centerX/CENTER_Y ·
  기울기 보정 · placeLinesFromEyes) 적용. `runFaceAI` 의 no-face·catch 두 실패 경로에서 호출.
- **SVG 사진은 건드리지 않는다**(S.photoType) — 회귀 픽스처의 변환 안정성. 회귀 149가 잠근다.
- ⛔ 성공 경로를 이것으로 대체하지 마세요 — 랜드마크가 늘 더 정확합니다.

### 1-59. ⭐ 위쪽·왼쪽 배치 미세 조정 · 사진 기준점 42% (v1.96.0 · 원장님 지시 2026-08-29)

> 「밸런스·왼쪽·오른쪽은 배경이 조금 있는 칸 하나에 넣어 여러라인 오른쪽에 간격 두고 배치」
> 「다시실행·되돌리기는 초기화 밑에 1행 — 초기화 밑이 되돌리기, 그 왼쪽이 다시실행」
> 「위아래 드래그바는 조금 더 왼쪽으로 · 길이 유지하고 조금 더 아래로」
> 「사진 중앙 정렬은 더 왼쪽으로 — 지금 오른쪽으로 너무 치우침」

- **밸런스 묶음**(`#balBox`): `.ov-tl` 안, 여러라인 뒤 10px 간격 · 옅은 배경+테두리 칸.
  v1.89.0 의 오른쪽 끝 정렬(.ov-bal)은 폐지 — 오른쪽 위는 초기화·다시실행·되돌리기 자리.
- **다시실행·되돌리기**(`.urow`): 초기화 바로 밑 1행, 오른쪽 끝 정렬 — [다시실행][되돌리기],
  되돌리기가 초기화 바로 아래. 칩 크기(34px)로 축소 (왼쪽 도크 특대 46px 폐지).
- **위아래 바**: 왼쪽 4px · `transform:translateY(22px)` 로 길이 그대로 조금 아래
  (원장님 확인: 「늘리라는 게 아니고 길이 유지하고 조금 더 밑으로」).
- **사진 기준점**: `CENTER_BIAS` — centerX = workLeft + BIAS×(workRight−workLeft).
  50%는 오른손 액션 공간까지 얼굴이 밀려 보였습니다. ⛔ 0.5 로 되돌리지 마세요.
  · v1.96.0 = **0.42** → v1.97.1 = **0.45** (원장님 실기기 확인 2026-08-29 「조금만 더 오른쪽으로」).
  · **v1.97.2 = 0.5 (확정 정의)** — 원장님이 기준을 정확히 정의하셨습니다 (파란 빗금 스크린샷):
    「왼쪽은 드래그바 옆, 오른쪽은 화면 끝으로 측정한 정중앙 — 얼굴과 배경 여백이 비슷하게」.
    같은 지시로 세로 기준 **CENTER_Y 0.60 → 0.56** (「고객 얼굴의 중앙은 조금 위로」).
    회귀 6·44 는 두 상수(PB.centerX·PB.CENTER_Y)를 직접 읽으므로 값만 바꾸면 따라옵니다.
- 안내(.guidetip)는 v1.96.0 에서 top 44px 로 내렸다가, **v1.97.3 에서 맨 위(2px)** 로
  (원장님 지시 2026-08-29 「맨위 카메라 끝으로」). 터치 통과라 조작은 안 막습니다.
- 회귀 6·24·39·44·83 갱신.

### 1-58. ⭐⭐ 놓은 선 · 서브 라인 설정 · **도크 왼쪽 이동** · 배경 탭 = 다음 단계 (v1.95.0 · 원장님 지시 2026-08-29)

> 「놓은선 기본 설정 추가 — 선 굵기·색상·투명도. 놓은선은 체크를 마무리하고 전체를 보는 선」
> 「아치엣지와 아치두께에서 뻗어 이너라인까지 닿는 서브 라인의 굵기·투명도」
> 「위아래 드래그·다시실행·되돌리기를 왼쪽 정렬로 — 오른손 액션 공간 확보.
>   초기화는 오른쪽 그대로, 위 밸런스 행에 위치 맞출 것」
> 「이미 맞은 라인은 가이드 블링킹 이후 배경 한 번 클릭으로 다음으로 — 설명도 넘어가라」

**① 놓은 선 (doneC/doneW/doneOp)**
- 체크를 마친 선(S.doneSet)은 이제 `drawDone()` 이 **놓은 선 전용 값**으로 그린다 — 잡은 선과 완전 분리.
- 설정 「잡은 선 · 놓은 선」 탭 아래 블록: 색상표(팔레트 8색) + 굵기/투명도 슬라이더.
- 미리보기: 잡은 선 탭에서 아치 자 = 잡은 모습, **아치두께 자 = 놓은 모습**.
- 기본값(흰색·85%·65%)은 기존 잡은 선 값과 같아 화면이 그대로다. 회귀 112·146.

**② 서브 라인 (subW/subOp)**
- 가로 자 → 이너선 옅은 연결선의 굵기·투명도가 설정을 따른다 (기본 1px·16% = 기존 그대로).
- 색은 먹색(#14161B) 고정 — 시끄러우면 안 되는 선. 기본 선 탭에 「서브 라인」 줄. 회귀 147.

**③ 도크 왼쪽 이동 · 작업 영역 [workLeft ~ workRight]**
- `.rdock`(위아래 바·다시실행·되돌리기)은 **왼쪽** 세로 중앙. id `#rightDock` 는 역사적 이름으로 유지.
- 초기화는 오른쪽 위(top 8px · 밸런스 행 높이) 단독 배치.
- `workLeft()` = 왼쪽 바 오른쪽 끝 + 여유 / `workRight()` = 거의 전폭(8px 여백).
  `centerX()` = 두 값의 가운데. ⛔ workRight 를 #rightDock 위치로 계산하지 마세요 —
  도크가 왼쪽이라 0.5 로 붕괴해 얼굴이 반쪽에 구겨집니다.
- 자(segPx)는 `workLeft` 아래로 안 들어간다 · 스크림은 `.scrim-l`(왼쪽) · 세로 조절자
  값 라벨은 바 **오른쪽**(캔버스 안쪽). 회귀 6·24·39·44·45·47·48 갱신.

**④ 배경 한 번 탭 = 단계 확인**
- 가이드 켜짐 + 차례 있음 + **배경**(선 없는 곳) 한 손가락 탭(3px 미만) → 그 단계를
  「끝냄」(doneSet · 놓은 선 색)으로 표시하고 다음 차례로 — 안내(설명)도 함께 이동.
- 인사(초기화셋팅) 중에는 안 넘어간다 — 인사는 선을 움직여야만 일찍 끝난다 (1-50).
- 선 위 탭은 기존 그대로 선택/숨김. 회귀 148.

### 1-57. ⭐⭐ 원장님 선 설정 = **초기 셋팅** 재확정 (v1.94.0 · 원장님 지시 2026-08-29)

> 「지금 선 설정을 초기 셋팅으로 저장」 — 설정 화면 스크린샷 3장 픽셀 판독으로 확정.

`LOOK_DEF` 가 원장님의 2026-08-29 실기기 설정으로 갱신됐습니다 (v1.60.0 확정값을 대체):

| 항목 | 값 |
|---|---|
| 기본 선 색 | 이너 **라임 #A3E635** · 아치 **민트 #5EEAD4** · 꼬리 **파랑 #2E8BFF** |
| 세로선 색 | 이너 민트 · 아치선/아우터 먹색 (v1.81.0 그대로) |
| 선 굵기 / 가로 길이 / 투명도 | **0.75 / 0.04(슬라이더 8%) / 0.55** |
| 테두리 | 없음 · 0% (그대로) |
| 잡은 선 | **흰색 심 #FFFFFF** · 테두리 없음 · 굵기 **0.85** · 투명도 **0.65** |

- 회귀 **112** 가 이 값을 통째로 잠급니다. ⛔ 바꾸려면 원장님 확인 먼저.
- 기존 기기의 `pb_look_v1` 저장값은 그대로 존중됩니다 (loadLook 병합) — 새 기기·「기본으로」 버튼이 이 값으로 시작합니다.
- LOOK_COMBOS(추천 조합)는 여전히 **색·테두리만** 바꿉니다.

### 1-56. ⭐⭐ 아래 도크 **자동 맞춤** · **무조건 가로** (v1.93.0 · 원장님 신고·지시 2026-08-29)

> 「휴대폰 크기·화면·사양에 따라 가로세로가 달라질 수 있는데 자동조정되어야 해. 다른 핸드폰으로
>  받으니 아래 사진저장 행이 다 겹쳤어, 특히 AI 자동정렬 다 겹침. 가로모드에서 아래 버튼 안 나옴.
>  세로모드 무조건 잠궈 — 회전 잠금과 무관하게 우리 앱은 무조건 가로모드.」

#### 겹침의 진짜 원인 두 가지 (⛔ 되돌리면 그대로 재발)

| 버그 | 내용 |
| --- | --- |
| `alignCenterDock` 이 rect 를 썼다 | `getBoundingClientRect` 는 **rot90(세로폰 가짜 회전)에서 90° 돌아간 좌표**를 준다. 테스트는 가로 뷰포트라 통과했지만 실제 폰에서 잠금이 엉뚱한 자리에 놓여 AI 버튼과 겹쳤다. → **offsetLeft(레이아웃 좌표)만** 쓴다. BASELINE 1-6(getScreenCTM 금지)과 같은 함정의 다른 얼굴이다 |
| `.screen` 의 dvh 폴백 순서 | `height:100dvh; height:100vh` 순서라 **항상 100vh 가 이겼다.** 주소창 있는 브라우저 가로모드에서 화면이 실제보다 길어져 아래 버튼 줄이 잘렸다. → `100vh` 먼저, **`100dvh` 마지막** |

#### fitDocks() — 기기 폭별 3단계 자동 축소

`applyLayout()` 끝에서 **실제로 재 보고** 좁으면 줄인다: `기본 → dock-tight → dock-min`.
판정: `AI 버튼 왼쪽끝 − 왼쪽도크 오른쪽끝 ≥ 잠금 폭 + 24px`. 언어 변경(`setLang`)·즐겨찾기 변경 때도 다시 잰다.
- `dock-tight`(~740px): 버튼 폭·글자 축소, 바 114%
- `dock-min`(~667px): **아이콘만**(잠금·AI 는 글자 유지 — 제일 중요한 두 버튼), 바 104%
좁은 폰에서는 잠금이 센터선에서 밀릴 수 있다 — **겹치는 것보다 밀리는 게 낫다**(클램프가 지킨다).

#### 무조건 가로

`rot = devPortrait && editing && isTouchDevice()` — **pb_orient 설정("off" 포함)을 더 이상 따지지 않는다.**
OS 회전 잠금이 있든 없든 **폰·태블릿에서는** 편집 화면이 언제나 가로다. `isTouchDevice` 만 남긴 이유:
데스크톱 브라우저 창을 세로로 좁힌 경우까지 돌리면 PC 확인 작업이 불가능하다(세로 폴백 · 회귀 11).
사진 선택 중 예외(1-27 · v1.27.0) 유지 · `tryOrientationLock` 의 "off" 예외 폐지.
⛔ pb_orient 조건을 되살리지 마세요.

회귀 **145** 가 잠급니다(667·740·812·844 겹침 없음 · pb_orient=off 강제가로 · dvh 순서 · rect 금지).

---

### 1-55. ⭐ 기본 언어 **영어** · 링크 미리보기도 영어 (v1.92.0 · 원장님 지시 2026-08-28)

> 「https://leahkshop.github.io/perfect-brow/ 이 링크에 설명을 **기본 영어**,
>  다운받을 때 설정 **기본 영어**로 변경」 (해외 학생에게 링크를 보내는 상황)

| 자리 | 내용 |
| --- | --- |
| 앱 기본 언어 | `LANG = localStorage.pb_lang \|\| "en"` — 저장값이 없는 새 기기는 **영어**로 시작. 한국어는 레일 언어 버튼 한 번이면 되고 그 선택은 저장됩니다 |
| 링크 미리보기 | `og:title` · `og:description` · `twitter:*` 영어. **`og:image` 는 절대 주소**(`https://leahkshop.github.io/perfect-brow/icon-512.png`) — 상대 주소면 카톡·왓츠앱이 아이콘을 못 읽습니다 |
| 설치(다운로드) | `manifest.webmanifest` 의 `description` 영어 · `lang: "en"`. **이름은 그대로 `Brow Balance`**(1-33 v1.62.0) |
| `<html lang>` | `en` |
| ⚠️ 이미 쓰던 폰 | `pb_lang` 이 저장돼 있으면 그 언어 그대로입니다. 한 번도 안 고른 폰만 영어로 시작합니다 |
| ⚠️ 영어는 **글자가 깁니다** | 영어를 기본으로 돌리며 실제로 깨진 것들: ① `editor_guide` 영어 번역이 아예 없었다 ② 안내 칩이 오른쪽 밸런스 묶음과 겹쳤다(`max-width:64%→42%`, `white-space:normal`) ③ 왼쪽 도크가 넓어져 **가운데 잠금이 센터선에서 밀리고 AI 버튼과 겹쳤다**. 그래서 영어 라벨을 짧게 잡았습니다 — `Save` · `Change` · `Lock` · `Align`(=AI 눈썹정렬). ⛔ 영어 라벨을 길게 되돌리지 마세요 |
| 좁은 폰(844) 폭 계산 | 왼쪽 도크 → 잠금(센터선) → AI(52~74) → 좌우 바(줌~밸런스 행 ×1.04)까지 **딱 맞습니다.** `.barrow{min-width:127%}` · `#btnExport/#btnChange/#btnLock{min-width:62px}` 가 그 균형입니다. 하나를 키우면 다른 하나가 밀립니다 |
| 가운데 도크 클램프 | `alignCenterDock()` 은 이제 **AI 버튼의 왼쪽 끝**까지 한계로 봅니다 — `.barrow` 가 `.bdock` 보다 왼쪽으로 넘쳐 있어 bdock 만 보면 잠금이 AI 위로 올라탑니다 |

회귀 **144** 가 잠급니다.

---

### 1-54. ⭐⭐ 앱 시작 = **AI 눈썹정렬 켜짐** · 프리미엄 게이트 자리 (v1.91.0 · 원장님 지시 2026-08-28)

> 「앱이 시작되면 기본으로 AI 눈썹정렬을 켜둬라. 나중에 **무료 캐시가 끝나면 꺼지고 프리미엄 가입 시
>  다시 켜진다.** 무료 캐시를 다 쓰면 기본정렬로 변경된다. 고객이 이미 프리미엄 AI 기능에 익숙해져서
>  없어지면 프리미엄를 구입할 수 있게 한다.」

| 규칙 | 내용 |
| --- | --- |
| 시작 순서 | 사진 로드 → 랜드마크 기본정렬 → **autoAiOnLoad() 가 AI 눈썹정렬을 자동으로 한 번** → 초기화셋팅 인사 → 가이드 ① |
| **안전판** (⛔ 빼지 마세요) | 판독이 실패하면(드로잉 없는 사진·못박음 검사 탈락) **아무것도 바꾸지 않고 기본정렬로 남는다.** v1.30~33 의 「어긋난 판독으로 시작」 사고를 다시 열지 않기 위한 장치다. v1.34.0 의 「버튼에서만」 규칙은 이 안전판으로 대체됐다 |
| 되돌리기 | 자동 정렬은 `step()` 한 건 — **되돌리기 한 번이면 기본정렬**로 돌아간다. 알림도 그렇게 말한다(`ai_auto_on`) |
| 프리미엄 게이트 | `aiAllowed()` — 지금은 무료(항상 true). **나중에 무료 캐시 소진 → false**(자동 정렬 꺼짐 · 시작 = 기본정렬 · 버튼 🔒 실제 잠금), **프리미엄 가입 → true.** 구현할 때 이 함수 하나만 바꾸면 되게 설계돼 있다 — 게이트를 우회하는 호출을 만들지 마세요 |
| 초기화 버튼 | **여전히 랜드마크 기본정렬**이다. 자동 정렬이 마음에 안 들 때의 탈출구이므로 AI 를 다시 돌리지 않는다 |

회귀 **96**(재작성)이 잠급니다.

---

### 1-53. ⭐ 안내는 **중앙 위 한 자리** · 끌 수 있다 · 가이드 켜짐 중엔 사라지지 않는다 (v1.90.0 · 원장님 지시 2026-08-28)

> 「가이드 안내를 중앙 위쪽으로 이동 · 가이드 버튼 옆에 (끄기/켜기) 안내 토글 · AI 눈썹 맞춤의 안내는
>  중앙 위로 가이드 안내와 동일 · 이름 「AI / 눈썹정렬」 두 줄 · 가이드가 켜져 있는 상태에서는 안내가 나오도록」

| 규칙 | 내용 |
| --- | --- |
| 자리 | 가이드 프롬프트(`#guideTip`)와 AI 눈썹정렬 알림(`#topNote`)은 **`.guidetip` 한 컨테이너**(top 8px · 중앙) 안에 세로로 쌓인다. ⛔ AI 알림을 화면 한가운데 HUD 로 되돌리지 마세요 — 사진을 가립니다 |
| 안내 토글 | `#btnTip`(가이드 옆) · `S.tipOn`. **앱을 열면 언제나 켜진 채 시작**(v1.90.1 원장님 확정) — 끈 것은 그 세션 동안만. ⛔ localStorage 로 되살리지 마세요. **프롬프트만** 끕니다 — 플로우는 그대로 |
| 가이드 켜짐 = 안내 있음 | ① 인사(초기화셋팅) 중에도 **첫 스텝 안내를 미리** 띄운다 ② 플로우 밖 선(눈·센터)을 골라 차례가 잠시 내려가도 **마지막 안내를 유지**(`S.tipKey`). ⛔ `guideCur` 가 null 이라고 무조건 숨기던 옛 규칙으로 되돌리면 「껐다 켜면 안내가 안 나온다」가 재발합니다 |
| AI 버튼 이름 | `.aihead` 「AI」 + `ai_name` 「눈썹정렬」 **두 줄**. 특별 그라데이션·🔒 는 1-52 그대로 |

회귀 **143** 이 잠급니다.

---

### 1-52. ⭐ 도크 재배치 · **AI 눈썹 맞춤** · 프리셋 숨김 (v1.89.0 · 원장님 지시 2026-08-28)

> 「프리셋 기능은 일단 숨김 — 시스템은 나중에 다시 쓸 수 있도록 둠. 사진 저장·사진 변경이 왼쪽 끝,
>  간격 두고 가이드·내 세트, 중앙에 사진잠금, 간격 두고 오른쪽 좌우 바 왼쪽에 드로잉 맞춤 버튼.
>  이름 변경: **AI 눈썹 맞춤** · 색상 특별하게 · 버튼 안 오른쪽 위에 아주 작은 잠금 이모지
>  (나중에 프리미엄 잠금, 업그레이드 시 해제) · 밸런스·오른쪽·왼쪽은 오른쪽 끝 정렬」

| 자리 | 내용 |
| --- | --- |
| 왼쪽 끝 (`#menuRow`) | **사진저장 · 사진변경** → `.dgap`(간격) → **가이드 · 내 세트** |
| 가운데 (`#centerDock`) | **사진잠금 홀로** — 중심은 여전히 센터 세로선(v1) 위(1-25 유지) |
| 좌우 바 왼쪽 (`.barrow`) | **AI 눈썹 맞춤**(`#btnSnap`) — 바와 한 행. `.barrow` 는 152% 로 왼쪽으로 늘려 바 길이를 지킨다 |
| 오른쪽 끝 (`.ov-bal`) | **밸런스 · 왼쪽 · 오른쪽** — 초기화(오른쪽 도크 맨 위) 왼쪽까지 |

#### AI 눈썹 맞춤 (구 「드로잉 맞춤」)

- i18n `ai_redraw` = **「AI 눈썹 맞춤」** / "AI Brow Fit". 기능은 그대로 `autoFromDrawing()`.
- **앱에서 유일한 보라→핑크→금빛 그라데이션.** ⛔ 다른 버튼에 쓰지 마세요 — 특별함은 하나일 때만 특별합니다.
- 오른쪽 위 `.ailock` = 아주 작은 🔒 이모지. **지금은 장식**입니다 — 나중에 프리미엄 잠금이 여기 들어옵니다
  (프리미엄 업그레이드 시 해제). ⛔ 지우지 마세요.

#### 프리셋 — 숨김이지 삭제가 아니다

`#btnPresetLoad` 와 `#favRow` 에 `hidden`. **프리셋 시스템(저장·불러오기·즐겨찾기 코드)은 전부 그대로**입니다 —
`hidden` 두 개만 빼면 되살아납니다. ⛔ 코드를 지우지 마세요. 원장님이 「나중에 리마인드」를 요청하셨습니다.
⚠️ `.menurow button{display:flex}` 가 `[hidden]` 을 이기므로 `.menurow button[hidden]{display:none}` 이 함께 있습니다(1-41 의 guidetip 칩과 같은 함정).

회귀 **40 · 83** 이 이 배치를 잠급니다.

---

### 1-51. ⭐ 잡는 범위 **섬세 교정** (v1.88.0 · 원장님 지시 2026-08-28)

> 「아치선과 아치엣지, 아치두께 선택하기가 겹쳐서 매우 불편하다. 아치엣지가 아치선 밑으로 깔려 있어
>  클릭시 아치선이 선택된다. **아치엣지와 아치두께 선이 아치선 위에 올라와라.** 아치선은 **아치두께
>  아래로 내려온 선**을 눌렀을 때 클릭되도록. **눈 가로선**은 근처에 손이 눌려도 움직이지 않도록 —
>  눈 선 클릭은 그 선 **아주 가까운 곳**에서만. 다른 선들은 기존 시스템 유지.」

| 규칙 | 내용 |
| --- | --- |
| 교차점 우선순위 | 아치엣지(h2)·아치두께(archThickness)의 높이에서는 **언제나 가로 자가 잡힌다.** 아치선(v6)은 그 높이에서 잡기 후보에서 아예 빠진다 |
| 아치선의 잡는 구간 | **`아치두께 y + V6_HIT_GAP(8px)` 아래**에서만. 아치두께가 숨어 있으면 아치엣지 기준, 둘 다 숨어 있으면 제한 없음 |
| 눈 가로선(h1) | `EYE_HIT_PX = 9` — 9px 안에서만 잡힌다. 화면을 좌우로 관통하는 긴 선이라 다른 선을 만지려던 손이 자꾸 잡았다 |
| 잡은 뒤는 그대로 | **드래그 시스템은 건드리지 않았다** — 일단 잡히면 손이 선에서 멀어져도 잡은 선이 움직인다(기존 유지). 바뀐 것은 「무엇이 잡히는가」뿐 |
| ⛔ 되돌리지 마세요 | h1 을 HIT_PX(28)로, v6 를 전체 구간으로 되돌리면 같은 불편이 돌아옵니다 — 회귀 **142** |

#### 초기화셋팅 손질 (같은 지시)

`INTRO_MS = 3200` (4000 → 「아주 조금만 짧게」) · 인사 중에 **선을 움직이면 즉시 종료**(`endIntroEarly`
— 드래그 시작·조절 바·화살표 전부). 조기 종료 때는 첫 스텝을 강제로 켜지 않는다 — 방금 잡은 선이 곧 차례다.

---

### 1-50. ⭐⭐ **「초기화셋팅」** — 4초 인사 · 네 곳에서 작동 (v1.87.0 · 원장님 지시 2026-08-28)

> 「앱을 열어 사진을 불러왔을때 **블링킹이 너무 짧다.** 세로모드에서 가로모드로 변경하는 사용자의
>  액션 때문에 블링킹 + 모든 선 화면에 전체 보임 → 가이드 켜진 상태 시작. 그러니 **첫 셋팅 전체 색
>  보임이 더 길어져야 한다. 이 셋팅을 초기화셋팅이라 이름 부르겠다.** 저장 바람.
>  초기화셋팅은 ① 앱을 처음 열었을때 ② 사진을 다시 불러왔을때 ③ 초기화 버튼을 눌렀을때
>  ④ 가이드버튼을 껐다가 다시 켰을때 작동되도록 해라」

**초기화셋팅** = 블링킹 1회 + 모든 선이 고유색으로 전체 보임 → 끝나면 가이드 첫 스텝.

| 규칙 | 내용 |
| --- | --- |
| 시간 | `INTRO_MS = 4000` (1.6초 → **4초**). 폰을 세로→가로로 돌리는 동안 인사가 지나가 버리지 않게. ⛔ 줄이지 마세요 |
| 코드 | `startIntro()` **한 곳**입니다. 네 트리거가 전부 이 함수를 부릅니다 — 흩어 놓으면 하나가 빠집니다 |
| 트리거 ①② | `loadPhoto()` — 처음 열었을 때 · 사진을 다시 불러왔을 때 |
| 트리거 ③ | `btnReset` — 초기화 버튼 |
| 트리거 ④ | `btnGuide` **켤 때** (끌 때는 즉시 종료 — 인사 없음) |
| 드로잉 맞춤은 예외 | `btnSnap` 은 인사 없이 바로 첫 스텝입니다 — 원장님이 이미 화면을 보고 계신 중이라 4초를 기다리게 하면 방해입니다 |
| 인사가 끝나면 | `guideOn` 이면 첫 스텝(`GUIDE_FLOW[0]`) 시작. 꺼져 있으면 그냥 전체 색 유지 |

회귀 **141**(초기화·가이드 재켬) · **101/134**(사진 로드) 가 잠급니다.

---

### 1-49. 🐛 레일 버튼 — **숨김은 같은 버튼을 연달아 두 번** 누른 때만 (v1.85.0 · 원장님 신고 2026-08-28)

> 「드로잉 맞춤 클릭시 **이너가 사라졌다**」

한 줄 모드의 레일 버튼은 「1번 탭 = 선택 · 같은 버튼 다시 탭 = 숨김」입니다.
문제는 **숨김의 판단 기준**이 `S.sel === 이 선` 이었다는 것입니다.

v1.83.0 에서 **이너가 플로우 첫 스텝**이 되면서, 드로잉 맞춤(`btnSnap`)과 가이드 켜기가
`noteSel(GUIDE_FLOW[0])` 로 **이너를 자동 선택**해 둡니다. 그 상태에서 원장님이 이너를
**한 번만** 눌러도 앱은 「두 번째 탭」으로 읽어 **선을 숨겨 버렸습니다.**

| 규칙 | 내용 |
| --- | --- |
| 숨김 판단 | `lastTapKey` — **직전 「탭」이 같은 버튼일 때만** 숨긴다 |
| 자동 선택은 근거가 아니다 | `noteSel()` 은 맨 앞에서 `lastTapKey = null` 로 지운다. 가이드 진행·드로잉 맞춤·초기화가 고른 선은 한 번 눌러도 안 숨는다 |
| 숨긴 순간 | HUD 로 「(선 이름) · 숨김 — 다시 누르면 나옵니다」를 띄운다. 조용히 사라지면 고장으로 보입니다 |
| 버튼 표시 | 숨긴 선의 레일 버튼은 **점선 테두리 + 이름에 줄**(`.lbtn.hidden-line`). 선택 채움보다 이 표시가 우선입니다 |
| ⛔ 되돌리지 마세요 | `S.sel === spec.key` 조건으로 돌아가면 같은 사고가 다시 납니다 — 회귀 **139** 가 잡습니다 |
| **차례인 선은 못 숨긴다** (v1.86.0) | `S.guideOn && S.guideCur === 이 선` 이면 두 번 눌러도 안 숨습니다 — 앱이 「이 선을 놓으세요」라고 시켜 놓고 그 선을 숨기면 시술이 그 자리에서 멈춥니다. HUD 로 「지금 차례라 숨기지 않습니다」 |
| **드로잉 맞춤이 되살린다** (v1.86.0) | `btnSnap` 은 맞춘 뒤 **가이드 순서에 있는 선의 가시성을 전부 켭니다**(원장님 지시 「예전에 쓰던 이너 라인 되돌려놔」). ⛔ 「모든 라인 숨김」으로 일부러 숨긴 상태까지 되살리지는 않습니다 — 회귀 **140** |

---

### 1-48. ⭐ 고른 선 말고는 **한 단계 물러난다** (v1.84.0 · 원장님 확인 2026-08-27)

> 원장님 물음: 「선택한 선이 아예 다른 색으로 변경되는 건 어떻니? 아니면 선택된 선이 현재 시스템
>  유지되고, 다른 선이 약간 더 낮은 밝기로 죽는 건 어떻니?」 → **뒤쪽(B안)으로 확정.**

| 규칙 | 내용 |
| --- | --- |
| 고른 선 | **그대로** — 고유색 · 굵기 ×1.3 · 밝기 +0.25 · 고른 순간 한 번 반짝(1-47) |
| 나머지 선 | **자기 색 그대로 옅게** (`SEL_FADE = 0.55`). ⛔ 회색으로 바꾸지 마세요 — **색이 곧 이름표**입니다(v1.46.2에서 세로선 배지를 지운 근거) |
| 언제만 | **가이드가 꺼졌을 때만.** 켜져 있으면 이미 차례 선 하나만 색이 있어 더 죽일 것이 없고, 두 신호가 겹치면 무엇이 차례인지 흐려집니다 |
| ⛔ 값 고정 (0.55) | **원장님 확인 2026-08-28 「이 부분 좋다」.** 더 내리면 「가이드를 끄면 전부 보인다」(1-40)가 무너지고, 더 올리면 고른 선이 안 도드라집니다 |
| ⛔ A안 금지 | **고른 선의 색을 전용 색으로 바꾸는 방식**(형광 노랑 등)으로 되돌리지 마세요. 사진 위에서 선 이름을 잃고, 색 상태가 넷(고유·잡음·죽음·선택)으로 늘어납니다 |

회귀 **138** 이 잠급니다(109 의 투명도 기준도 이 규칙을 따라갑니다).

---

### 1-47. ⭐ 고르면 **되살아난다** · 이너가 플로우로 돌아왔다 (v1.83.0 · 원장님 지시 2026-08-27)

> 「선을 선택시 … 그 선 고유의 선색이 **조금 더 굵어지고 조금 더 밝아지게** 해서 선이 선택되었음을
>  표시한다. 이동시 잡은선은 설정되어있는 이동선 색상으로 변경. **선이 죽어있다 다시 선택될경우
>  고유의 색이 생성.** 선택했다는 것을 표기한다」 / 「설정서 가이드 순서 정할때 **이너가 빠져있다. 이너도 포함하라**」

#### 한 선이 그려지는 **우선순위** (⛔ 순서를 바꾸지 마세요)

| 순위 | 상태 | 모습 |
| --- | --- | --- |
| 1 | 밸런스 경고 | 빨간 토막 |
| 2 | **잡고 있음**(`grabbed`) | 잡은 선 색 — 손이 붙어 있는 동안은 언제나 이것이 이깁니다 |
| 3 | **고름**(`isSelected`) | **고유색 · 굵기 ×1.3 · 투명도 +0.25** — 죽어 있던 선도 여기서 되살아납니다 |
| 4 | **끝냄**(`settled`) | 잡은 선 색으로 죽어 있음 (가이드 꺼짐에서만) |
| 5 | 조용함 | 얇은 회색 |

`settled` 는 **`&& !isSelected(sp.key)`** 를 달고 있습니다. 이 조건을 빼면 죽은 선을 골라도
먹색 그대로라 **사진 위에서 무엇을 골랐는지 알 수 없습니다**(원장님이 지적한 바로 그 문제).

#### 고른 **그 순간** 한 번 반짝 (v1.83.0 · AI 판단 고도화)

굵기·밝기만으로는 이미 굵은 선(꼬리)이나 밝은 사진에서 변화가 눈에 늦게 들어옵니다.
`S.pulseKey` 가 1.5초 동안 그 선에 `blink1`(1.4초 한 번)을 붙입니다.
⛔ 반복 깜빡임(`blink`)으로 바꾸지 마세요 — 시술 중 계속 깜빡이면 눈이 피로하고,
**가이드 차례 신호**(blink)와 구분이 사라집니다. 차례 선이면 차례 깜빡임이 이깁니다.

#### 이너가 기본 플로우로 돌아왔다

`FLOW_DEF = ["v2","front","frontThickness","h2","archThickness","v4","h3"]` — **이너가 맨 앞**입니다.
이너는 모든 자의 기준점(1-7)이라, 이너가 흔들리면 뒤 여섯 스텝이 전부 다시 흔들립니다.
저장 키는 `pb_flow_v2` — 옛 키(`pb_flow_v1`)에 저장된 순서는 **읽을 때 이너를 맨 앞에 넣어** 옮겨 담습니다.
⛔ 옛 키를 그대로 읽지 마세요 — 이미 쓰던 기기에서 이너가 계속 빠진 채로 남습니다.

회귀 **101**(새 순서) · **135**(순서 편집) · **137**(되살아남·한 번 반짝)이 잠급니다.

---

### 1-46. ⭐ 설정 시트 **배치** — 한 줄에 하나 · 테두리는 덤 (v1.83.0 · 원장님 지시 2026-08-27)

> 「현제 세트, 밝은사진, 어두운 사진 카드 세개를 **눈썹 제시 밑에** 작은 카드로 만들고 옮겨 ·
>  선굵기, 가로길이가 **같은 행 말고 선굵기를 개별 행**으로, 그 밑에 **투명도**, 그 밑에 **선 길이** ·
>  테두리는 따로 별도로 **백그라운드 색을 처리하여 별도의 추가 작업**이라고 느낄수있게 해라.
>  테두리는 **중요하지 않지만 있으면 좋음**」

| 자리 | 규칙 |
| --- | --- |
| 조합 카드 3개 (`#lookCombo`) | **미리보기(`#lookPrev`) 바로 밑**, 같은 왼쪽 칸. `.combo.mini` — 설명줄(`i`)을 감추고 색 점만 남긴 작은 카드입니다. ⛔ 시트 맨 위로 되돌리지 마세요 — 「무엇이 바뀌는지」는 미리보기 옆에 붙어 있어야 손이 덜 갑니다 |
| 슬라이더 세 줄 | **선 굵기 → 투명도 → 가로 길이** 순서로 **각각 자기 줄**. ⛔ 굵기와 길이를 한 줄에 다시 합치지 마세요 — 390px 화면에서 손가락이 옆 슬라이더를 잡습니다 |
| 테두리 | `.setblock` — **다른 배경색 + 얇은 테두리 + 작은 머리말**(`set_edge_hd`). 「없어도 되는 덤」이라는 것이 눈으로 보여야 합니다. 블록 안에서는 `테두리` 이름표를 빼고 색 4칸 + 굵기만 둡니다(머리말이 이미 이름입니다) |
| 시트 높이 | 여전히 **스크롤 없이 한 화면**(844×390). 회귀 136 이 `scrollHeight <= clientHeight` 로 잠급니다 |

회귀 **136** 이 이 배치를 잠급니다.

---

### 1-45. ⭐⭐ 아치두께의 **마지노선** (v1.81.0 · 원장님 지시 2026-08-27)

> 「**절대로** : 아치두께는 절대로 꼬리가 측정된 위치 밑으로 내려오지 않는다.
>  눈썹중 꼬리가 가장 낮은곳에 위치한다. 아치두께는 앞머리와 같은선 위치 하거나 높은곳에 위치한다.
>  그러니 아치두께를 자동위치할때 꼬리보다 낮은곳에 쉐도우, 어두운 선을 아치두께라고 인식할수 없다」

**눈썹의 해부학이 정해 주는 순서입니다.** y 는 아래로 갈수록 커집니다.

```
아치두께  ≤  앞머리  ≤  꼬리
```

아치두께가 이 선을 넘었다면 그것은 눈썹이 아니라 **눈꺼풀 그늘**을 읽은 것입니다.
`autoFromDrawing()` 끝에서 상한을 겁니다.

| 항목 | 값 | 왜 |
| --- | --- | --- |
| 상한 | `min(꼬리, 앞머리) − AT_GAP` | 원장님이 정하신 순서 |
| `AT_GAP` | 캔버스 **3px** | ⚠️ 상한에 **딱 붙이면 안 됩니다.** 붙이면 아치두께와 앞머리가 같은 값이 되어 **못박음 검사(회귀 131)** 가 「사진에서 읽은 값이 아니다」로 잡습니다. 상한이 걸렸다는 것 자체가 판독이 흔들렸다는 뜻이므로 조금 위에 세웁니다 |
| 하한 | `아치 + 4px` | 아치 윗선보다는 반드시 아래 (윗선·아랫선이 뒤집히지 않게) |

회귀 **133** — 산 아래에만 짙은 그늘을 깐 사진. 상한을 지우면 아치두께가 꼬리·앞머리 아래로 내려가 바로 실패합니다.
회귀 **131** 도 까다로운 사진 9장 전부에서 `아치두께 ≤ 앞머리 ≤ 꼬리` 를 함께 봅니다.

---

### 1-44. ⭐ 선의 상태는 **다섯 가지** · 시작에 전체라인 인사 (v1.81.0 · 원장님 지시 2026-08-27)

> 「선을 선택시 지금은 아무런 액션이 없이 왼쪽 버튼만 이름으로 밝게 표기된다.
>  선을 선택시 이 선택되었다는 것을 **사진 위 선에 그 선 고유의 선색이 조금더 굵어지고
>  조금더 밝아지게** 해서 선이 선택되었음을 표시한다」
> 「앱이 열리면 가이드 첫 라인이 시작되기 전에 **전체라인이 아주살짝 더 두껍게 밝게** 표시되고,
>  그러면서 동시에 **1번 가벼운 블링킹** 뒤에 가이드 첫 라인 시작해라 …
>  첫 사진 불러오자마자 이너 라인만 색이 있고 나머지는 검정색이라 사용자가 이게 뭐지? 한다」
> 「움직임을 한번이라도 한 색상은 **잡은선 색으로 색이 죽어**, 시선에서 움직이지 않는 선들이
>  눈길을 끌어, 가이드가 자동으로 없더라도 사용자가 움직임으로 체크를 한 선과 남아있는 선을
>  직관적으로 볼수있다」

| 상태 | 모습 | 코드 |
| --- | --- | --- |
| ⓪ **인사** (사진 직후 1.6초) | 전체 라인이 고유색 + 조금 굵게 + **1회 깜빡임** | `S.intro` · CSS `pbBlink1` |
| ① 조용 (가이드 ON · 차례 아님) | 얇은 짙은 회색 한 줄 | 기존 (1-24) |
| ② 기본 (가이드 OFF · 아직 안 건드림) | 고유색 | v1.80.0 (1-40) |
| ③ **선택됨** | 고유색 · **굵기 ×1.3 · 투명도 +0.25** | `SEL_W` / `SEL_OP` |
| ④ 잡는 중 | 「잡은 선」 색 | 기존 (1-24 ③) |
| ⑤ **끝냄** (가이드 OFF · 한 번 움직인 선) | 「잡은 선」 색으로 **남는다** | `S.doneSet` |

**⑤ 가 이 버전의 핵심입니다.** 가이드를 끄고 쓰실 때, 색이 살아 있는 선 = 아직 안 본 선입니다.

| 규칙 | 내용 |
| --- | --- |
| 언제 「끝냄」이 되나 | 드래그로 실제로 움직였을 때(3px 데드존 통과) · 조절 바를 끌었을 때 · 화살표를 눌렀을 때. **탭만 한 것은 아닙니다** |
| 꼬리·아우터 | 한 점이라 하나를 움직이면 **둘 다** 끝냄 (`markDone`) |
| 언제 지워지나 | **새 사진 · 초기화** 뿐입니다. 되돌리기로는 지워지지 않습니다 — 선택 상태와 같은 이유(1-9) |
| 가이드 ON 이면 | ⑤ 를 쓰지 않습니다. 그때는 플로우가 순서를 알려 줍니다 |
| ⓪ 의 타이머 | `INTRO_MS` 1600ms · **플래그를 한 번 뒤집는 `setTimeout` 하나**입니다. ⛔ v1.54.0 처럼 매 프레임 `render()` 를 도는 깜빡임으로 되돌리지 마세요 (1-24 의 ⛔ 는 그쪽을 막는 것입니다) |
| ⓪ 의 CSS | `@keyframes pbBlink1` — 밝은 쪽(0%·100%)은 **반드시 1**. 저장본은 애니메이션 없이 그 값으로 찍힙니다 |

회귀 **134** 가 세 가지(인사 · 선택 강조 · 끝냄 유지)를 한꺼번에 잠급니다.

---

### 1-43. ⭐ 아치는 **혼자** 움직인다 (v1.81.0 · 원장님 지시 2026-08-27)

> 「지금은 아치가로선과 아치세로선이 동시움직였는데 **따로 움직이도록 되돌린다**」
> (아치두께 동반도 함께 해제 — 원장님 확인 2026-08-27)

**v1.67.0 의 `ARCH_PAIR` 를 폐지했습니다.** 아치 · 아치두께 · 아치선이 **전부 따로** 움직입니다.

| 선 | 축 |
| --- | --- |
| 아치(엣지) `h2` | 위아래만 |
| 아치두께 `archThickness` | 위아래만 |
| 아치선 `v6` | 좌우만 |

- **십자 안쪽 모서리 표식은 아치에서 그리지 않습니다.** 두 선이 한 점이 아니게 됐으므로
  표식이 거짓말을 하게 됩니다. `crossOfStep()` 이 **꼬리 두 스텝에만** 표식을 줍니다.
- ⚠️ **꼬리·아우터의 사선 동시 이동(1-27)은 그대로입니다.** 꼬리 끝은 여전히 한 점입니다 —
  플로우에서만 두 스텝으로 나뉜 것입니다. 회귀 113 이 그것을 지킵니다.
- ⛔ `ARCH_PAIR` 를 되살리지 마세요. 회귀 **117** 이 바로 잡습니다.

---

### 1-42. ⭐ 가이드 플로우 — 새 순서 · **원장님이 순서를 바꾼다** (v1.81.0 · 원장님 지시 2026-08-27)

> 「가이드 - **순서 변경가능 기능 추가** —
>  5라인 : 앞머리 - 앞두께 - 아치 (아치엣지:이름변경) - 아치두께 - 꼬리 아우터 - 꼬리 높이」

```
기본 순서  이너 → 앞머리 → 앞두께 → 아치엣지 → 아치두께 → 꼬리 아우터 → 꼬리 높이
                 (v1.83.0 — 이너가 맨 앞으로 돌아왔습니다 · 1-47)
```

| 규칙 | 내용 |
| --- | --- |
| **이름 변경** | 가로선 `h2` 「아치」 → **「아치엣지」**. 세로선 `v6` 은 「아치선」 그대로입니다 |
| **이너가 맨 앞입니다** (v1.83.0) | v1.81~82 에서는 빠져 있었습니다. 원장님 지시로 **기본 순서 첫 스텝**이 됐습니다(1-47). 안 쓰면 설정 → 가이드 순서에서 끄면 됩니다 |
| **꼬리는 두 스텝** | 아우터(좌우 = 끝점의 x) → 꼬리 높이(위아래 = y). 손으로 끄는 사선은 그대로 한 점입니다(1-27) |
| 순서 저장 | `localStorage["pb_flow_v2"]` — 옛 키 `pb_flow_v1` 은 읽을 때 이너를 앞에 넣어 옮겨 담습니다(1-47) |
| ⛔ **재대입 금지** | `GUIDE_FLOW` 는 **배열 내용만** 바꿉니다(`setFlow`). `window.PB.GUIDE_FLOW` 가 같은 배열을 가리키므로 재대입하면 회귀 테스트가 옛 순서를 봅니다 |
| ⛔ **번호를 문구에 박지 마세요** | 프롬프트의 ①②③… 은 `updateGuideTip()` 이 **지금 순서에서** 매번 붙입니다. 문구(`tip_*`)에는 번호가 없습니다 |
| 목록에서 사라지지 않게 | 꺼진 단계도 목록 아래에 흐리게 남습니다. 사라지면 다시 켤 방법이 없습니다 |
| 마지막 하나 | 전부 끌 수는 없습니다 — 하나는 남습니다 |

편집 UI 는 설정 시트의 **세 번째 탭**입니다. ⛔ 시트를 세로로 늘리지 마세요 — 시술 중 스크롤은 손이 더 갑니다.
회귀 **101**(새 순서·종료) · **135**(편집·번호·저장)가 잠급니다.

---

### 1-41. ⭐ 설정 — 색 목록 · 슬라이더 · **눈썹 위 미리보기** (v1.81.0 · 원장님 지시 2026-08-27)

> 「선 색상 제공 : **흰색 추가** · 선 굵기 - **드레그 조정으로 변경** ·
>  모두 이색: 버튼 클릭시 **버튼이 밝게 변경** — 클릭되었다는것 볼수있게 ·
>  테두리 굵기 - **드레그 조정** · 테두리 색상 - **없음 흰색 검정 먹색** ·
>  **세로색 목록 추가** — 이너: 기본 민트·먹색·흰색 / 아치선: 먹색·민트·흰색 / 꼬리선: 먹색·민트·흰색」
> 「설정에서 선들이 미리보기가 되어있는데 그냥 선들이 정렬되어있어. **선의 이름과 그 선이
>  눈썹 위에 올려지는 위치가 매칭되어 보일수 있게** … 밝은피부 어두운피부 … 미리보기를 **더 크게**」
> 「가이드가 꺼진상태에서 **이너라인은 제외한 세로색상은 기본적으로 먹색을 유지**」

#### 색 — 가로 자와 세로선이 **분리**됐습니다

| 열쇠 | 대상 | 목록 | 기본 |
| --- | --- | --- | --- |
| `inner` / `arch` / `tail` | 가로 자 (앞머리·앞두께 / 아치·아치두께 / 꼬리) | `PALETTE` **8색** (흰색 추가) | 민트 / 파랑 / 보라 |
| `vInner` / `vArch` / `vTail` | 세로선 (이너 / 아치선 / 아우터) | `V_PALETTE` **3색** (민트·먹색·흰색) | **민트 / 먹색 / 먹색** |

- ⛔ 세로선에 8색 팔레트를 붙이지 마세요 — 세로선은 얼굴을 세로로 가로질러 색이 많으면 시끄럽습니다.
- 「모두 이 색」은 가로 세 묶음을 이너 색으로 맞추고, **그 색이 세로선 목록에 있으면 세로선까지** 함께 맞춥니다.
  목록 밖 색(라임·핑크 등)이면 세로선은 그대로 둡니다.
- 「모두 이 색」 버튼은 **세 묶음이 같은 색인 동안 켜져 있습니다** — 눌렀는지 되짚을 필요가 없게(원장님 지시).

#### 테두리 — 「자동」 폐지

`edgeC` = **`none` / `light`(흰색) / `black`(검정) / `dark`(먹색 #14161B)**.
옛 저장값 `auto` 는 대비색으로 한 번 환산해 줍니다(기존 기기에서 테두리가 사라지지 않게).
색을 골랐는데 굵기가 0 이면 아무 변화가 없어 고장처럼 보이므로, **그때만 70% 로 올려 줍니다.**

#### 굵기 — 슬라이더

`선 굵기` 60~180% · `테두리 굵기` 0~120% · **`잡은 선 굵기` 60~180%** (v1.82.0).
⛔ 3단 세그먼트로 되돌리지 마세요 — 그 사이 값을 만들 수 없고, 기본 선과 조작이 달라 손이 헷갈립니다.
⚠️ **값은 여전히 따로입니다** — 잡은 선의 굵기·투명도는 기본 선과 무관하게 저장됩니다(v1.59.0 규칙 유지).
조작 방식만 같아졌습니다 (원장님 지시 2026-08-27).

#### 미리보기 — **눈썹 그림 위에 선과 이름표**

| 규칙 | 내용 |
| --- | --- |
| 무엇이 보이나 | 밝은 피부 / 어두운 피부 두 칸에 **같은 눈썹**을 그리고, 지금 설정한 선을 **실제 자리·실제 규칙**(테두리·굵기·길이·투명도)으로 올립니다. 각 선 옆에 **그 선 색으로 이름표** |
| 왜 | 초보자가 「이 색이 눈썹 **어디에** 올라가는가」를 설정 화면에서 바로 봅니다 |
| ⚠️ **사진이 아니라 코드로 그립니다** | ① 오프라인 앱이라 사진을 넣으면 용량과 `sw.js` 캐시가 무거워집니다 ② 저장소가 **공개**라 고객 얼굴 사진을 올릴 수 없습니다(1-10). 원장님 확인 2026-08-27 |
| 눈썹 비율 | **판정 규칙과 같은 눈썹**입니다 — 꼬리가 가장 낮고, 아치두께는 앞머리보다 위(1-45). ⛔ 이 순서가 깨진 모양으로 바꾸지 마세요. 미리보기가 앱의 판정과 다른 말을 하게 됩니다 |
| 이름표 헤일로 | **대비색**으로 깝니다. 늘 어둡게 두면 **먹색 이름표가 검은 헤일로에 묻힙니다**(세로선 기본이 먹색이 된 이 버전에서 실제로 그랬습니다) |
| 탭을 따라간다 | 「잡은 선」 탭에서는 아치 자·아치선이 **잡은 선 색**으로 그려집니다 |
| 자에 붙는 클래스 | `pv-ruler`. 회귀 118 이 **y 좌표를 하드코딩하지 않고** 이 클래스로 자를 찾습니다 |
| 시트 높이 | 슬라이더는 `.sheet input[type=range]{height:26px}` — 전역 40px 을 그대로 쓰면 세 줄만으로 시트가 화면을 넘습니다 |

#### 🐛 함께 고친 버그 — 가이드를 꺼도 안내 칩이 남았다

`.guidetip .chip{display:block}` 이 브라우저 기본 `[hidden]{display:none}` 을 이겨서,
**가이드를 꺼도 마지막 프롬프트 칩이 화면에 그대로 남아 있었습니다**(v1.64.0부터).
`el.hidden = true` 는 잘 걸려 있었기에 회귀 114 도 못 잡았습니다.
→ `.guidetip .chip[hidden]{display:none}` 추가 · 회귀 114 는 이제 **화면 표시(computed display)** 로 검사합니다.

---

### 1-40. ⭐ 가이드 OFF = **전부 고유색** · ON = **한 줄씩 플로우** (v1.80.0 · 원장님 지시 2026-08-25)

> 「가이드를 끄면 **모든 선 기본 색상** 보이도록, **잡을 때 잡는 색상** 삽입,
> **가이드가 켜졌을 때만** 선 하나씩 플로우 적용해라」

| 상태 | 선 모양 | 깜빡임 |
|---|---|---|
| **가이드 OFF** | **전부 고유색** — 한눈에 다 보고 시술한다 | 없음 |
| **가이드 ON** | **지금 차례 하나만** 고유색, 나머지는 조용한 회색 | 차례 선만 |
| **잡는 중** | **잡고 있는 선만** 「잡은 선」 색 (설정 → 잡은 선) | 정지 |

```js
emph    = (sp) => !S.guideOn || S.guideCur === sp.key || isSelected(sp.key);
grabbed = (sp) => S.dragOn && (gDrag.keys ? gDrag.keys.includes(sp.key) : isSelected(sp.key));
blinkOf = (sp) => (S.guideOn && S.guideCur === sp.key ? "blink" : null);
```

**⚠️ `grabbed` 를 예전처럼 `sel && S.dragOn` 으로 되돌리지 마세요.** 가이드를 끄면 모든 선이
`sel` 이므로 **화면 전체가 한꺼번에 잡은 색**이 됩니다.

**⚠️ 「조용 = 얇은 회색」은 이제 가이드 ON 일 때의 규칙입니다.** 회귀 95·102·104·106 은
그래서 `S.guideOn = true` 로 조용한 상태를 잽니다.

회귀 **132** 가 세 상태를 한꺼번에 잠급니다.

---

### 1-39. ⭐⭐ 못박음 검사 · 판독 룰 문서 (v1.79.0 · 원장님 지시 2026-08-25)

> 「왜 어떤 사진은 잘 잡고 어떤 사진은 에러가 나는지 확인하고 **일률적으로 눈썹 포인트를
> 잘 잡도록 해결**. 이런 에러 사항이 생기지 않도록 **룰 저장**해서 앞으로도 파일을
> 업로드할 때 저장할 수 있도록 해라. **못박음 에러 없도록** 깃, 백업, 커밋 등 md 로 저장」

**📄 `판독-룰.md` 를 새로 만들었습니다.** 이 문서가 판독 규칙·못박음 검사·배포 절차를
한 곳에 모아 둡니다. **매 배포마다 함께 올립니다 (올리는 파일 7개).**
`클로드-시작-명령서.md` 가 첫머리에서 이 파일을 읽으라고 지시합니다.

**못박음(pinning)이란.** 판독이 사진이 아니라 **탐색창 경계나 그늘 끝에 못박혀서**,
서로 달라야 할 자 둘이 **같은 값**이 되는 것.

```
앞머리 212 = 아치두께 212   ← 탐색창 바닥
앞머리 199 = 아치두께 199   ← 그늘 끝
```

**서로 달라야 할 두 값이 같으면, 그것은 사진에서 읽은 값이 아닙니다.**

**회귀 131 — 까다로운 사진 9장 일률 검사.** 그늘(창바닥) · 그늘(창바닥 위) · 위 번짐 ·
얇아지는 꼬리 · 얇아지는 앞머리 · 잔털 꼬리 · 옅은 번짐 · 모양 A · 모양 B 에 대해
① 앞두께 < 앞머리 ② 아치 < 아치두께 ③ |앞머리 − 아치두께| > 2 ④ 가로선 다섯 개가 모두
서로 다름 — 네 가지를 한꺼번에 봅니다.

**⛔ 새 판독 규칙을 넣을 때는 131 을 먼저 돌리세요.** 개별 검사는 통과해도 여기서 걸립니다.

---

### 1-38. ⭐ 자는 **드로잉 위에** 놓인다 (v1.78.0 · 원장님 지시 2026-08-25)

> 「너가 준 사진의 위치는 다르지 않니? 자기는 드로잉 위치가 아닌데」

**증상.** 앞머리·앞두께 자가 눈썹 옆 **맨살 위에** 떠 있었습니다. 판독 y 는 맞는데도
자가 눈썹에 닿아 있지 않으니 원장님이 맞았는지 틀렸는지 볼 수가 없습니다.

**원인.** 가로 자는 자기 묶음의 세로선 위에 **가운데 맞춰** 그려졌습니다 (`[a−half, a+half]`).
그런데 **이너와 아우터는 눈썹의 양 끝**입니다 — 가운데 맞추면 자의 절반이 눈썹 밖으로 나갑니다.

**해결.** 자는 세로선에서 **눈썹 쪽으로만** 뻗습니다. 길이는 그대로, 방향만 바뀝니다.

| 자 | 기준 세로선 | 뻗는 방향 |
|---|---|---|
| 앞머리 · 앞두께 | 이너 (눈썹 **안쪽 끝**) | 바깥(꼬리) 쪽으로 |
| 꼬리 | 아우터 (눈썹 **바깥 끝**) | 안쪽(앞머리) 쪽으로 |
| 아치 · 아치두께 | 아치선 (눈썹 **한가운데**) | 지금처럼 가운데 맞춤 |

덤으로, 세로선과 가로선이 만나는 자리가 그대로 **90° 꼭지점**이 됩니다 — 1-34 의 판정
기준(「이너 × 앞머리의 90° 꼭지점에 눈썹 앞부분이 닿는다」)과 화면이 정확히 일치합니다.

**⚠️ 잡는 범위도 같이 움직입니다** — 그리는 범위 = 잡는 범위 = 재는 범위는 모두 `segPx`
하나에서 나옵니다 (1-11). 자를 옮기면 손가락으로 잡는 자리도 함께 옮겨집니다.

회귀 **130** — 가운데 맞춤으로 되돌리면 바로 실패합니다.

---

### 1-37. ⭐⭐ 아랫선은 **언제나 색이 살아 있는 곳까지** (v1.75.0 → v1.76.0 · 원장님 폰 2026-08-25)

> 「이 라인이 매번 앱에 적용 안되는데 … 폰에서 아예 안 올라간다」

**결정적 단서.** 원장님 화면에서 **앞머리와 아치두께가 똑같은 y** 에, 눈썹이 아니라
**눈꺼풀 위**에 서 있었습니다. 두 값이 같다는 것은 그 자리가 사진에서 읽어낸 값이 아니라
**탐색창 바닥**이라는 뜻입니다. 픽셀로 재 보니 바닥 = `eyeY − 0.55h` 와 정확히 겹쳤습니다.

**원인.** 눈썹 아래 피부가 그늘지면 `columnRuns` 의 덩어리가 눈썹부터 눈꺼풀까지 **하나로
이어져** 읽히고, 그 덩어리가 창 바닥에서 잘립니다 → 모든 열의 `bot` = 바닥.
아랫선을 쓰는 두 자(앞머리·아치두께)가 나란히 바닥에 못 박힙니다.

**왜 여태 못 봤나.** 이 컨테이너에서는 MediaPipe CDN 이 막혀 **예비 경로**로만 돌아갑니다.
예비 창은 바닥이 눈에서 멀어 이 현상이 잘 나지 않습니다. **랜드마크 경로**(원장님 폰)의
창 바닥이 훨씬 눈에 가깝습니다. → 회귀 127 은 **랜드마크를 켜고** 돌립니다.

**해결.** `columnRuns` 가 덩어리마다 **어두운 핵심**(`coreTop`/`coreBot`)을 함께 잽니다 —
제일 진한 곳에서 위아래로 걸어 나가며 진하기가 `CORE_DROP`(0.45) 아래로 떨어지는 자리.
`readDrawing` 은 덩어리가 **바닥에 닿았을 때만** `bot` 대신 `coreBot` 을 씁니다.

```
바닥에 안 닿음  → bot 그대로        (기존 사진 전부 영향 없음)
바닥에 닿음     → min(bot, coreBot) (색이 살아 있는 곳까지)
```

이것은 1-36(양 끝은 색이 있는 곳까지)과 **같은 원리를 위아래에 적용**한 것입니다.

**⛔ `r.bot` 을 그대로 쓰도록 되돌리지 마세요** — 회귀 **127**(눈썹 아래 그늘 · 랜드마크 경로)이
바로 잡습니다. 이 검사를 빼면 앞머리 212 = 아치두께 212 = 창 바닥으로 되돌아갑니다.

| | 고치기 전 | v1.75.0 | 눈썹 아랫선 |
|---|---|---|---|
| 앞머리 | 212 (창 바닥) | **177** | 178 |
| 아치두께 | 212 (창 바닥) | **141** | 140 |

#### v1.76.0 — **조건을 없앴습니다** (같은 날, 원장님 두 번째·세 번째 화면)

> 「드로잉 맞춤 아치두께만 조정해라. 현재는 드로잉이 아니라 눈 근처에 있다」
> 「이 사진은 앞머리 안 맞음」

v1.75.0 은 「**창 바닥에 닿은** 덩어리」만 구제했습니다. 그래서 앞머리는 눈썹으로 올라왔지만
**아치두께**는 눈꺼풀 위에 그대로 남았습니다 — 그 열의 덩어리는 바닥까지 가지 않고 그늘
안에서 끝나 구제 대상이 아니었기 때문입니다. 다른 손님 사진에서는 같은 이유로 **앞머리**가
눈썹 아래 맨살에 74px 떨어져 있었습니다.

→ 조건을 **없앴습니다.** 아랫선은 언제나 `min(bot, coreBot)` 입니다.

```
아랫선 = min(덩어리의 끝, 색이 살아 있는 끝)      ← 조건 없음
```

**⚠️ 단 하나의 예외 — 테두리로 그린 드로잉(`outline`).** 그때 `r` 은 **위·아래 두 줄을 묶은
것**이라, 씨앗 한 줄의 핵심으로 자르면 아랫줄이 통째로 날아갑니다 (회귀 **89** 가 잡습니다.
실제로 이 예외를 빼고 돌렸더니 89 가 앞머리 149 vs 178 로 바로 실패했습니다).

회귀 **128** — 그늘이 창 바닥 **위에서** 끝나는 사진. v1.75.0 규칙으로 돌리면
앞머리 199 = 아치두께 199 (그늘 끝)로 되돌아가 바로 실패합니다.

#### v1.77.0 — **윗선도 같은 규칙** (원장님 폰 10:12 「앞두께, 아치두께 안 맞음」)

파우더 눈썹은 **위쪽 경계도 부옇게 번집니다.** 그 번짐이 판독 문턱을 아슬아슬하게 넘으면
덩어리가 눈썹 위 맨살까지 올라가고, 앞두께 자가 눈썹 윗선보다 위에 섭니다
(원장님 화면에서 27px 위 · 캔버스로 약 8px).

```
윗선   = max(덩어리의 시작, 색이 살아 있는 시작)
아랫선 = min(덩어리의 끝,  색이 살아 있는 끝)
```

**⚠️ 문턱은 위아래가 다릅니다 — 이건 실측으로 정한 값입니다.**

| | 문턱 | 왜 |
|---|---|---|
| 아랫선 `CORE_DROP` | **0.45** | 아래는 눈꺼풀·그늘이 눈썹만큼 진해서 세게 잘라야 한다 |
| 윗선 `CORE_UP` | **0.1** | 위는 번짐뿐이라 살짝만 자르면 된다 |

윗선을 0.45 로 두고 원장님 사진을 재현하니 **앞두께 89 → 98** 로, 원장님 표시(87.7)에서
10px 멀어졌습니다. 0.1 에서는 89 그대로입니다. **⛔ `CORE_UP` 을 올리지 마세요.**

회귀 **129** — 눈썹 위 옅은 번짐이 있는 사진. 윗선 잘라내기를 빼면 앞두께 148 → **100**
(번짐 꼭대기)으로 떨어져 바로 실패합니다.

| 원장님 사진 재현 | v1.74.0 | v1.77.0 | 원장님 표시 |
|---|---|---|---|
| 앞두께 | 89 | **89** | 87.7 |
| 앞머리 | 138 | **136** | 137.2 |
| 이너 | 316 | **316** | 318.6 |
| 아우터 | 93 | **93** | 93.1 |

---

### 1-36. ⭐ 양 끝은 **색이 있는 곳까지** 간다 (v1.74.0 · 원장님 지시 2026-08-25)

> 「이너 라인은 앞 라인이 맞아 색이 시작하는 선이잖아. 눈앞꼬리에서 밸런스를 맞춰서
> **왼쪽(현재 밸런스축) 드로잉 시작점이 이너 시작 라인**이야」

**증상.** 원장님이 화면에 분홍 펜으로 두 줄을 그어 주셨습니다 — 앱의 이너(민트)보다
**안쪽(코 방향)** 입니다. 캔버스 좌표로 환산하니 **12.6px**. 실제 드로잉은 x 319 에서
시작하는데 앱은 306 에 세우고 있었습니다.

**원인.** `trimOutside` ⓑ 는 **잉크(두께 × 진하기)** 로 바깥 열을 자릅니다. 눈썹 앞머리는
끝으로 갈수록 **얇아지므로** 잉크가 먼저 떨어집니다 — 색은 아직 남아 있는데 잘렸습니다.
v1.72.0 에서 꼬리에 대해 고친 것과 **똑같은 함정**이 앞머리 쪽에 남아 있었습니다.

| x | 두께 | 잉크 | 진하기(dark) | 잉크 기준(0.38×중앙값≈456) | 진하기 기준(0.25×중앙값≈6.8) |
|---|---|---|---|---|---|
| 306 | 42 | 598 | 13.9 | 통과 | 통과 |
| 311 | 32 | 377 | 11.4 | **잘림** | 통과 |
| 316 | 32 | 254 | 7.9 | **잘림** | 통과 |
| 321 | — | — | — | — | **창 천장(머리카락) → 정지** |

**해결.** 다듬기가 끝난 뒤 `innerStart()` 가 **안쪽 끝만** 다시 이어 붙입니다.

- 조건: 이어져 있고(중심이 두께의 55% 안, 두께 1.6배 이내) · 색이 남아 있고(진하기가
  중앙값의 `INNER_DARK`=0.25 이상) · 창 천장에 닿지 않았고(`edge`) · **센터를 넘지 않는다**
- 상한: 판독 폭의 `INNER_MAX`=0.18 까지만 (폭주 방지)
- **밴드 자체는 건드리지 않습니다.** 늘어난 끝은 **이너(세로선)에만** 씁니다 —
  앞두께·앞머리는 「앞부분」의 윗선·아랫선이라 성근 시작 털을 평균에 넣으면
  자가 아래로 끌려갑니다 (시험해 보니 앞두께 89 → 92 로 밀렸습니다. 원장님 표시는 87.7).

**⛔ 바깥(꼬리) 끝에는 절대 같은 규칙을 쓰지 마세요** — 「얇은털 따라가는것 금지」
(원장님 지시 2026-08-24). 회귀 **122·123** 이 지킵니다.

**결과** (원장님 사진 재현):

| | v1.73.0 | v1.74.0 | 원장님 표시 |
|---|---|---|---|
| 이너 | 306 | **316** | ≈318.6 |
| 앞두께 | 89 | 89 | 87.7 |
| 나머지 7선 | — | **그대로** | — |

회귀 **125** — 앞머리가 얇아지는 눈썹(`SHAPE_HEAD`)에서 이너가 색이 시작하는 곳(344)에
서는지 봅니다. 이 규칙을 빼면 321 로 23px 짧아집니다.

---

#### 꼬리 쪽도 **똑같은 함정이었습니다** (같은 버전 · 원장님 두 번째 지시)

> 「꼬리 판독은 **1 얇은 헤어 · 2 너의 판독 · 3 이 맞는 드로잉 끝선** 인데 이게 판단이 안 되니?」

원장님이 화면에 원 세 개를 그려 세 자리를 짚어 주셨습니다. 캔버스 좌표로 환산하면:

| | 원장님 표시 | 캔버스 x |
|---|---|---|
| ① 얇은 헤어 | 가면 안 되는 곳 | ≈81 |
| ② 그때의 판독 | 앱이 서 있던 자리 | 104 |
| ③ **맞는 드로잉 끝선** | 서야 할 곳 | ≈93 |

**정답은 ①과 ② 사이**입니다 — 더 나가야 하지만 잔털까지는 아닙니다. 원인은 이너와 같았고
(잉크 기준이 얇아진 꼬리를 먼저 자름), 해결도 같습니다 — 다만 **문턱이 다릅니다**:

| | 문턱 (진하기 ÷ 중앙값) | 왜 |
|---|---|---|
| 이너 `INNER_DARK` | **0.25** | 코 방향에는 머리카락이 없다 |
| 아우터 `OUTER_DARK` | **0.5** | 관자놀이에는 머리카락·번짐·잔털이 있다 |

`growEnd(band, kept, toInner)` 하나가 양쪽을 처리합니다. 늘어난 꼬리 열은
**`seqT` (꼬리·아우터 전용)** 에만 들어가고, 앞머리·아치·아치두께는 원래 밴드(`seq`)로
계산합니다 — 성근 끝 털을 구간 평균에 넣으면 자가 끌려갑니다.

| | v1.73.0 | v1.74.0 | 원장님 표시 |
|---|---|---|---|
| 이너 | 306 | **316** | ≈318.6 |
| 아우터 | 104 | **93** | ≈93.1 |
| 꼬리 | 134 | **140** | ≈143 |
| 앞두께 | 89 | 89 | 87.7 |
| 앞머리·아치·아치두께·아치선 | — | **그대로** | — |

**⛔ `OUTER_DARK` 를 0.4 아래로 내리지 마세요** — 「얇은털 따라가는것 금지」(2026-08-24)가
깨집니다. 회귀 **122**(잔털) · **123**(옅은 번짐) · **126**(얇아진 꼬리 · 잔털 제외) 이 함께 지킵니다.

---

### 1-35. ⚠️⚠️ 앞머리와 앞두께 — 위아래가 이렇게 나뉩니다 (v1.73.0 · 원장님 확정 2026-08-25)

> 원장님이 화면에 번호를 찍어 다섯 자리를 지정해 주셨습니다:
> **① 앞머리 · ② 앞두께 · ③ 아치 · ④ 아치두께 · ⑤ 꼬리**

| 선 | 자리 |
| --- | --- |
| **앞두께** | 눈썹 앞부분의 **윗선** ← 위 |
| **앞머리** | 눈썹 앞부분의 **아랫선** ← 아래 |
| 아치 | 산꼭대기의 **윗선** (그대로) |
| 아치두께 | 산꼭대기의 **아랫선** (그대로) |

| 계산식 (v1.72.0 까지 쓰던 **그대로**) | |
| --- | --- |
| 앞두께 | `at(0, 0.18, "top", 0.3)` — 앞부분 18% 구간의 **윗선 30% 분위** |
| 앞머리 | `at(0, 0.18, "bot", 0.7)` — 같은 구간의 **아랫선 70% 분위** |

⚠️ **계산은 처음부터 맞았습니다** — 원장님: 「드로잉 판독은 정확하게 하고 있음에도 **선의 명칭과
위치**를 잘못 제공하고 있다」. 원장님 표시와 재보니 예전 `앞머리` 값이 **앞두께** 자리(4.6px),
예전 `앞두께` 값이 **앞머리** 자리(4.3px)였습니다. **이름만 바꿔 달면 되는 문제였습니다.**
⛔ 구간을 8% 로 좁히지 마세요 — 안쪽 끝만 보면 앞두께가 40px 아래로 처집니다 (실측).

**v1.72.0 까지 앞머리·앞두께를 거꾸로 놓고 있었습니다.** 원장님 표시를 캔버스 좌표로 환산하니
「② 앞두께」가 그때까지 앱이 **앞머리**라고 놓던 자리(y 88)와 정확히 겹쳤고, 「① 앞머리」는
그보다 한참 아래(y 137)였습니다. 아치·아치두께·아치선·이너는 그 표시와 **1px 안쪽으로 일치**
했으므로, 어긋난 것은 이 두 선의 위아래뿐이었습니다.

⛔ 다시 뒤집지 마세요. 회귀 **124** 가 「앞두께가 위, 앞머리가 아래」를 검사하고, 87~94 의
기대값도 이 기준으로 맞춰져 있습니다. 가이드 프롬프트도 ② 윗선 · ③ 아랫선으로 적혀 있습니다.

### 1-34. 드로잉 맞춤 — **판정 기준** (v1.70.0 · 원장님 확정 2026-08-24)

> 원장님이 사진 3장에 직접 그려 정해 주신 기준입니다. 「어디에 맞을 것인가」의 정의이므로
> 판독 알고리즘을 손볼 때 **이 세 문장이 기준**입니다.

| 선 | 기준 | 코드 |
| --- | --- | --- |
| **앞머리 + 이너** | 「안쪽 라인과 위쪽 라인이 닿는 **90° 꼭지점**에 눈썹 앞부분이 닿는다」 → 구간을 평균 내지 않고 **안쪽 끝 8%** 만 본다. ⚠️ v1.73.0 — **앞두께=윗선 · 앞머리=아랫선** (1-35). v1.69 까지는 안쪽 18% 를 분위수로 평균 내어 앞머리 자가 실제보다 **아래**로 내려갔습니다 (원장님 표시: 아래 빨간 ㄴ = 그때 자리 · 위 빨간 ㄴ = 있어야 할 자리) | `at(0, END, "top")` · `END = 0.08` |
| **꼬리 + 아우터** | 「십자 꼭지점에 **꼬리의 뾰족한 끝점**이 닿는다」 → **검은 드로잉이 끝나는 열**(아래 ⭐)과 그 안쪽 8% 구간의 **아랫선**. ⛔ 얇은 잔털을 따라가지 않습니다 (아래 ⛔) | 진하기 기준 `tailIdx` · `bot` 중앙값 |
| **아치선(세로)** | 「아치 가로선을 뒤로 뺐을 때 눈썹이 그 선에서 **아래로 빠져 각도가 생기는 꼭지점**」 → 산꼭대기가 아니라 **꺾임점**. 임계는 **산꼭대기~꼬리 낙차의 15%** (`KINK_DROP`) — 두께로 재면 눈썹 모양에 따라 30px 넘게 흔들립니다 | 봉우리에서 바깥으로 스캔 |


#### ⭐ 검은 드로잉이 끝나는 곳 (v1.72.0 · 원장님 지시 2026-08-25 「검은 드로잉 고도화로 찾기」)

판독은 눈썹 가장자리의 **옅게 번진 열**까지 읽습니다. 그 열까지 꼬리로 치면 십자가 **눈썹이
없는 피부 위**에 섭니다 (원장님이 보라=그때 자리 · 파랑=있어야 할 자리로 표시해 주셨습니다).

| 규칙 | 내용 |
| --- | --- |
| 꼬리 끝 = **검은 드로잉의 끝** | **평균 진하기**가 중앙값의 `TAIL_INK`(55%) 이상인 열만 「검은 드로잉」. 바깥에서 안쪽으로 훑어 처음 만나는 그 열이 꼬리 끝이고, **아우터도 그 x** 입니다 |
| ⚠️ **진하기**로 재고 **잉크량**으로 재지 않는다 | `ink` 는 두께 × 진하기라 **얇은 꼬리에서 자연히 작아집니다** — 잉크량으로 재면 정상 꼬리가 33px 잘리고, 반대로 **넓고 옅은 번짐**은 통과해 버립니다. 두께와 무관한 `dark = ink / 길이` 를 씁니다 |
| 안쪽 한계 | 눈썹의 안쪽 45% 아래로는 내려가지 않습니다 (아무리 옅어도 꼬리를 눈썹 한가운데로 옮기지 않게) |

⛔ 진하기 기준을 잉크량으로 되돌리거나 문턱을 낮추지 마세요. 회귀 **123** 이 잡습니다 —
몸통 바깥에 **넓고 옅은 번짐**이 있는 사진에서 아우터가 검은 드로잉 끝(≈180)에 서고 번짐
끝(118)까지 가지 않는지 검사합니다. 규칙을 끄면 아우터가 **119** 로 밀려 바로 실패합니다.

#### ⛔ 얇은 털 추적 금지 (v1.71.0 · 원장님 지시 2026-08-25)

> 「얇은털 따라가는것 금지」

v1.70.0 은 판독이 끊긴 자리에서 **문턱을 낮춰 연한 잔털을 한 걸음씩 따라갔습니다**(`chaseTail`).
원장님이 **금지**하셨습니다 — 잔털은 눈썹 디자인의 끝이 아닙니다.

**꼬리 자리 = 본 판독(대비 18)이 실제로 읽은 진한 눈썹의 끝 구간, 그 아랫선.**
아우터도 그 끝 그대로입니다.

⛔ 추적·문턱 낮추기를 다시 넣지 마세요. 회귀 **122** 가 잡습니다 — 몸통은 진하고 꼬리는
피부와 겨우 14 차이인 사진에서, 아우터가 **진한 눈썹 끝(≈172)** 에 서고 잔털 끝(146)까지
가지 않는지 검사합니다.

#### ✅ 아치선 자리 — **원장님 확인 완료** (2026-08-24 「이 위치 아치 해석 완료형으로」)

원장님이 실제 화면에 빨간 십자로 찍어 주신 자리를 픽셀로 재니 **눈썹 바깥 끝에서 폭의 26%**
지점이었고, 꺾임점 계산이 내는 값도 **26%** 였습니다 — 해석이 맞았음을 확인했습니다.
회귀 121 이 이 띠(**바깥에서 15~40%**)를 잠급니다. 이 밖으로 나가면 해석이 다시 어긋난 것입니다.

#### 아치선의 두 번째 기준 — 자리 1·2·3

> 「보통 아치세로선의 위치는 1·2·3 에 있다 — ① 검은 눈동자 바깥 끝과 눈꼬리 사이
>  ② 눈꼬리 ③ 눈꼬리에서 나온 꼬리 섀도우. 그중 한 곳」

⚠️ **순서가 있습니다**: 기준 ①(꺾임점)이 **우선**이고, 자리 1·2·3 은 꺾임을 **못 찾았을 때**의
자리입니다 (`eyeArchRange()`). ⛔ 1·2·3 으로 꺾임점을 **가두지 마세요** — 아치가 안쪽에 있는
눈썹에서 아치선이 30px 넘게 밀립니다 (회귀 91 이 그 모양입니다).
합성 랜드마크에는 **눈꼬리·내안각(33·133·362·263)** 이 반드시 있어야 합니다 — 빠지면 눈 폭이
0 이 되어 엉뚱한 구간이 나옵니다 (그래서 폭이 8px 미만이면 `null` 을 돌려 가두지 않습니다).

회귀 **121** 이 이 세 기준을 잠급니다: 꼬리 자가 윗선보다 아래인가 · 아치선이 산꼭대기보다
바깥인가 · 앞머리 꼭지점이 눈썹 앞부분 끝에 닿는가. 예전 규칙으로 되돌리면 바로 실패합니다.

⚡ **v3.0.0 — 위 「꺾임점(KINK_DROP)」 규칙은 폐지되었습니다.** 원장님이 실제 사진 4장에
파란 세로선으로 직접 표시해 확인한 결과, 꺾임점이 잡던 자리와 원장님이 보시는 자리가
달랐습니다 (2026-08-30). 새 규칙: 「**아치엣지 가로선이 눈썹과 맞닿아 피부색이 드러나는
자리**」— `app.js`의 `autoFromDrawing()` 안, `smoothTop(pk)`(산꼭대기)를 기준으로
`ARCHV_EPS`(눈썹 낙차의 5%)를 **계속 늘어나며** 넘는 첫 구간(`ARCHV_HOLD=5`, 결 노이즈로
잠깐 튀었다 가라앉는 「가짜 상승」은 버림)을 아치선으로 잡습니다. **+ 산꼭대기에서 최소
3칸(`frontTickPx()`×3, 실제 화면 눈금 거리) 안쪽은 후보 자격이 아예 없습니다** — 원장님
지시 「3개칸 이후부터 찾거나, 못 찾으면 3개칸 이후의 눈꼬리 위쪽을 표준값으로」. 실제
사진 4장 중 A는 25.46(원장님 「3번째 칸으로 조정한 것 인정」으로 확정 승인), B·C·D는
17.65/20.56/25.1 그대로 확정. 회귀 121 은 그대로 유지(같은 밴드 15~40%를 이 새 규칙의
출력도 만족). 1·2·3(`eyeArchRange`) 은 여전히 **못 찾았을 때만** 쓰는 대체 자리입니다.

⚡ **v3.1.0 — 마지노선이 3~4칸으로, 눈썹 길이(눈금)에 따라 늘어납니다.** 원장님 지시(2026-
08-30) 「사진이 늘어나거나 눈썹이 길어지면 3, 3.5, 최대 4칸까지 마지노선」— 고정 3칸 대신
`browSpanUnits`(산꼭대기~꼬리쪽 끝의 실제 눈금 거리, 확대율과 무관)가 12칸 이하면 3칸,
20칸 이상이면 4칸, 사이는 선형 보간(`BROW_SPAN_LO/HI`, `FLOOR_LO/HI`). ⚠️ 12·20 경계는
아직 미검증 — 지금 4장은 전부 10.4~14.75칸이라 거의 3칸 그대로였고, 실제로 긴 눈썹 사진을
받아야 20칸 근처가 맞는지 확정됩니다. **+ 눈꼬리 표준값을 마지노선 밖으로 강제로 밀어내던
v3.0.0의 동작은 폐지**했습니다 — 원장님 지시 「4칸 전 눈꼬리가 3·3.5·4에 걸릴 경우 눈꼬리가
마지노선으로 더 늘어나지 않는다」. 눈꼬리는 랜드마크 실측값이라 그 자체로 신뢰할 자리이고,
억지로 밀어내면 오히려 얼굴 비율을 왜곡합니다. 실제 사진 4장은 값 변화 없음(회귀
174/174 확인) — 이 변경은 지금 4장이 아니라 **앞으로 받을 긴 눈썹·확대 사진**을 위한
안전판입니다.

⛔⛔ **v3.2.0 — 아치선이 눈썹 몸통 한가운데 서던 실기기 버그. 원인은 「열 순서 뒤집힘」.**
원장님이 2026-08-30 보내 주신 실기기 스크린샷에서 아치선이 산꼭대기보다 **안쪽**,
num ≈ 35(눈동자보다 한참 코 쪽)에 섰습니다. 마지노선 3~4칸 규칙과 정반대 방향입니다.

- **찾은 방법**: 원장님이 원본 사진을 주셔서, 스크린샷과 원본을 화소 정합(일치도 0.996)해
  눈금 자를 맞췄습니다. 같은 사진을 컨테이너에서 같은 코드로 돌리면 **24.18**(산꼭대기에서
  3.27칸 바깥, 정상)이 나옵니다. 그 실제 열 자료를 **순서만 뒤집어** 같은 알고리즘에
  넣으니 **35.08** — 실기기 실측과 0.3칸 이내로 일치했습니다.
- **원인**: `autoFromDrawing()` 의 방향 판정이 `pts[0].x > cx` — **끝점 하나가 센터보다
  오른쪽인가** — 였습니다. 화면 오른쪽 눈썹에서 읽힌 열이 **센터를 하나라도 넘으면**
  (미간 잔털·그늘) 판정이 뒤집혀 **열 순서 전체가 거꾸로** 서고, 아치선 탐색이 꼬리 쪽이
  아니라 앞머리 쪽으로 걸어갑니다. 이 사진은 미간 잔털이 센터에서 **3.3칸** 거리까지
  읽혔습니다 — 여유가 거의 없었습니다.
- **왜 실기기에서만**: 예비 경로(`fallbackBox`)는 상자를 센터(cx±4)에서 잘라 애초에 못
  넘습니다. **랜드마크 경로(`browBoxes`)에는 그 잘라내기가 없습니다.** 그래서 컨테이너
  재현(MediaPipe 차단)에서는 한 번도 안 났습니다.
- **고침**: `seqOrient(pts, cx)` 신설 — 앞머리(seq[0])는 **센터에 가까운 끝**.
  같은 파일의 `growEnd` 가 쓰던 잣대와 같습니다. 열이 센터를 안 넘는 보통 사진에서는
  예전 식과 결과가 같습니다. 회귀 **175** 가 straddleR(센터를 넘은 오른쪽 눈썹) 경우로 잠급니다.
- **함께 고친 것**: 픽셀 판독도 실패하고 눈꼬리 랜드마크도 없으면 예전에는 **아무것도 안 해서**
  임시로 놓아 둔 **산꼭대기 값(0칸)** 이 그대로 남았습니다 — 「3개의 칸 내부로 들어오지
  않는다」를 가장 크게 어기는 자리입니다. 이제 산꼭대기에서 마지노선(3~4칸)만큼 내보냅니다.
  회귀 **176** 이 세 갈래(pixel·corner·floor) 모두에서 이 띠를 잠급니다.
- **`S.archRead` 신설** (이너의 `S.innerRead` 와 같은 방식) — 무엇을 보고 그 자리에 섰는지
  (`from` · `pkX` · `floorUnits` · `browSpanUnits`)를 남깁니다. 이번 버그를 찾는 데 오래
  걸린 이유가 그 기록이 아무 데도 없어서였습니다. ⛔ 지우지 마세요.

❌ **같이 시도했다가 접은 것 — 눈 구간으로 픽셀 판독 검증하기.** 원장님 질문(「눈꼬리
랜드마크 설정하는것도 좋은 방법이니?」)에 따라 `eyeArchRange().a`(눈동자 바깥 끝)보다
안쪽으로 들어온 픽셀 판독을 버리는 안을 넣어 봤습니다. **회귀 91 이 바로 잡았습니다** —
모양 B(아치가 안쪽 x260에 있는 눈썹)의 **정상 판독 234** 를 「너무 안쪽」으로 오판해
눈꼬리(150)로 버렸습니다. 눈 구간은 사람마다 눈썹-눈 배치가 달라 판독을 감시할 만큼
촘촘한 자가 못 됩니다. ⛔ 문턱만 느슨하게 해서 되살리지 마세요 — 통과하게 맞춘 문턱은
근거가 없습니다. 눈꼬리는 지금처럼 **못 찾았을 때의 표준값**으로만 씁니다.

⚠️ **아직 확인 못 한 것**: ① 실기기에서 뒤집힘을 일으킨 **정확한 입력**은 컨테이너에서
재현하지 못했습니다(MediaPipe 차단으로 랜드마크 경로를 못 탐) — 재현한 것은 「순서가
뒤집히면 그 값이 나온다」까지입니다. ② 확정 4장(A·B·C·D)은 이 세션에 사진이 없어(공개
저장소·컨테이너에 고객 사진을 남기지 않는 규칙) **재확인하지 못했습니다.** 배포 후
실기기에서 4장 + 이번 사진을 다시 열어 값을 확인해야 확정입니다.

### 1-33. 가로 길이 슬라이더 · 표식 테두리는 설정을 따른다 (v1.68.0 — 원장님 지시 2026-08-24)

> 「가로 길이 **아주 짧게도 가능하도록 슬라이드로** 처리해 주고, 슬라이드 이동 시 미리보기에서
>  **길이가 짧아졌다 길어지는 것이 보이는 상호작용** 보이도록」
> 「꼬리와 아우터에는 **회색 테두리가 자동으로 생겼다. 내가 의도하지 않음.**
>  기본은 모두 테두리 없음으로 했으니 변경 — 테두리 없게」

| 규칙 | 내용 |
| --- | --- |
| 가로 길이 = 슬라이더 | `#rngLen` **8~60%** (자 전체 길이 ÷ 눈썹 폭). `S.look.hlen` = 값/200 → 0.04~0.30. 기본 28%(0.14) 그대로 ⛔ 3단 세그먼트로 되돌리지 마세요 — **아주 짧은 자**를 만들 수 없습니다 |
| 끄는 동안 같이 변한다 | `input` 마다 **미리보기와 실제 화면을 둘 다** 다시 그립니다. 미리보기 자의 반폭 = `prevHalf()` — 설정 범위가 미리보기 폭에 그대로 대응하므로 **눈앞에서 줄었다 늘어납니다** |
| 시트 한 화면 | 가로 길이·투명도는 **한 줄**에 둡니다. 시트가 길어지면 스크롤해야 해서 시술 중 손이 더 갑니다 |
| **표식 테두리 = 설정** | 십자 모서리 표식의 테두리는 「테두리」 설정을 그대로 따릅니다 (`drawLive` 와 같은 규칙). v1.64~1.67 은 짙은 테두리(`#0A0D14` 5.5px)를 **코드에 박아** 두어, 설정이 「없음」인데도 회색 테두리가 났습니다 ⛔ 다시 박지 마세요 |

회귀 **118** 이 이 전부를 잠급니다 (슬라이더 존재 · 3단 제거 · 미리보기/실제가 함께 줄고 늚 · 되돌림 · 테두리 없음 = 코너당 1겹 / 70% = 2겹).

### 1-32. 아치·아치선은 한 점 · 플로우는 꼬리에서 끝 (v1.67.0 — 원장님 지시 2026-08-24)

> 「아치선 세로선도 꼬리와 마찬가지로 아치 가로선을 움직일 때 함께 사방으로 움직이도록 조절.
>  아치선을 움직일 때 아치세로선이 따라 움직이고, **아치두께 가로선도 이에 따라 함께 따라온다.**
>  대신 아치두께선을 움직일 땐 양방향 아니고 **위아래만** 움직이는 것 유지한다. …
>  이에 따라 가이드 플로우는 **아치 - 아치두께 - 꼬리로 종료**된다」

| 규칙 | 내용 |
| --- | --- |
| **아치 + 아치선 = 한 점** | 산꼭대기 = (아치선 x, 아치 y). 아치 자든 아치선 세로선이든 잡고 사선으로 끌면 dy → 아치(h2), dx → 아치선(v6). 거울 쪽에서 잡으면 dx 부호 반전 — 꼬리·아우터(1-27)와 **같은 규칙** (`ARCH_PAIR`) |
| **아치두께가 따라온다** | 아치가 움직인 만큼 `archThickness` 가 **같은 dy** 로 따라옵니다 → 두께는 유지된 채 산 전체가 움직입니다. 손으로 끄는 사선과 **바·화살표 둘 다** 같은 규칙입니다 (한쪽만 고치면 두께가 어긋납니다) |
| ⛔ 아치두께 자체는 위아래만 | 두께를 재는 선이라 x 가 흔들리면 잰 값이 무너집니다. 사선을 퍼뜨리지 마세요 |
| **플로우 종료 = 꼬리** | `이너 → 앞두께 → 앞머리 → 아치(+아치선) → 아치두께 → 꼬리(+아우터)` 로 끝. 아치선(v6)은 독립 스텝이 **아닙니다** ⛔ 다시 넣지 마세요 |
| 스텝의 바 배정 | 쌍 스텝에서는 **위아래 바 = 가로선 · 좌우 바 = 짝 세로선**. 짝 세로선을 움직여 끝내도 같은 스텝을 끝낸 것으로 칩니다 (`STEP_PAIRS` 표 하나가 마커·바·스텝넘김을 모두 결정) |

#### 십자 안쪽 모서리 — 팔 방향이 스텝마다 다르다

> 「아치선의 굵은 색상은 **아래와 안쪽이 아니라 안쪽과 위** 라인이어야 한다 — 위치 교정」

| 스텝 | 팔 방향 | 왜 |
| --- | --- | --- |
| 아치 (h2 × v6) | 안쪽 + **위** | 산 **바로 아래가 눈썹**이라, 아래로 그으면 드로잉을 덮습니다 |
| 꼬리 (h3 × v4) | 안쪽 + **위** | v1.68.0 에서 아래 → **위**로 교정 (아래 참고) |

> ⚠️ **v1.68.0 (원장님 지시 2026-08-24)** — 「플로우 적용 시 **꼬리와 아우터 지점은 내부쪽과 위**다.
> 기존 보라색 유지하고 **민트색 쳐진 부분**으로 색상 굵음 처리 다시 할 것」 (스크린샷에 민트 펜으로
> **위·안쪽** 두 팔을 그려 주셨습니다.) → 두 스텝 모두 **안쪽 + 위**입니다.
> ⛔ 아래쪽으로 되돌리지 마세요. 회귀 114 가 두 스텝을 각각 검사합니다.

### 1-31. 드로잉 맞춤 — 머리카락 방어 (v1.66.0 · 2026-08-24)

> 원장님 사진에서 `드로잉 맞춤` 이 **관자놀이 머리카락을 눈썹으로 읽었습니다.**
> 스크린샷을 회전해 픽셀로 재보니 아치선 280px · 아우터 100px · 꼬리 340px · 앞두께 175px
> 벗어났고, 맞은 것은 앞머리와 이너뿐이었습니다.

#### 왜 그랬나

| 원인 | 설명 |
| --- | --- |
| ① 탐색 상자가 **화면 절반** | 랜드마크가 없으면 기준 쪽 절반을 통째로 훑었습니다. 눈썹이 없는 가장자리 열에서는 **거기서 제일 어두운 것**(머리카락)이 그대로 표본이 됩니다 |
| ② 양 끝과 아치를 **열 하나**로 결정 | `seq[0]`·`seq[n-1]`(이너·아우터)와 아치 봉우리가 극값 열 하나로 정해져, 머리카락 한 조각이 선 전체를 끌고 갔습니다 |

#### 고친 것 (5겹)

| 단계 | 규칙 | 왜 |
| --- | --- | --- |
| 탐색 상자 | 랜드마크가 없으면 **지금 선이 있는 자리**를 사전 정보로 씁니다 — 이너~아우터 사이에 안쪽 +55% · 바깥 +30%, 위아래는 밴드의 앞뒤 한 배쯤, 눈 기준선 위까지. ⛔ 화면 절반으로 되돌리지 마세요 | 바깥(관자놀이)에 머리카락이 있습니다. 안쪽은 사람마다 앞머리 위치가 크게 다르므로 넉넉히 |
| ⓐ 범위 | 랜드마크 눈썹 폭의 **바깥 14%** 까지만 허용 | 머리카락은 늘 눈썹 바깥에 있습니다 |
| ⓑ 잉크 | 잉크가 중앙값의 **38% 미만**인 바깥 열을 끝에서부터 버림 | 눈썹이 없는 빈 열 |
| **ⓒ 창 천장** | 탐색창 **위에 닿은** 바깥 열을 끝에서부터 버림. ⚠️ 바닥은 세지 않습니다 (창 아래는 눈꺼풀 방어선이라 앞머리가 닿습니다) | 눈썹은 옆으로 눕고 **머리카락은 세로로 서서 창 위로 빠져나갑니다.** 잉크·두께로는 못 거릅니다 — 머리카락이 눈썹보다 **더 짙습니다** (원장님 사진: 눈썹 2039 vs 머리카락 5160) |
| **ⓓ 끊긴 조각** | 열 간격의 **2.5배**보다 크게 벌어져 떨어진 **전체의 25% 미만** 조각은 양 끝에서 버림. 가운데가 끊긴 것은 그대로 둠 | 눈썹은 한 덩어리입니다. 성근 털 사진에서 눈썹 절반을 잃지 않으려고 **끝에서만** 버립니다 |
| ⓔ 실패 인정 | 다듬고 남은 폭이 눈썹 폭의 **50% 미만**이면 **판독 실패**로 돌림 | 「애매하면 아무것도 보여주지 않는다」 — 틀린 자리에 확신 있게 놓는 것이 안 놓는 것보다 나쁩니다 |
| 아치 봉우리 | 이웃 3열 평균으로 완만하게 보고, **양 끝 15% 는 후보에서 제외** | 아치는 앞머리나 꼬리 끝에 있을 수 없습니다. ⛔ 단일 극값으로 되돌리지 마세요 |


#### ⚠️ v1.69.0 보완 — 예비 경로의 **위아래 창**과 두께 검사 (원장님 지시 2026-08-24)

> 「맞은 라인은 앞두께와 아치 라인만 맞았다고 — **나머지 안 맞은 것 교정**」

원장님 사진(얼굴을 크게 확대한 사진)에서 **앞머리·아치·꼬리가 전부 같은 값**으로 나와
자들이 눈썹 위 여백에 뭉쳤습니다. 실제 픽셀로 재현해 보니 세 값이 모두 **탐색창 천장(112px)**
이었습니다 — 창이 눈썹 몸통을 가로지르고 있었던 것입니다.

| 원인 | 고침 |
| --- | --- |
| 예비 경로 창의 **위아래**를 「지금 선 ±1.3h/1.7h」로 잡았다 (v1.66.0). 눈썹이 지금 선보다 위에 있으면 창이 눈썹을 자른다 | 위아래는 **눈 기준선 위 45% ~ 위 3%** (`FB_UP`/`FB_DOWN`). 눈썹은 언제나 눈 위 45% 안에 있습니다 ⛔ 「지금 선 ±몇 배」로 되돌리지 마세요 |
| **두께 상식 검사**가 `b.h`(예비 경로에서는 **지금 선 간격**)를 기준으로 돌았다 | 두께 검사는 **랜드마크가 있을 때만**. 원장님 사진은 선 간격 15px vs 실제 눈썹 50px 이라, 제대로 읽은 판독이 「두껍다」는 이유로 버려졌습니다 |
| 아래 경계가 눈 기준선 위 **3%** 라 **눈꺼풀·쌍꺼풀 주름**이 판독에 섞였다 | v1.70.0 — 원장님이 그 자리에 X 를 찍어 「**해석 불필요**」라고 하셨습니다 (2026-08-24). 아래 경계를 눈 기준선 위 **8%**(`FB_DOWN`)로 올려 그 띠를 아예 보지 않습니다 ⛔ 3% 로 되돌리지 마세요 |

⚠️ **좌우(x) 범위는 그대로 「지금 선 기준」입니다** — 관자놀이 머리카락을 막는 자입니다 (위 표).
회귀 **120·120b** 가 이 증상을 재현합니다: 옛 창으로 되돌리면 아치가 80 대신 133px 로,
세 가로선이 한 값으로 뭉칩니다.

#### 무엇이 잠겨 있나

회귀 **115**(얼굴 인식 성공 경로) · **116**(인식 실패 = 예비 경로) 가 관자놀이 머리카락이 있는
사진으로 검사합니다. 방어를 끄면 116 이 **아우터 73px(정답 120px)** 로 즉시 실패합니다 —
머리카락이 47px 바깥으로 끌고 간 그 오차입니다. 이 두 검사를 지우지 마세요.

> 실제로 116 을 지키는 것은 **ⓒ·ⓓ** 입니다. ⓐ·ⓑ·ⓔ 는 합성 사진에서는 발동하지 않는
> **추가 방어**입니다 (실제 사진의 옅은 그림자·먼 얼룩용). 지울 때는 그 사실을 알고 지우세요.

### 1-30. 가이드 플로우 재정렬 · 프롬프트 · 십자 모서리 (v1.64.0 — 원장님 지시 2026-08-23)

> 「가이드 루틴 앞두께에서 아치 - 아치두께로 변경. 플로우가 아치 - 아치두께 - **꼬리+아우터
>  동시적용** - 아치선. 꼬리와 아우터는 지금 함께 움직이므로 두 선이 맞닿아 **십자가 모양의
>  내측을 포인트로** 꼬리 자동 정렬 찾는 프롬프트로 넣을 것」

#### 플로우 (확정)

`이너 → 앞두께 → 앞머리 → 아치 → 아치두께 → 꼬리+아우터 → 아치선`
> ⚠️ **v1.67.0 에서 바뀜** — 아치선이 아치와 한 스텝이 되어 플로우는 **꼬리에서 끝납니다** (1-32).

| 규칙 | 내용 |
| --- | --- |
| 아치 → 아치두께 | **순서를 바꿨습니다** — 산 높이를 먼저 잡고 그 위에 두께를 얹습니다 (v1.63 까지는 두께가 먼저) |
| **꼬리+아우터 = 한 스텝** | `TAIL_PAIR`. 그 차례에는 **위아래 바=꼬리 · 좌우 바=아우터**, 화면을 사선으로 끌면 둘이 함께 (1-27). 아우터를 움직여 끝내도 **같은 스텝을 끝낸 것**으로 쳐서 아치선으로 넘어갑니다 |
| 아치선이 마지막 | 그 뒤로는 종료 (처음으로 돌아가지 않음) |
| ⛔ 아우터를 독립 스텝으로 되돌리지 말 것 | 꼬리와 한 점입니다 |

#### 스텝 프롬프트 (`#guideTip`)

캔버스 위쪽 가운데에 **한 줄**로, 지금 차례가 무엇을 맞추는지 알려 줍니다 (`tip_<key>` i18n).
가이드를 끄면 숨습니다. ⛔ 두 줄로 늘리지 마세요 — 시술 화면을 가립니다.

> **v1.69.0 (원장님 지시 2026-08-24 「나머지 교정 프롬프트 다시 써라」)** — 문구 앞에 **①~⑥ 번호**를
> 붙여 지금 몇 번째인지 화면에서 읽히게 하고, 각 줄이 **무엇의 어느 선**에 맞추는 것인지와
> **어느 바로 움직이는지**를 함께 적습니다. 드로잉 맞춤을 누르면 가이드가 **① 부터 다시** 돕니다.
> 칩은 `nowrap + ellipsis` 라 길면 끝이 조용히 잘립니다 — 회귀 119 가 **좁은 폰(667×375)** 에서
> 여섯 줄이 잘리지 않는지 검사합니다. ⛔ 번호를 떼지 마세요.

#### 십자 안쪽 모서리 표식

꼬리 차례일 때만, 꼬리 자(h3 = 꼬리 **윗선**)와 아우터(v4)의 교점에서 **눈썹 몸통 쪽(안쪽·아래)**
으로 ㄱ자 팔을 그립니다 — 원장님이 빨간 펜으로 그려주신 모양. 이 모서리를 드로잉의 꼬리 끝에
얹으면 꼬리·아우터가 한 번에 맞습니다. ⛔ 다른 차례에는 그리지 않습니다 (화면이 시끄러워집니다).

#### 화살표 미세 이동 · 오폭 방지

| 항목 | v1.63 | v1.64 | **v1.65** | 왜 |
| --- | --- | --- | --- | --- |
| 줌 화살표 | 0.03 (≈3.4%/회) | **0.004** (≈1.5%/회) | 그대로 | 「이동이 매우 큼 — 아주 미세하게」 |
| 위아래·좌우 화살표 | 0.012 (≈9px/회) | 0.002 (≈1.7px) | **0.0012** (≈**1px**/회) | 「더 예민하게」 — 한 번 눌러 튀면 처음부터 다시 |
| **위아래 모드 방향** | ▶ = 아래로 | — | **▶ = 사진이 위로** | 원장님 손이 그렇게 기억합니다. ⛔ 부호를 되돌리지 마세요 (바를 끄는 방향도 같이 뒤집혀 오른쪽=위 로 일치) |
| 좌우 조정바 길이 | 버튼 행과 동일 | **행 × 1.10** (왼쪽으로만) | 「10% 더 길게」. 오른쪽으로 늘리면 화면 밖 |
| 버튼 행 ↔ 바 갭 | 6px (compact 5) | **16px (compact 13)** | 「화살표 누를 때 위 버튼이 실수로 눌리지 않도록」 |

⛔ 화살표 step 을 다시 키우지 마세요. 바를 끌면 여전히 크게 움직입니다 — 화살표는 **마무리 미세조정 전용**입니다.

회귀 38·101·114 가 이 전부를 잠급니다.

### 1-29. 앱 아이콘 = 데칼코마니 나비 (v1.63.0 — 원장님 확정 2026-08-23)

> 원장님 원본 와이어 나비 일러스트를 **사진에서 100% 트레이스**해 만든 아이콘. 여러 시안
> (골드/민트/그라데이션 채움 · 백라인 · 발광 강도 3단계) 중 **H6** 를 확정하셨습니다.

| 항목 | 값 |
| --- | --- |
| 구조 | **양쪽 날개 같은 짙은 검정 선**(캡컷·마누스급 두께) · **오른쪽 선을 따라서만 민트 발광** |
| 발광 | 선에 붙는 진한 틸(#00BEA6 근처) + 넓게 퍼지는 민트 번짐 — 흰 배경에서 민트가 씻겨 보이는 문제를 진한 틸 코어로 해결 |
| 모티브 | **데칼코마니 오마주** — 왼쪽 = 먹 원본, 오른쪽 = 빛나는 전사. 앱의 원리(기준 쪽에 그리면 반대쪽에 찍힌다)를 로고가 말한다 |
| 파일 | `icon-512` / `icon-192` / `apple-touch-icon(180)` — 원본 1024 는 백업 zip `icons/HG-H6.png` |
| ⛔ | 나비 곡선은 원장님 원본 일러스트의 트레이스입니다 — 임의로 다시 그리지 마세요 |

### 1-28. 설치 이름 = Brow Balance (v1.62.0 — 원장님 지시 2026-08-23)

> 「내부 이름은 브로우 밸런스 영어로 — 다운받을 때 이름」

| 항목 | 값 |
| --- | --- |
| 홈 화면 설치 이름 | **Brow Balance** (manifest `name`/`short_name` · `apple-mobile-web-app-title` · `<title>`) |
| 앱 안의 표기 | **Perfect Brow 유지** (홈 로고·버전 표시) — 원장님이 바꾸라 하시기 전까지 |
| 주의 | 이미 설치된 폰은 이름이 안 바뀝니다 — **삭제 후 다시 「홈 화면에 추가」** 해야 새 이름 |

### 1-27. 꼬리·아우터 사선 동시 이동 (v1.61.0 — 원장님 지시 2026-08-23)

> 「꼬리와 아우터는 붙어있는데, 이 라인 두 개가 위아래로만 움직이는 것이 아니라
>  **양방향 사선 동시에** 움직일 수 있니?」 → 선택지 중 **「둘 다 사선」** 을 고르심.

꼬리 끝은 사실 **한 점**입니다 — (x = 아우터, y = 꼬리 자). 그래서 꼬리 자든 아우터
세로선이든 **잡고 사선으로 끌면 둘이 함께** 움직입니다: dy → 꼬리(h3) · dx → 아우터(v4).

| 규칙 | 내용 |
| --- | --- |
| 대칭 | `setLine()` 이 처리 — 오른쪽 꼬리·아우터도 같이 움직인다 (1-2 그대로) |
| 거울쪽 부호 | 오른쪽(거울) 인스턴스를 잡으면 dx 부호가 반전된다 — 기존 세로선 mirrored 와 동일 규칙. 꼬리 자의 좌/우 판정은 **누른 지점이 센터선(v1)의 어느 쪽인가** |
| 한 축만 만지고 싶을 때 | 아래 조절 바(위아래/좌우)는 여전히 **한 축씩**만 움직인다 — 사선 드래그의 미세조정 보완 |
| 여러라인 모드 | 이 규칙을 **끈다** — dragManyBy 가 이미 각자 축으로 움직인다 |
| ⛔ 다른 자로 확대 금지 | 앞머리·앞두께·아치·아치두께는 **두께를 재는 쌍**이라 x 가 세로선에 묶여 있습니다. 사선이 되면 잰 값이 흐트러집니다. 꼬리·아우터만 「점」입니다 |

회귀 113 이 이 전부를 잠급니다 (실제 포인터 드래그로 검증).

### 1-26. ⚠️ 원장님 확정 기본 세팅 (v1.60.0 — 2026-08-23)

> 「지금 내가 앱에 설정한 선을 기본으로 셋팅하고 **못 박아줘**」
>
> **누가 · 언제 · 왜** — 원장님이 실제 시술 화면에서 직접 맞춘 값을, 2026-08-23 폰 스크린샷을
> 픽셀로 판독해 코드 기본값(`LOOK_DEF`)으로 확정했습니다. 취향 메모가 아니라 **기준**입니다.

| 항목 | 확정값 | 왜 |
| --- | --- | --- |
| 이너 / 아치 / 꼬리 | 민트 `#5EEAD4` / 파랑 `#2E8BFF` / 보라 `#A855F7` | 기존 고유색 유지 (레일 띠 색과 1:1) |
| 테두리 | **없음** · 색 자동 | 원장님 사진에서는 테두리 없이도 읽힘 → 화면을 더 조용하게 |
| 선 굵기 | **얇게 (0.8)** | 자가 눈썹 위를 덜 가림 |
| 가로 길이 | **짧게 (0.14)** | 재는 구간만 있으면 충분 |
| 투명도 | **75%** | 선 아래 드로잉이 비쳐 갭을 눈으로 잰다 |
| 잡은 선 심 | **먹 `#14161B`** | 잡은 순간 「짙은 한 줄」 |
| 잡은 선 테두리 | **없음** | 살구 헤일로가 시술 중엔 과했다는 판단 |
| 잡은 선 굵기 · 투명도 | **얇게 (0.8)** · **95%** | 기본 선과 따로. 잡았을 때만 살짝 진해진다 |

| 규칙 | 내용 |
| --- | --- |
| ⛔ **임의 변경 금지** | 이 값을 바꾸려면 **원장님 확인을 먼저** 받으세요. 회귀 112 가 12개 값을 통째로 잠급니다 — **테스트를 고쳐서 통과시키지 마세요** |
| **새 기기·데이터 삭제 후에도 동일** | `loadLook()` 이 저장값 없으면 이 값으로 시작합니다. 「기본으로」 버튼도 여기로 돌아옵니다 |
| **추천 조합은 색·테두리만** | `LOOK_COMBOS` 의 밝은/어두운 사진 조합은 `inner·arch·tail·edge·edgeC` 만 담습니다. 굵기·길이·투명도·잡은 선은 **원장님 값이 그대로 남습니다** — 조합을 돌려도 손에 익은 두께가 안 변하게 |
| 회귀 테스트의 중립값 | 선 그리기 규칙을 보는 검사(101·102·103·104·108·109)는 `weight 1 / hlen 0.19 / alpha 1` 중립값으로 고정해 둡니다. 기본값이 바뀌어도 그 검사들이 흔들리지 않게 |

### 1-25. 설정 — 선 모양 (v1.56.0 — 원장님 지시 2026-08-23)

> 「눈썹 위에 한/영 버튼 제거 후 그 자리에 **설정 배지** 하나 추가. 설정에 들어가면 각 선마다
>  사용자가 선호하는 **색상표와 아래 미리보기 선** … 선의 **테두리 유무, 몇 퍼센트, 테두리 색상도
>  대비색으로** … 하나 색상 선택 후 **동일 적용** … **선 굵기 / 가로선 길이 / 투명도** …
>  맨 위에 **추천 3개 조합** (현재 세트 - 추천 - 추천) … 너무 자세하게는 말고 직관적으로 깔끔하게」

#### ⚠️ 색상표를 이렇게 고른 이유 (숫자 — 이 근거를 지우고 색만 바꾸지 마세요)

원장님 사진의 피부(밝은 쪽 `rgb(215,170,140)` · 보통 `rgb(189,128,100)`)와 짙은 눈썹
(`rgb(55,42,38)`) **양쪽 모두에 밝기 대비 2:1 을 넘기는 색은 사실상 없습니다.**

| 색 | 밝은 피부 | 보통 피부 | 짙은 눈썹 | 어두운 배경 |
| --- | --- | --- | --- | --- |
| 민트 #5EEAD4 | 1.42 | 2.21 | 9.33 | 13.14 |
| 파랑 #2E8BFF | 1.60 | 1.03 | 4.11 | 5.79 |
| 흰색 #FFFFFF | 2.10 | 3.26 | 13.80 | 19.43 |
| 먹 #14161B | **8.63** | 5.55 | 1.31 | 1.07 |

→ 그래서 **역할을 나눴습니다**: **색**은 피부의 주황(약 25°)에서 **색상(hue)이 먼 쪽**으로 고르고,
**밝기 대비는 테두리(대비색 헤일로)** 가 만듭니다. 테두리가 이 설정의 핵심입니다.

| 항목 | 값 |
| --- | --- |
| 색상표 7 | 민트 `#5EEAD4`(166°) · 라임 `#A3E635`(81°) · 하늘 `#38BDF8`(199°) · 파랑 `#2E8BFF`(215°) · 보라 `#A855F7`(280°) · 핑크 `#FF4D94`(334°) · 먹 `#14161B`(밝은 피부 8.63:1 — **밝은 사진 최강**) |
| 묶음 | 이너(v2·앞머리·앞두께) / 아치(아치선·아치·아치두께) / 꼬리(아우터·꼬리). 눈·센터는 스펙 색 유지 |
| 테두리 | 없음 / 40% / 70% / 100% — 선 굵기의 그 비율만큼 **양쪽으로** 더 넓은 대비색을 아래에 깐다 |
| 테두리 색 | 자동(휘도 0.32 기준 — 밝은 선엔 먹 `#0A0D14`, 짙은 선엔 흰색 `#FFFFFF`) / 흰색 / 먹색 |
| 선 굵기 | 얇게 0.8× · 중간 1× · 두껍게 1.35× |
| 가로 길이 | **슬라이더 8~60%** (= 자 길이 ÷ 눈썹 폭 · `S.look.hlen` 0.04~0.30 · 기본 28% = 0.14). v1.67 까지의 3단(짧게·중간·길게)은 **폐지** — 아주 짧은 자를 만들 수 없었습니다 (1-33) |
| 투명도 | 50~100% (5% 단위) |
| 추천 3조합 | ① **현재 세트**(시트를 연 순간 값 — 되돌아올 자리) ② **밝은 사진**(이너 먹 + 흰 테두리 70%) ③ **어두운 사진**(밝은 색 + 먹 테두리 70%) |
| **잡은 선** (v1.57.0 · v1.59.0 확장) | 색 변경은 **두 컨셉**입니다 (원장님): ① 선택하기 전(차례) — 「기본 선」 탭 ② **선택 후 움직일 때** — 「잡은 선」 탭. 잡은 선은 기본 선과 **완전 분리**: `dragCore`(심 · 기본 먹) · `dragEdge`(테두리 · 기본 살구 · **"none" = 테두리 없음**, 사선 스와치) · `dragW`(굵기 0.8/1/1.35 — 기본 선 weight 와 무관) · `dragOp`(투명도 50~100%). 미리보기 맨 아랫줄이 이 상태입니다 |
| **설정 시트 구조** (v1.59.0) | 왼쪽 = 미리보기(상시) · 오른쪽 = **탭 2개** 「기본 선 · 차례」/「잡은 선 · 움직일 때」 — 원장님: 「잡은선과 다른선을 따로 분리, 명칭이 없어 불분명」. ⛔ 두 탭의 값을 한 화면에 섞어 돌아가지 마세요 |
| **rot90 회전** (v1.59.0) | 시트는 body 직속 fixed 라 `.screen` 의 가짜 회전을 상속받지 못해 **아이폰 세로 잠금에서 설정만 세로로 떴다**. `body.rot90 .sheet` 가 같은 변환을 적용한다. `.sheet-in` 폭은 **%**(회전 컨테이너 기준)로 — vw/vh 는 기기 세로 기준이라 좁아진다. ⛔ 이 블록을 지우면 재발 (회귀 111) |
| **이전 설정으로** (v1.57.0) | 조작하다 별로면 **시트를 연 순간 값으로 한 번에 복귀** (`S.lookSnap`). 「기본으로」와 다릅니다 — 기본으로는 공장값, 이전 설정으로는 방금 전 내 값 |
| **조합 순환 버튼** (v1.58.0) | 가이드 오른쪽 `#btnLookCycle`. 클릭마다 **내 세트 → 밝은 사진 → 어두운 사진 → 다시 내 세트**. 「내 세트」는 순환을 시작한 순간의 내 설정(`S.lookOwn`)이라 **한 바퀴 돌면 그대로 복귀** — 시술 중 잘못 눌러도 잃는 것이 없다. 라벨이 지금 조합 이름을 보여준다. ⚠️ 이 버튼은 **좁게** 유지할 것 — 왼쪽 도크가 넓어지면 가운데 도크(잠금=센터선)를 밀어낸다 (회귀 83 도크간격) |
| 저장 | `localStorage["pb_look_v1"]`. 값은 **누르는 즉시** 적용·저장 (확인 버튼을 찾을 필요 없게) |

| 규칙 | 왜 |
| --- | --- |
| **선 색 = 레일 띠 색** | 설정에서 색을 바꾸면 왼쪽 레일 버튼 띠도 함께 바뀝니다 (BASELINE 1-20). 배지를 지운 뒤로 **색이 곧 이름표**입니다 |
| **미리보기는 실제 값으로** | 왼쪽 밝은 피부 / 오른쪽 짙은 눈썹 두 배경 위에 `S.look` 그대로 그립니다. 색을 하드코딩하면 「선택 시 어떤 색이 보일지」가 거짓말이 됩니다 |
| **조용·드래그 상태는 안 바뀜** | 1-24 의 세 상태는 그대로입니다. 설정은 **② 차례일 때의 고유색**만 바꿉니다 |
| 편집 화면 한/영 칩 제거 | 언어는 **사진 선택(홈) 화면**에서 고릅니다. 시술 중 쓰지 않는 버튼은 없앱니다 (손 덜 가게) |

회귀 60·109 가 이 전부를 잠급니다.

### 1-24. 선의 상태는 딱 세 가지 (v1.55.0 — 원장님 지시 2026-08-22 · 1-22, 1-23 을 대체)

> 「가로바들이 회색과 고유색으로 섞여있는데 **고유색으로만 1개선**으로 변경.
>  가이드 시작되면 블링킹이 첫 2번 깜빡이는 것을 **느린 블링킹 한 번, 4초에 한 번씩**
>  다음 움직임 전까지 계속 반복. 블링킹은 고유색으로 밝았다 조금 투명해진다.
>  **모든 선을 클릭해서 움직일 때는 짙은 회색과 살구색 테두리**,
>  움직임 없을 때는 현재 얇은 회색 유지」

| 상태 | 모습 | 값 |
| --- | --- | --- |
| ① **조용** (지금 차례가 아닌 선) | 얇은 짙은 회색 **한 줄** — 가로 자도 색 토막 없이 전부 회색 | 가로 `HALF_GREY #14161B` 1.0 / 0.5 · 세로 `VGREY_W 1.7` / 0.6 |
| ② **지금 차례 · 선택** | **고유색 한 줄** (자의 전체 길이) · 4초에 한 번 느리게 깜빡 | `sp.w + 1.8` · 불투명 · CSS `.blink` |
| ③ **잡고 움직이는 중** | **짙은 회색 심 + 살구색 테두리** | 심 `#14161B` (②와 같은 굵기) · 테두리 `APRICOT #FFC9A3` +3.2px / 0.95 |

깜빡임은 **CSS** 가 돌린다 — `index.html` 의 `@keyframes pbBlink` + `#guides line.blink`
(4s · ease-in-out · infinite · 10% 지점에서 `stroke-opacity: .45` 까지만 내려간다).

| 규칙 | 왜 |
| --- | --- |
| ⛔ **자를 색/회색으로 쪼개지 말 것** | v1.51~1.53 의 「안쪽 절반 회색 + 50% 관통」은 **폐지**됐습니다. 원장님 지시는 「1개선」입니다 |
| ⛔ **JS 타이머 깜빡임 금지** | v1.54.0 은 160ms 마다 `render()` 를 돌려 드래그 중 화면을 잡아먹었습니다. CSS 애니메이션은 공짜입니다 |
| ⛔ **밝은 쪽(0%·100%)은 반드시 1** | 저장(내보내기)은 SVG 를 직렬화해서 그립니다 — CSS 가 안 붙으므로 **이 값 그대로** 찍힙니다 |
| 깜빡임은 「조금 투명」까지만 | 0.45. 사라졌다 나타나면 시술 중 눈이 피곤합니다 |
| 잡는 순간 깜빡임 정지 | `.blink` 클래스가 빠집니다. 「다음 움직임 전까지 반복」이 지시입니다 |
| 살구색 테두리는 **잡은 선 하나만** | 두 개 이상 살구색이면 어느 것을 잡았는지 다시 알 수 없습니다 |
| 꼬리 자 바깥 회색 참조선 | `TAIL_EXT = true` — v1.53.0 지시를 유지합니다. 자와 **떨어진 별도의 선**이라 「1개선」을 깨지 않습니다 |

회귀 102·104·106·108 이 이 전부를 잠급니다.

### 1-23. (폐지 · v1.55.0) 지시등과 드래그 (v1.54.0 — 원장님 지시 2026-08-22)

> 원장님 제안: 「가이드는 처음 시작 시 **지시등 블링킹 효과 두 번 후 색상 선명** — 그리고
> 드래그 시 … 다음 지시선 두 번 블링킹 — 움직일 때 … 다음 플로우 동일 적용」.
> 원장님이 「너가 추천하는 방식으로 해보자」 하셔서, **깜빡임은 그대로 채택하고
> 드래그 중만 회색이 아니라 반투명 고유색**으로 정했습니다 (아래 '왜' 참조).

| 순간 | 강조된 선의 모습 | 상수 |
| --- | --- | --- |
| **차례가 넘어온 직후** | 두 번 깜빡인다 — 켜짐·꺼짐·켜짐·꺼짐 → **켜짐으로 남는다** | `BLINK_OP 0.12` · `BLINK_MS 160` |
| **잡고 움직이는 중** | **고유색 그대로, 반투명** (아래 드로잉이 비친다) | `DRAG_OP 0.6` |
| **손을 뗀 뒤** | 선명 (불투명) | — |
| **저장(내보내기)** | 언제나 선명 — `exportImage` 가 `stopBlink()` + `dragOn=false` 후 캡처 | — |

한 곳에서만 정합니다: `emphOp()` — 깜빡임 > 드래그 > 선명 순.

| 규칙 | 왜 |
| --- | --- |
| **깜빡임은 켜짐으로 시작** | 느린 기기·회귀 테스트가 `render()` 직후 화면을 읽을 때 선이 사라진 것처럼 보이면 안 됩니다 |
| **타이머는 하나** | 다음 선으로 넘어가면 이전 깜빡임은 즉시 취소 (`clearTimeout`) |
| ⛔ **드래그 중 회색 금지** | 원장님 원안은 회색이었지만, 잡은 선이 회색이 되면 **어느 선을 잡고 있는지 색으로 알 수 없습니다.** 목적(선 아래 드로잉을 보기)은 반투명으로 똑같이 달성되고, 색은 남습니다 |
| ⛔ **저장본에 남기지 말 것** | 깜빡임의 '꺼진' 순간이나 반투명이 그대로 찍히면 선이 흐린 사진이 남습니다 |
| 깜빡임 시작 지점 | ① 사진을 올려 편집이 시작될 때(이너) ② 가이드 버튼을 켤 때 ③ `guideAdvance` 로 다음 차례가 생길 때 |

회귀 108 이 이 전부를 잠급니다 (실제 렌더 픽셀로도 확인: 선명 `rgb(94,234,212)` /
깜빡임 순간 `rgb(148,139,117)`(피부에 묻힘) / 드래그 `rgb(119,191,168)`).

### 1-22. (폐지 · v1.55.0 — 1-24 로 대체) 방향 신호 (v1.52.0)

> v1.51.0 의 「자 바깥쪽만 굵게」 토막 방식은 하루 만에 반려됐습니다 —
> 「이게 세로로 움직여야하는지 가로로 움직여야하는지 직관적이지 않다. 원래 세로로 있었을 땐
> "아 세로줄 이동이구나" 라고 보이는데 지금은 확실치 않은 행동으로 보인다」.
> **긴 세로선 자체가 "좌우로 미는 것"이라는 방향 신호**였는데, 토막만 남기니 자 끝에 붙은
> ㄱ자 모서리로 보였습니다.

두 가지로 해결합니다 (둘 다 원장님 제안).

| 대상 | 조용할 때 | 잡을 때 (선택·가이드 차례) |
| --- | --- | --- |
| **세로선** (이너·아치선·아우터) | **전체가 회색 한 줄** — `#14161B` · 굵기 `VGREY_W 1.7` · 투명도 0.6. 색·토막 없음 | **전체 길이가 고유색** — 그 순간 "세로줄 = 좌우 이동"이 읽힌다 |
| **가로 자** | 고유색이 세로선을 **지나 안쪽으로 반폭의 50%**(`OVERSHOOT 0.5`) · 그 뒤는 얇은 회색 1.0 | 같은 모양으로 색만 진해짐 |
| 센터 `v1` | 검정 전체 길이 (얼굴 축 · 예외) | — |

- **관통 50%의 근거**: 원장님이 스크린샷에 빨간 펜으로 그어주신 길이를 픽셀로 재니
  자 반폭의 45~55%였습니다. 색 토막이 세로선을 **관통**해야 "이 선은 가로"가 읽힙니다.
- 화면 문법이 하나로 정리됩니다: **회색 = 위치 표식 · 색 = 지금 보는/만지는 것.**
- ⛔ 색 토막을 세로선에서 뚝 끊지 마세요(ㄱ자로 돌아감) · 조용한 세로선에 색 토막을
  되살리지 마세요 · 조용한 상태에서 세로선은 색으로 구분되지 않습니다 — 위치와 레일 버튼
  띠로 구분하며, 원장님이 이 트레이드오프를 알고 결정하셨습니다.
- 회귀 106 이 관통 비율(0.38~0.62)·조용=회색·잡으면=색을 잠급니다.
- 시안 검증 교훈: **시안도 렌더 결과를 픽셀로 재서 확인한 뒤 보여드릴 것** — 관통 패치가
  빠진 시안을 세 번 보여드리는 실수가 실제로 있었습니다(2026-08-22).

### 1-21. (폐지 · v1.52.0) 일하는 곳만 굵게 (v1.51.0 — 원장님 지시 2026-08-22)

> 「이너라인이 닿은 앞머리와 앞머리두께 가로라인 사이는 회색 얇은선 처리 …
> **바깥에만 굵은선이고 눈썹 내부는 얇은 회색으로**」
> 「이너라인을 기준으로 **앞부분 얇은 회색 뒷부분만 유지** … 사용자에 따라 얇은 회색으로
> 앞부분을 볼 건지 뒤에 굵은 선으로 맞춰서 볼 건지 자율적으로 사용가능」

한 줄로: **선은 일하는 곳만 굵고, 나머지는 얇은 짙은 회색으로 잇는다.**

| 대상 | 굵은 곳 | 얇은 곳 |
| --- | --- | --- |
| **가로 자** | 세로선(anchor) **바깥쪽 절반** | **안쪽(센터 쪽) 절반** |
| **세로선** (자 2개: 이너·아치선) | 두 자의 **바깥쪽**으로 `VOUT`(0.065)만큼 | **두 자 사이 — 눈썹 몸통** |
| **세로선** (자 1개: 아우터) | 그 자 위아래로 `VOUT` | 나머지 |
| 센터 `v1` | 전체 길이 그대로 | — (얼굴 축이라 예외) |

얇은 부분: `HALF_GREY #14161B` · 굵기 1.0 · 투명도 0.5 (원장님이 세 밝기 중 2안 선택)

| 규칙 | 내용 |
| --- | --- |
| **왜 눈썹 속을 비우나** | 굵은 세로선이 눈썹 몸통을 가로지르면 **그 자리 드로잉이 가려집니다.** 자를 놓고 갭을 보는 작업에서 방해가 됩니다. 세로선은 자 끝을 집어주는 표식이면 충분합니다 |
| **왜 자의 안쪽을 죽이나** | 앞부분(코 쪽)은 참고용이고, 실제로 맞추는 것은 뒷부분입니다. 원장님이 둘 중 무엇을 볼지 고를 수 있게 됩니다 |
| **꼬리 자는 길이 절반** | `H_SPECS[].halfK = 0.5` (원장님 지시). 아우터를 가운데 두는 것은 그대로 |
| **꼬리 자 바깥 회색 (v1.53.0)** | 색 토막의 바깥(관자놀이 쪽)으로 **자 길이만큼 얇은 회색 참조선** — 원장님 지시 「꼬리 선 밖으로 얇은 회색선 더 빼주고」 |
| ⛔ 밸런스 빨간 토막 | **나누지 않습니다.** 판정 표시가 가려지면 안 됩니다 |
| `VOUT` | 원장님이 3단계(0.065) 선택. 1단계 0.030 / 2단계 0.045 는 「너무 짧다」로 반려 |

회귀 106 이 이 전부를 잠급니다.

### 1-16. 자의 정렬 규칙 (v1.24.0)

> 자는 **짝끼리 길이와 끝이 같아야** 두께를 눈으로 잽니다. 그리고 **각 자는 실제로 재는 부위 위에** 있어야 합니다.

| 자 | 기둥 (anchor) | 규칙 |
| --- | --- | --- |
| **앞머리 ↔ 앞두께** | 이너 `v2` | **완전히 동일.** 이너 선을 가운데 두고 |
| **아치 ↔ 아치두께** | **아치선 `v6`** | **완전히 동일.** 눈썹 **산** 위 (v1.32.0) |
| **꼬리** | 아우터 `v4` | 제일 바깥. **얇은 실선 없음** |
| **눈** | 없음 | 좌우 관통. `#3A3F4A` · **opacity 0.5** · 얇은 실선 없음 |

- **아치 자를 상수 위치로 되돌리지 마세요.** v1.23.0 에서는 앞머리 옆에 뒀다가 **눈썹 산이 아니라
  앞머리를 재고 있어서**, v1.31.x 에서는 아우터에 묶여 있어서 — 두 번 다 원장님이 잡아내셨습니다.
  이제 아치 자는 **자기 세로선(아치선)** 을 따라갑니다 (1-14).
- **눈 기준선은 늘 켜져 있으므로 조용해야 합니다.** 원색 빨강(`#FF3B4E`)이면 화면을 가로질러
  시야를 방해합니다. 짙은 회색 + 반투명이라 흰 헤일로 덕에 어떤 피부톤에서도 보이면서도 눈에 안 띕니다.
- **꼬리와 눈에는 얇은 실선을 그리지 않습니다** — 꼬리는 구간 밖이라 실선이 엉뚱하게 길어지고,
  눈은 이미 좌우를 관통합니다. `renderGuides()` 의 `sp.key !== "h1" && sp.key !== "h3"` 조건.
- 회귀 73번이 **짝끼리 `segPx()` 가 완전히 같은지**, 아치가 앞머리보다 바깥인지, 꼬리가 제일 바깥인지,
  꼬리·눈에 얇은 실선이 없는지, 눈 색·투명도를 검사합니다.

> ⚠️ **테스트에서 선 좌표를 하드코딩하지 마세요.** 자의 길이는 원장님 피드백으로 자주 바뀝니다.
> `tapAt()` 처럼 **실제로 그려진 토막의 한가운데**를 찾아서 찍어야 합니다.
> v1.24.0 에서 아치를 옮겼을 때 하드코딩된 좌표 때문에 54·55·56 번이 한꺼번에 깨졌습니다.

### 1-17. 얼굴 기준틀 — 프리셋을 고객 얼굴에 맞춘다 (v1.25.0)

> 프리셋을 **화면 좌표 그대로** 적용하면 얼굴 폭·눈썹 높이가 다른 고객에게 전혀 안 맞습니다.
> 선이 엉뚱한 자리로 가서 "얼굴이 작아진 것처럼" 보입니다. (사진은 그대로입니다 — 선만 틀어진 것)

```js
faceFrame(g) → {
  cx: g.v1,                              // 중심축
  ux: |g.v2 − g.v1|,                     // 가로 단위 = 얼굴이 넓다/좁다
  ey: g.h1,                              // 눈 기준선
  uy: |g.h1 − g.h2|,                     // 세로 단위 = 눈과 눈썹 사이가 멀다/가깝다
}
```

| 규칙 | 내용 |
| --- | --- |
| 저장 | `savePreset()` 이 `frame: faceFrame(S.g)` 을 **같이 저장**합니다 |
| 불러오기 | 세로선은 `cx + (값−cx) × (ux_지금/ux_저장)`, 가로선은 `ey + (값−ey) × (uy_지금/uy_저장)` |
| 대칭 | 환산 후 **`v3 = 2v1 − v2`, `v5 = 2v1 − v4` 로 다시 세웁니다** (1-2) |
| 옛 프리셋 | `frame` 이 없으면 **환산 없이 그대로** 적용 — 기존 저장분이 깨지면 안 됩니다 |
| 내장 프리셋 | `BUILTIN_FRAME()` = `faceFrame(DEFAULT_GUIDE)` |

- **가로·세로 단위를 하나로 합치지 마세요.** 얼굴은 넓은데 눈썹이 낮은 사람이 있습니다.
  두 축을 따로 환산해야 그 사람에게 맞습니다.
- `ux`·`uy` 는 0 이 되지 않게 `Math.max(…, 0.02)` 로 막습니다 (선이 겹쳤을 때 무한대 방지).
- 회귀 74·75·76 번이 넓은/좁은 얼굴 환산, 적용중 표시, 옛 프리셋 하위호환을 검사합니다.

**프리셋 적용 중 표시**

`S.activePreset` 에 id 를 넣고 `프리셋` 버튼에 `.preset-on` 을 붙입니다.
초기화·새 사진에서 `null` 로 되돌립니다.

**줌 상한 (v1.25.0)**

`ZOOM_MAX` 를 8 → **20** 으로 올렸습니다. 작게 찍힌 사진도 **목표 눈 크기(`EYE_FRAC` 44%)까지
확대**되어야 매번 비슷한 자리에 얼굴이 옵니다. 8 로 되돌리면 작은 사진에서 얼굴이 작게 남습니다.

### 1-20. 가이드 플로우 · 선 색 규칙 (v1.42.0 — 원장님 지시 2026-08-21)

**선 색 (v1.46.0 — 원장님 지시 「회색이라 내 드로잉과 겹쳐 잘 안 보인다」)** —
회색 기본(`GREY_LINE`)은 **폐지**. 모든 선은 **항상 자기 고유색**으로 그린다:
강조(가이드 차례 `S.guideCur` 또는 선택)가 아니면 **연하게**(`dimOp` = op×0.6, 최소 0.4),
강조면 **불투명 + 굵기 +1.6**. 색만 봐도 어떤 선인지 알 수 있어 가이드를 모르는
사용자도 레일 버튼·라벨 색과 바로 짝지을 수 있다. **흰색 헤일로는 없습니다** (v1.42.1
원장님 지시 — 되살리면 선이 두꺼워 보여 정교함이 죽습니다).
**이너 묶음(이너·앞머리·앞두께)은 v1.48.0에서 두 색으로 나뉩니다 — 강조 = 민트 `#5EEAD4`
(레일 버튼 띠와 같은 색) / 연한 = 연회색 `#C9D1D6` (알파 없이 그대로).**

> ⚠️ 원장님 지적(2026-08-22): 「이너 색은 눈이 조금 아프면서 톤다운된 이상한 색이다」
> 원인이 **두 개**였고, 색 이름만 바꾸는 것으로는 네 번(검정→앰버 v1.46.0→민트 v1.46.1→
> 딥 틸 v1.46.3) 모두 해결되지 않았습니다.
>
> ① **밝기 대비가 없었다.** 딥 틸(#0D9488)은 원장님 사진에서 피부와 밝기 대비가 **1.1~1.5:1**.
>    색상은 174° vs 피부 18°(거의 보색), 명도는 58% vs 60%(거의 같음), 채도 91%.
>    **같은 밝기 + 보색 + 고채도** = 눈이 초점을 못 잡고 진동합니다. 확대할수록 심해지고,
>    원장님은 시술 중 늘 확대해서 보십니다.
> ② **연한 상태를 알파로 만들었다.** op×0.5 로 흐리게 하면 그 색이 피부와 섞여,
>    화면에 실제로 나오는 색은 `#617F6A`(탁한 이끼색)였습니다 — 고른 색이 그대로 안 나옵니다.

| 규칙 | 내용 |
| --- | --- |
| 강조 색 | `#5EEAD4` — **레일 버튼 띠(dot)와 같은 값**. 세로선 배지를 지운 지금(v1.46.2) 색이 곧 이름표이므로, 버튼 띠와 선 색이 같아야 눈으로 바로 짝지어집니다 |
| 연한 색 | `#C9D1D6` — 스펙의 **`dimColor`**. 원장님 선택(2026-08-22, 세 밝기 중 「연회색 중간」) |
| **알파 금지** | `dimColor` 가 있는 스펙은 **불투명하게** 그립니다 (`dimOpOf()` = 1). ⛔ 알파로 되돌리면 ②의 탁한 색이 그대로 돌아옵니다 |
| **적용 범위** | v1.48.0 은 이너 묶음만이었고, **v1.49.0 에서 아치·꼬리까지 넓혔습니다** (아래 표). 눈·센터는 중성선이라 종전대로. ⚠️ 연한 색을 **전부 같은 회색**으로 만들지 마세요 — 각 묶음의 연한 색은 자기 계열이어야 「색이 곧 이름표」가 유지됩니다 |
| 한계 | 밝은 색은 **밝은 피부에서 대비가 약해집니다**(민트 기준 1.1:1). 흰 피부 고객에게서 같은 불만이 나오면, 색을 또 바꾸지 말고 **얇은 이중선**(선 아래 어두운 테두리)을 검토하세요 — 색 하나로는 모든 피부톤을 못 이깁니다 |

연한(비강조) 상태 투명도는 `dimColor` 가 없는 스펙에만 적용됩니다: op×0.5(최소 0.4) — v1.46.3에서 0.6→0.5로 낮춤.

**v1.49.0 — 아치·꼬리도 같은 처방으로 (원장님 지시 2026-08-22)**

> 원장님 지적: 「이너라인의 색이 아주 잘 보인다… 그러니 지금 아치 라인의 파랑색이 톤다운된
> 느낌이라 눈이 더 아파진다. 이너라인이 밝아졌는데 비교하여 아치 파랑라인이 죽어있는 느낌」
>
> **원인은 강조 색이 아니라 연한 상태였습니다.** 알파 0.475 로 흐리게 하니 화면에 실제로 나오는 색이
>
> | | 화면 실제색 | 휘도 |
> | --- | --- | --- |
> | 피부 | — | 0.199 |
> | 연한 아치 `#2E8BFF`×0.475 | `#6D7CA4` | **0.203** |
> | 연한 보라 `#A855F7`×0.475 | `#A762A0` | **0.195** |
>
> 피부와 밝기가 **거의 같아 사실상 보이지 않는 상태**였습니다. 이너만 알파를 걷어낸 뒤로
> 그 대비가 드러난 것입니다.

| 선 | 강조 (그대로) | 연한 (`dimColor`) |
| --- | --- | --- |
| 아치 · 아치두께 · 아치선 | `#2E8BFF` | **`#A9CFF2`** (휘도 0.60) |
| 꼬리 · 아우터 | `#A855F7` | **`#D0B8F0`** (휘도 0.54) |
| 이너 · 앞머리 · 앞두께 | `#5EEAD4` | `#C9D1D6` (휘도 0.63) |

> ⛔ **고유색(강조 색)은 바꾸지 마세요.** 원장님이 유지를 원하셨습니다 — 파스텔로 올린 시안
> (아치 `#7DD3FC` · 보라 `#D8B4FE`)은 「둘 다 별로, 고유색 유지하고 싶다」로 반려됐습니다.
> 세 연한 색의 휘도를 0.54~0.63 으로 나란히 맞춰 어느 하나만 튀지 않게 했습니다. 회귀 104.
**기본(비강조) 선 굵기 = 스펙 w + 0.6, 강조 = w + 1.8** (v1.47.1 원장님 지시 「선택시
굵어지는 굵기보단 얇지만 지금 기본 굵기보다는 조금 더 굵게」) — 강조를 1.6→1.8로 올려
기본이 굵어져도 차이(1.2)가 눈에 남는다 (회귀 51: 차이 >1).
센터(v1)만 중성색(#14161B) 유지.
**세로선 이름 배지(캔버스 위 이너·센터·아치선·아우터 태그)는 전부 숨김** (v1.46.2 원장님
지시 「세로선들 이름 배지 모두 숨김」) — 선이 항상 고유색이라 색이 곧 이름표. 회귀 46 = 0개.
⚠️ 옅은 연결선(HAIR)은 이너 색을 따라가지 않도록 #14161B 하드코딩.
밸런스 빨강은 그대로 우선합니다. 레일 버튼 색 줄무늬는 스펙 `dot`을 자동 반영.

**가이드** — 왼쪽 아래 `가이드` 버튼(프리셋 옆, `btnGuide`).
**v1.47.0 (원장님 지시 「가이드는 앱이 켜지면 항상 시작 상태로 유지, 사용자가 클릭할 때만
꺼짐」)** — 사진이 올라와 편집이 시작되면 가이드는 **저절로 켜져 이너부터** 시작한다.
끄는 방법은 가이드 버튼 클릭뿐(초기화·플로우 종료로는 안 꺼짐). 상시 ON이 되면서
가이드 중에도 **선택한 선은 켜진다** — 플로우 밖 선(센터·아치선·아우터)을 골라도
표시가 나오게 (회귀 51). 플로우 중엔 움직인 선이 곧 선택이라 사실상 하나만 켜져 보인다.

| 규칙 | 내용 |
| --- | --- |
| 순서 | `GUIDE_FLOW` = 이너 → 앞두께 → 앞머리 → 아치두께 → 아치 → 꼬리 → **아치 세로선 → 아우터** (v1.47.2 원장님 지시) |
| 시작 | 켜는 순간 **이너부터** 굵게 켜진다 (v1.44.0 원장님 지시 — 처음엔 "첫 터치 선부터"였으나 변경) |
| 진행 | 움직임이 끝나면(드래그 뗌 · 슬라이더 change) **그 선의 다음** 순서가 켜지고, **이전 단계 선은 표시가 꺼진다** — 가이드 중에는 지금 차례 하나만 켠다 (v1.44.0) |
| 자유 이동 | 어떤 선이든 순서와 무관하게 움직일 수 있다 — 움직이는 동안 그 선이 켜지고, 끝나면 그 선의 다음 |
| 종료 | 아우터(마지막) 뒤로는 **처음으로 돌아가지 않는다.** 가이드를 끄면 즉시 종료 |
| **밝은 선은 언제나 하나 (v1.49.0)** | 원장님 지시(2026-08-22): 「오직 하나의 플로우에 하나의 색만 밝고 짙어진다」. ⚠️ v1.47.0 이 강조 조건에 `isSelected` 를 OR 로 더하면서 **다음 차례로 넘어가도 방금 쓴 선이 선택으로 남아 둘이 함께 밝았습니다**(v1.48.0 에서 실측 확인). `guideAdvance()` 가 다음 선으로 **선택까지 함께 옮기고**(`noteSel`), `noteSel()` 은 가이드 중 고른 선을 곧 지금 차례로 만듭니다. 덤으로 **조절 바가 곧바로 다음 선을 잡아** 레일에서 다시 고를 필요가 없습니다. 회귀 103 |
| 플로우 밖 선 | 센터·V 피봇 등을 고르면 **추천을 잠시 내리고**(`guideCur = null`) 그 선만 밝습니다. 플로우 안의 선을 다시 고르면 **그 선부터 재개**합니다 |

회귀 101이 이 흐름 전체를 검사합니다.

### 1-19. 실제 시술 순서 — 이 앱이 따라가야 하는 흐름 (v1.30.0 · 읽는 법은 v1.31.0)

> ⚠️ **이 순서가 앱의 존재 이유입니다. 코드를 고칠 때 이 흐름을 깨지 마세요.**

**🪞 데칼코마니 원칙 (v1.38.0 — 원장님 설명 2026-08-21, 전문 보존)**

> 「이 앱은 데칼코마니를 하는, 왼쪽을 먼저 사용자가 그리고 오른쪽에 그와 같은 기준의
> 드로잉을 그릴수있도록 도와주는것이다. …선들의 기준점은 오른쪽왼쪽 드로잉의 **평균
> 밸런스가 아닌 단 한쪽에만 정확히 선을 맞추고 반대쪽의 밸런스만 교정하는 방식**이다」

1. 원장님이 **기준쪽(보통 왼쪽)** 에 디자인을 그리고, 반대쪽엔 점 5개만 찍는다
2. 앱은 5개의 선(앞머리·앞두께·아치·아치두께·꼬리)을 **기준쪽에 정확히** 올린다
3. 센터는 양쪽 내안각 중점(자동 밸런스) — 이 축의 **거울상**으로 반대쪽 기준이 저절로 생긴다
4. 반대쪽 드로잉을 그리며 선과의 **갭을 서서히 줄여** 대칭을 만든다
5. 양쪽이 다 그려지면 `밸런스` — 기준쪽 대비 갭이 큰 반대쪽 선만 빨갛게

**따라서 선 배치에 좌·우 평균을 쓰면 안 됩니다.** v1.37.0 까지 랜드마크 배치가 좌우
평균이라 기준쪽조차 어긋난 채 양쪽이 반씩 뜨는 배치가 나왔습니다 (원장님 채점 6점).
센터(v1)·눈(h1)만 양쪽에서 잡습니다 — 대칭 축 자체이기 때문입니다.
`refIsLeft()` · `refOfPair()` · 회귀 70 이 이 원칙을 지킵니다.

**기준쪽 선택 규칙** — 밸런스의 `왼쪽`/`오른쪽` 버튼이 기준쪽(`S.refSide`)입니다.
선택쪽에 굵은 드로잉이 있으면 그대로(일반 적용). 선택과 **반대쪽이 확실히 더 짙으면**
사진의 굵은 선을 따른다 — 「선택사항보다, 사진에 굵은선으로 선택한다」 (회귀 98).

1. 원장님이 **왼쪽 눈썹에 먼저 굵은 드로잉**을 그린다
2. 오른쪽에 그리기 전 **포인트를 먼저 찍는다**
3. **자(가로선)는 왼쪽 드로잉의 짙은 선에 맞춰 그 위에 올라간다**
4. 오른쪽은 그 자와 눈썹 사이 **갭**을 보며 포인트를 맞춘다
5. 양쪽이 완성되면 `밸런스` 로 어긋난 곳을 확인한다

| 규칙 | 내용 |
| --- | --- |
| **시작 배치 = 초기화와 동일한 랜드마크 배치** (v1.34.0) | 사진을 불러오면 **AI 얼굴 정렬까지만** 합니다. ⚠️ v1.30.0~v1.33.0 은 여기서 `autoFromDrawing()` 까지 자동으로 돌렸는데, 실제 고객 사진에서 판독이 어긋나면 **선이 엉뚱하게 벌어진 채 시작**됐습니다. 원장님 판정(2026-08-21): 「초기화 눌렀을때 올라온 선들이 맞다. 이것을 내가 사진을 입력하는 순간부터 적용하고싶다」. 자동 호출을 되살리면 회귀 96번이 깨집니다 |
| **드로잉 판독은 `드로잉 맞춤` 버튼에서만** | 3번 단계(자를 드로잉 위에 올리기)는 원장님이 드로잉을 그린 **뒤** 버튼으로 직접 실행합니다. 판독이 어긋나도 되돌리기 한 번이면 됩니다 |
| **판독 기준쪽 = 밸런스에서 고른 쪽** (v1.37.0) | 원장님 지시(2026-08-21): 「사용자가 밸런스 왼쪽을 선택한 상황을 시스템상 알수있다. 왼쪽을 선택한상황에서 드로잉이 오른쪽이 더 짙을경우 오토로 오른쪽에 맞춘다. 그 이외에 상황을 제외하고 기본에 맞춤한다」. 양쪽을 다 읽고 기본은 `S.refSide` 쪽 — 반대쪽 잉크가 **`DRAW_SIDE_SWITCH`(1.5)배 이상** 짙을 때만 반대쪽으로 자동 전환. ⚠️ 문턱을 낮추면 조명 좌우 차이만으로 기준이 뒤집힙니다. 회귀 98 |
| **앞머리 구간은 분위수** (v1.37.0) | 앞머리는 털이 성글어 중앙값이 빗나갑니다 — 윗선은 위 30% 지점, 아랫선은 아래 70% 지점. 아치·꼬리는 표본이 적어 **중앙값 유지** (분위수로 바꾸면 회귀 89가 깨집니다) |
| 자동으로 올리는 것 | 앞머리 · 앞두께 · 아치 · 아치두께 · 꼬리 · **이너 · 아우터**. 센터(v1)는 얼굴 중심축이라 건드리지 않습니다 (1-15) |
| **V 피봇 · V 앵글은 자동 배치 금지** | 쓰실 때만 켜는 보조선입니다. 자동으로 올리면 화면이 시끄러워집니다 |
| 못 읽으면 | **조용히 얼굴 기준 배치 그대로** 둡니다. 토스트로 시끄럽게 알리지 마세요 (1-18 과 같은 원칙) |
| 다시 맞추기 | 위쪽 `드로잉 맞춤` 버튼 — 드로잉을 더 그리거나 지운 뒤 다시 올립니다 |
| **선은 전부 켜져 있어야 합니다** (v1.30.1) | ⚠️ v1.30.0 까지 앞머리·앞두께·아치·아치두께·꼬리·아우터의 `DEFAULT_GUIDE` 표시가 `false` 라, **자리는 잡아 놓고도 화면에는 안 보였습니다**(원장님 지적).<br>`baseStructureVisible`(V 피봇·V 앵글)과 `eyeGuideVisible` 만 꺼 둡니다 — 그 둘은 사용자 선택입니다 |
| 프리셋의 역할 | **원장님이 직접 저장한 것만** 씁니다. ⚠️ v1.51.0 에서 **내장 기본 3종(자연·강한·아치형)을 없앴습니다** — 원장님 판단(2026-08-22): 「자동 맞춤이 매번 다른 고객 드로잉마다 되기 때문에 프리셋 일자 아치 이런 게 의미없어 보인다」. 드로잉 맞춤이 그 고객의 실제 잉크를 읽으므로 고정 비율은 근사치일 뿐입니다. 프리셋 기능(저장·로드·즐겨찾기·내보내기)은 그대로 두었습니다. 회귀 107 |

**드로잉을 읽는 법 (`readDrawing`) — v1.31.0 에서 통째로 바뀜**

> ⚠️ **v1.30.x 의 실패를 반복하지 마세요.**
> 그때는 x 를 `browX(frac)` 으로 뽑았습니다. `browX()` 는 **이너·아우터가 이미 맞아야**
> 눈썹 위를 지나가므로, 처음 한 번 어긋나면 계속 **눈꺼풀**을 읽었습니다.
> 원장님 지적(2026-08-20): 「기본 라인이 이렇게 올라옴. 전혀 프로페셔널하지 못한 설정이다.
> 눈썹의 드로잉을 보고 선들을 올려줄래?」
> → **`autoFromDrawing()` 안에서 `browX()` 를 다시 쓰면 같은 버그가 돌아옵니다.**

순서를 뒤집었습니다. **사진에서 눈썹을 먼저 찾고, 찾은 모양에서 각 선을 뽑습니다.**

```js
browBoxes()        // MediaPipe 눈썹 윤곽(위 70·63·105·66·107 / 아래 46·53·52·65·55)
                   //   → 지금 화면 좌표의 탐색 상자. 아래쪽은 동공까지 여유를 남겨 눈꺼풀을 안 본다
fallbackBox(side)  // 얼굴 인식이 실패했을 때만 — 기준 쪽 절반의 눈 기준선 위쪽
inkRunAt(img,x,…)  // 한 열에서 **잉크량(어두운 정도 × 두께)** 이 가장 많은 덩어리
keepBand(pts)      // 중앙값에서 크게 벗어난 열(머리카락·안경테)을 버리고 이웃 3개 중앙값으로 다듬기
```

| 상수 | 값 | 뜻 |
| --- | --- | --- |
| `DRAW_COLS` | 56 | 눈썹 하나를 훑는 세로 열 개수 |
| `DRAW_CONTRAST` | 18 | 피부보다 이만큼 어두워야 "그린 선" |
| `DRAW_MIN_HITS` | 10 | 이보다 적게 찾으면 **조용히 실패** (얼굴 기준 배치 유지) |
| `DRAW_PAD_X` | 0.16 | 좌우 여유 — 드로잉은 눈썹 털보다 길게 그립니다 |
| `DRAW_PAD_UP` / `DRAW_PAD_DN` | 0.95 / 0.70 | 위/아래 여유 (눈썹 높이 비율) |
| **`DRAW_EYE_GAP`** | **0.55** | 동공까지 이만큼은 남긴다 — ⚠️ **눈꺼풀을 읽지 않기 위한 방어선. 줄이지 마세요** |
| `DRAW_MAX_FILL` | 0.72 | 열의 이만큼을 넘게 어두우면 머리카락·그림자로 보고 버림 |
| `DRAW_PAIR_INK` | 0.5 | 짝이 되려면 씨앗 잉크의 이만큼은 되어야 한다 |
| `DRAW_PAIR_FILL` | 0.7 | 두 줄의 두께 합이 전체 폭의 이만큼 이하 = **사이가 비어 있다** |
| `DRAW_OUTLINE_RATIO` | 0.5 | 이 비율 이상의 열이 짝을 이뤄야 "테두리로 그린 드로잉" |
| `DRAW_THICK_MAX` / `MIN` | 1.9 / 0.3 | 읽어낸 두께가 랜드마크 눈썹 높이의 이 범위를 벗어나면 **버린다** |
| **`DRAW_CONTRAST_SOFT`** | **9** | **2차 패스** (v1.33.0) — **맨 눈썹**은 털·피부 대비가 약해 1차(18)가 놓친다. ⚠️ 반드시 1차 **실패 후에만** — 순서를 바꾸면 그림자를 눈썹으로 잡는다 |

**찾은 모양에서 각 선을 뽑는 법** — 안쪽(코 방향)을 `t=0`, 바깥(꼬리)을 `t=1` 로 둡니다.

| 선 | 어디서 | 왜 |
| --- | --- | --- |
| **앞머리** | `t 0 ~ 0.18` 의 **윗선** 중앙값 | 눈썹 머리의 시작 높이 |
| **앞두께** | 같은 구간의 **아랫선** | 머리 두께는 고정 오프셋이 아니라 **실측** |
| **아치** | 윗선이 **제일 높은 곳**(±8%) | ⚠️ 위치를 미리 정하지 않고 **사진에서 찾습니다.** 사람마다 산의 위치가 다릅니다 |
| **아치두께** | 그 자리의 아랫선 | |
| **꼬리** | `t 0.86 ~ 1` 의 윗선 | |
| **이너 · 아우터** | 드로잉이 실제로 있는 **x 양 끝** | `setLine` 이 반대쪽을 대칭으로 맞춥니다 (1-2) |
| 센터 · V 피봇 · V 앵글 | **건드리지 않음** | 센터는 얼굴 축(1-15), V 는 사용자 선택 |

- 평균이 아니라 **중앙값**을 쓰는 이유: 점·머리카락 한 올에 결과가 끌려가지 않게 (1-18 과 같은 이유).
**속을 채워 그리든 테두리만 그리든 (v1.31.2)**

> ⚠️ **v1.31.1 의 실패를 반복하지 마세요.**
> 그때는 한 열 안에서 가까운 덩어리를 **그냥 이어 붙였습니다.** 그 결과 맨 눈썹 사진에서
> **쌍꺼풀 선·눈꺼풀 주름이 눈썹에 딸려 붙어**, 앞두께가 눈썹 아래 피부까지 내려갔습니다
> (원장님 스크린샷 2026-08-20). **열마다 따로 합치면 안 됩니다.**

이제 **사진 전체를 보고 한 번만** 판정합니다. "테두리로 그린 드로잉"이 되려면

1. 한 열에 **잉크가 비슷한 두 줄**이 있고 (테두리는 위·아래를 같은 힘으로 그립니다)
2. 그 **사이가 비어 있고** (꽉 채워 그린 드로잉은 여기서 걸러집니다)
3. 이런 열이 **과반**이어야 합니다

눈썹 털은 한 덩어리라 1을 못 넘고, 눈꺼풀 주름은 잉크가 훨씬 옅어 짝이 되지 못합니다.

**두께 상식 검사 (v1.31.2)** — 읽어낸 두께가 랜드마크 눈썹 높이의 `0.3 ~ 1.9` 배를 벗어나면
잘못 읽은 것으로 보고 그 패스를 버립니다. 억지로 올리면 원장님이 다시 다 옮기셔야 합니다.

**2패스 판독 (v1.33.0)** — ① `DRAW_CONTRAST`(18)로 그린 드로잉을 찾고, 실패하면
② `DRAW_CONTRAST_SOFT`(9)로 **맨 눈썹의 털**을 읽습니다. 원장님 스크린샷(2026-08-20)에서
맨 눈썹 사진이 1차에서 떨어져 랜드마크 배치로 남았고, 그 배치가 「전혀 프로페셔널하지
못한」 위치였습니다. 두 패스 모두 실패했을 때만 랜드마크 배치가 남습니다.
회귀 94번의 옅은 눈썹 색(#e0cab5)은 일부러 1차 문턱 아래로 맞춘 것입니다 — 진하게 바꾸면
2차 패스가 죽어도 테스트가 모릅니다.

**회귀로 잠근 것 — 「드로잉이 바뀌어도, 고객 얼굴이 바뀌어도」 (원장님, 2026-08-20)**

| # | 무엇이 바뀌어도 | 검사 |
| --- | --- | --- |
| 87 | 기본 | 얼굴 인식 성공 · 아치가 바깥쪽인 눈썹 |
| 88 | **얼굴 인식 실패** | 랜드마크 없이도 같은 결과 |
| 89 | **테두리만 그린 드로잉** | 두께를 제대로 잡는다 |
| 90 | **사진 확대·이동** | 같은 드로잉 위에 그대로 붙는다 |
| 91 | **드로잉 모양이 다름**(아치가 안쪽) | 선이 그 모양을 따라간다 |
| 92 | **눈썹 아래 쌍꺼풀 선** | 앞두께가 주름에 끌려가지 않는다 (원장님 스크린샷의 실제 문제) |

⚠️ 네모나 한 가지 모양으로만 검사하면 "아치를 사진에서 찾는다"가 그냥 통과됩니다 —
실제로 v1.30.0 이 그렇게 통과했습니다. **모양을 바꾼 91번을 지우지 마세요.**

### 1-18. 밸런스 판정 (v1.26.0) — 이 앱의 최종 목표

> **가이드 선은 대칭이라 늘 맞습니다.** 그래서 비교 대상은 선이 아니라
> **사진에 실제로 그려진 드로잉**입니다. 기준 쪽에 그린 자리를 기준으로,
> 반대쪽이 같은 높이에 있는지 봅니다.

**동작**

```js
photoPixels()          // 지금 화면 그대로의 사진을 오프스크린 캔버스에 그려 픽셀을 읽는다
measureSegY(img, seg, y0, band)
                       // 그 토막의 x 를 21곳 표본 → 각 x 에서 가장 어두운 y → **중앙값**
runBalance()           // 모든 가로선을 **한 번에** 검사 → S.balance = { off, skipped }
```

| 규칙 | 내용 |
| --- | --- |
| 기준 쪽 | `S.refSide` = `"L"` / `"R"`. **`localStorage` 에 남아 다음에도 유지**되고, 언제든 바꿀 수 있습니다 |
| 표시 | **기준 반대쪽 토막만** 빨갛게(`#FF3B4E`). 기준 쪽은 정답이므로 건드리지 않습니다 |
| 검사 순서 | **전부 동시에.** 하나씩 넘기면 "오른쪽 눈썹 전체가 올라갔다"와 "꼬리만 처졌다"를 구분 못 합니다 |
| 못 읽으면 | **조용히 건너뜁니다** (`skipped`). 억지로 빨갛게 칠하면 잘못된 시술로 이어집니다 |
| 낡은 결과 | `commitEdit()` 이 `S.balance = null` — **선을 건드리면 측정값이 무효**입니다 |
| 눈 기준선 | 좌우 관통이라 토막이 하나뿐이므로 검사 대상에서 뺍니다 |

**튜닝 상수 — 함부로 낮추지 마세요**

| 상수 | 값 | 뜻 |
| --- | --- | --- |
| `BAL_BAND` | 0.045 | 선 위아래로 훑는 범위(캔버스 높이 비율). 넓히면 엉뚱한 것을 잡습니다 |
| `BAL_SAMPLES` | 21 | 한 토막의 x 표본 수 |
| **`BAL_TOL_MM`** | **0.8 mm** | 이보다 작은 차이는 **맞다고 본다.** ⚠️ **원장님이 실제 시술 감각으로 정하신 값**입니다 (2026-08-20) — 임의로 낮추지 마세요 |
| `INNER_MM` | 32 mm | 양쪽 내안각 사이 평균 거리. mm ↔ px 환산의 자 |
| `BAL_CONTRAST` | 14 | 이만큼도 안 어두우면 "선 못 찾음". 낮추면 그림자를 선으로 오인합니다 |

- 평균이 아니라 **중앙값**을 쓰는 이유: 점·머리카락 한 올에 결과가 끌려가지 않게 하려고.
- 표본의 절반도 못 찾으면 그 토막은 실패로 처리합니다.

**허용 오차는 화면 px 이 아니라 얼굴 크기 기준 (v1.29.0) — px 고정으로 되돌리지 마세요**

> ⛔ v1.28.1 까지 `BAL_TOL = 1px` **고정**이었습니다. 이러면 **사진을 확대할수록 앱이 더 예민해집니다.**
> 눈썹 하나를 화면 가득 확대해 보실 때(시술 중 늘 그렇게 하십니다) 1px 이 나타내는 실제 거리가
> 0.05mm 수준까지 줄어들어, **원장님이 보시기에 완벽히 맞는 눈썹도 빨갛게** 칠했습니다.
> 원장님 지적으로 잡았습니다 — "이 정도는 밸런스가 맞다고 봐야 한다" (2026-08-20).

```js
balTolPx() = clamp(BAL_TOL_MM × |v3 − v2| × W / INNER_MM, 2, 16)
             // |v3 − v2| = 화면에서 잰 양쪽 내안각 사이 거리(정규화)
```

| 규칙 | 내용 |
| --- | --- |
| 왜 이너 선인가 | 화면에서 **확실히 얻을 수 있는 얼굴 치수**입니다. 확대하면 이너 간격도 같이 커지므로 허용 오차가 배율을 따라갑니다 |
| 클램프 | 이너가 겹치거나(0에 가까움) 극단적으로 벌어졌을 때를 막는 안전선. **빼지 마세요** — 0 이 되면 모든 차이가 어긋남으로 나옵니다 |
| 자동 정렬을 안 썼다면 | 손으로 맞춘 이너 선을 그대로 씁니다. 근사치지만 **px 고정보다는 언제나 낫습니다** |
| 회귀 86번 | 같은 사진·같은 6px 차이인데 **이너가 좁으면 잡고 넓으면 통과**하는지 검사합니다 |

> ⚠️ **이 기능의 한계 — 반드시 사용자에게 알릴 것**
> 사진을 읽는 방식이라 ① 그린 선이 흐릴 때 ② 좌우 조명이 다를 때(그림자를 선으로 오인)
> ③ 얼굴이 옆으로 돌아가 한쪽이 짧게 찍혔을 때 틀릴 수 있습니다.
> **"빨간 표시가 없다 = 완벽하다"가 아닙니다.** 못 읽어서 건너뛴 것일 수도 있으므로
> HUD 에 건너뛴 개수를 반드시 같이 보여줍니다.

**테스트 방법 (회귀 77~80)**

실제 고객 사진을 쓸 수 없으므로 **답을 아는 합성 사진**을 만듭니다 — 왼쪽·오른쪽 막대의
높이 차이를 정확히 6px 로 넣고 그대로 잡아내는지 봅니다.
**반드시 가로 모드에서** 돌리세요. 세로 폴백은 작업 영역이 좁아 오른쪽 토막이 아예 안 그려집니다
(v1.26.0 개발 중 이것 때문에 테스트가 "통과하는 것처럼" 보였습니다 — 실제로는 건너뛰고 있었음).

### 1-12. 다국어 (i18n) — 버튼 이름을 코드에 박지 마세요 (v1.19.0)

편집 화면 **왼쪽 버튼들 맨 위**에 `한` / `영` 칩이 있습니다 (`#railLang`). **현재 언어만 색이 켜집니다.**

```js
const I18N = { ko: {...}, en: {...} };      // 언어 하나 = 키 묶음 하나
let LANG = localStorage.getItem("pb_lang") || "ko";
const t = (k) => (I18N[LANG] && I18N[LANG][k]) || I18N.ko[k] || k;
```

| 규칙 | 내용 |
| --- | --- |
| 라인 스펙 | `H_SPECS`/`V_SPECS` 는 `label` 이 아니라 **`i18n` 키**를 갖습니다 (`line_eye`, `line_center` …). **`label` 을 되살리지 마세요** |
| 이름을 읽는 곳 | 레일 버튼 · 캔버스 세로선 배지 · 조절자 이름 · HUD — **전부 `labelOf(key)` / `t(sp.i18n)` 하나만** 씁니다 |
| 언어 전환 | `setLang(l)` → `applyI18n()` 이 `[data-i18n]` 을 다시 칠하고 **`.lbtn` 텍스트도 다시 씁니다** |
| 언어 추가 | `I18N` 에 묶음을 하나 더 넣고 `#railLang` 에 `<button data-lang="…">` 을 추가하면 끝입니다. 다른 코드는 손대지 않습니다 |

- 새 UI 문자열은 **반드시 `ko` 와 `en` 양쪽에** 넣으세요. 한쪽만 넣으면 `t()` 가 조용히 한국어로 떨어집니다.
- 회귀 테스트 60번이 `한/영` 전환 시 버튼 이름이 실제로 바뀌는지 검사합니다.

### 1-10. 개인정보 / 보안

- 사진은 **절대 서버로 보내지 않습니다.** 전부 브라우저 안에서 처리.
- 저장소가 **Public** 이므로 API 키·비밀번호·토큰을 코드에 넣지 마세요.
- 원본 `perfect-brow-source/.project-config.json` (TiDB 비밀번호, AWS 키, JWT 시크릿 포함)은
  **절대 이 저장소에 올리지 마세요.**

---

### 1-62. ⭐⭐⭐ 이너 판독 룰 — 40~48 사이 「드로잉이 시작되는 선」 (v1.99.0 · 원장님 지시 2026-08-29)

> 「40이 눈 앞꼬리, 밸런스 기본이 되는 선. 48이 맥시멈 … 보통은 45가 맥시멈, 최대 48까지.
>   40에서 48까지 들어오면서 **드로잉이 시작되는 굵은 혹은 검정색의 라인**을 이너라인으로
>   선택한다. 선택할 수 없을 경우 40에서 45의 중간인 **43** 을 잡아낸다」
> 「이너라인을 선택하는 룰은 **가장 높은 값을 선택하는 게 아니다** … 46은 거의 맨살, 45도
>   맨살 낮은 점수, 44에서 조금 더 높은 점수로 띄는 값이 선택지다. **맨살에서 「이곳에 선이
>   있다」라고 판단되는 점수가 확 띄는 지점**이 이너라인이다」

**증상 (실기기)** — 원장님 드로잉의 정답은 **44** 인데 앱이 **48**(미간 맨살)에 세웠습니다.

**진짜 원인** (컨테이너 재현으로 확정) — `growEnd`(1-36)는 열마다 `columnRuns` 가 스스로
찾은 제일 진한 덩어리의 **진하기만** 봅니다. 눈썹 안쪽에 **눈꺼풀 그늘**이 이어져 있어
눈썹이 끝난 뒤에도 진하기가 중앙값의 30% 대로 남습니다 (문턱 8.5 vs 실제 10~13).
결정적 단서는 **두께** — 그 열들은 78~111px 로 진짜 눈썹(약 40px)의 **2~3배**였습니다.

**고침** — 이너만 **전용 판독**(`innerDecide` · `innerProfile`)이 정합니다.

| 원장님 숫자 | 뜻 | 상수 | `f` = (이너−내안각)/(센터−내안각) |
|---|---|---|---|
| 40 | 눈 앞꼬리(내안각) · **하한** | `INNER_F_LO` | 0.000 |
| 43 | **읽을 수 없을 때의 답** | `INNER_F_MID` | 0.228 |
| 45 | 보통 맥시멈 | `INNER_F_SOFT` | 0.380 |
| 48 | 절대 맥시멈 | `INNER_F_HARD` | 0.608 |

- 숫자는 **화면 눈금이 아니라 얼굴 비율**입니다 — 프레임이 바뀌어도 같은 자리를 가리킵니다.
  자(anchor)는 `S.innerAnchor` = 배치 때의 |내안각 − 센터| (랜드마크가 있으면 실측).
- 잉크는 **두께를 눈썹으로 고정한 창**에서, 그 창의 **제일 어두운 35%(`INNER_CORE`) 평균**으로
  잽니다. 창 전체 평균을 쓰면 얇아진 앞머리가 맨살에 희석됩니다.
- **맨살 기준**은 `f` 0.380~0.858 표본의 **낮은 15% 분위**(골짜기).
  ⛔ 48 **너머**에서 재지 마세요 — 미간·콧대 그늘이 기준을 부풀려 **이너가 42 로 밀립니다**.
- 48 → 40 방향으로 걸어 나오며 `올라온 정도 ≥ 0.12` **그리고** `맨살의 1.28배` 를 두 열 연속
  만족하는 자리를 찾고, **답은 그 바로 안쪽 열**(= 선의 안쪽 경계)입니다.
- 못 읽으면 **43** — 케이스가 3개 이상 쌓이면 **케이스 `f` 중앙값**(`INNER_CASES` · 1-63).

**결과: 기준 사진 앱 44.2 → 원장님 정답 44** (고치기 전 48).

⛔ 이너를 `growEnd` 결과로 되돌리지 마세요. ⛔ `INNER_RISE` 를 0.10 아래로, `INNER_MULT` 를
2.0 위로 옮기지 마세요 (각각 맨살을 선으로 보거나, 진짜 선을 놓칩니다). **회귀 151** 이 잡습니다.

**안내문도 함께 고쳤습니다** — 원장님: 「앱이 자기 안내문에 「콧방울·내안각 선에 맞추세요」는
잘못된 설명, **「드로잉 앞부분에 맞추세요」** 가 맞다」 → `tip_v2` (ko/en) 교체.

### 1-63. 드로잉 케이스 보관함 — 숫자만 저장 (v1.99.0 · 원장님 지시 2026-08-29)

> 「이제 **드로잉 케이스마다 너가 저장**하고, 여러 케이스에서 가장 잘 사용되는 것, 판독을
>   자세히 측정할 수 없을 때 **가장 비슷하다고 생각되는 지점**을 선택하도록 룰을 만들어야 한다」

`app.js` 의 `INNER_CASES` 배열이 보관함입니다.

| # | 눈썹 종류 | 원장님 정답 | 앱(v2.0.1) | `f` | 차이 | 확인 |
|---|---|---|---|---|---|---|
| 1 | 드로잉(굵은 선) | **45.4** | 45.1 | 0.411 | 0.3 ✅ | |
| 2 | 짙은 파우더 반영구 | **47.3** | 48.0 | 0.555 | 0.7 ✅ | |
| 3 | 자연 결 눈썹 | **46.0** | 46.7 | 0.456 | 0.7 ✅ | |
| 4 | 옅은 반영구 | **42.6** | 40.4 | 0.198 | 2.2 ⚠️ | |
| 5 | 옅은 자연 ① | **41.8** | 43.0 | 0.137 | 1.2 ⚠️ | ⭐ |
| 6 | 옅은 자연 ② | **42.4** | 42.2 | 0.183 | 0.2 ✅ | ⭐ |
| 7 | 옅은 자연 ③ | **41.9** | 47.2 | 0.144 | **5.3 ❌** | ⭐ |
| 8 | 옅은 자연 ④ | **45.1** | 45.4 | 0.388 | 0.3 ✅ | ⭐ |

평균 오차 **1.36 눈금** · 8건 중 5건이 1 눈금 이내.
⭐ = 원장님이 **앞꼬리 자리까지 확대해 확인**해 주신 케이스 (2026-08-29 「맞다 이대로 넣어라」).
**자동 인식(1-65) 검증의 정답지**입니다 — 이 4장 8점에서 평균 0.58 · 최대 0.94 눈금.
⚠️ `f` 는 얼굴에 붙은 값이라 앱을 고쳐도 그대로입니다. 앱을 고치면 **app 열만** 다시 잽니다.

**❌ 시도했다가 접은 것 — 「털의 결(texture)」.** 평균 어둡기 대신 창 안 세로 밝기차로 재 봤더니
케이스 1·8 은 계단이 **정답 자리에 정확히** 생겼지만(44→51%, 45→30%), 케이스 7 은 전 구간
50~100% 로 **떨어지지 않았고**(눈꺼풀 주름에도 결이 있습니다) 케이스 4 는 들쭉날쭉했습니다.
= **이미 맞히던 사진에서만 통했습니다.** 넣지 않았습니다. 자세한 표는 `판독-룰.md`.

**⚠️ 2026-08-29 — 표를 전부 다시 쟀습니다.** 그 전 숫자는 제가 **내안각을 잘못 짚어**
최대 3눈금 어긋나 있었습니다. 원장님 확인: **내안각(40) = 위·아래 눈꺼풀이 만나는 코쪽
끝점**(눈물샘 **안쪽** 끝). 눈물샘이 시작하는 자리가 아닙니다. 앱은 MediaPipe **133/362**
를 쓰므로 앱 안에서는 처음부터 맞았고, 틀린 것은 컨테이너에서 손으로 잰 측정이었습니다.

**v1.99.2 에서 고친 것** (원장님 옅은 눈썹 사진 4장 테스트로 드러남)
- 잉크를 재는 넓은 창이 **화면 위로 넘치면 그 열을 버리던 것** → 화면 안으로 **자릅니다**.
  자동 정렬한 가로 화면은 눈썹이 위에 앉아 **모든 열이 버려졌고**, 4장 중 3장이 이너 판독을
  통째로 건너뛰었습니다.
- 밴드를 못 믿을 때 **`growEnd` 로 되돌아가던 것** → 자만 있으면 **언제나** 이너 전용 판독이
  답을 냅니다(못 읽으면 43). 되돌아가면 다시 미간 맨살(46.7·48)에 섭니다.
- 회귀 **152** 가 둘 다 잡습니다.

**남은 과제 — 옅은 눈썹.** 옅은 눈썹은 잉크가 계단처럼 끊기지 않고 눈두덩 그늘로 서서히
이어져서, 「맨살에서 확 띄는 지점」이 프로파일에 **아예 없습니다**(케이스 7: 39→116% 에서
50→33% 까지 한 번도 평평해지지 않음). 문턱을 어디에 두든 한쪽으로 틀립니다.
평균 어둡기가 아니라 **털의 결(texture)** 을 봐야 할 것으로 보입니다.

- **⛔ 사진은 절대 저장하지 않습니다** — 저장소가 **공개**이고 고객 얼굴입니다. **숫자만.**
- **대체값은 언제나 43**(`INNER_F_MID`). 44로 올릴지는 넘버링을 고친 지금 **다음 케이스로
  다시 판단**합니다 (원장님: 「너의 눈앞꼬리 넘버링을 먼저 고도화 해라」).
- 회귀 **151** 이 「사진 필드가 없을 것」·「대체값 43 고정」·「케이스 4건 이상」을 검사합니다.


### 1-64. ⭐⭐⭐ 눈 앞꼬리 = **언제나 40** (v2.0.0 · 원장님 지시 2026-08-29)

> 「눈 앞꼬리를 항상 40으로 잡아야 하는거 아니니?」

**증상.** 그렇지 않았습니다. 자동 정렬이 **동공 간격**을 화면의 44%(`EYE_FRAC`)로 맞추는데,
사람마다 「내안각 ÷ 동공」 비율이 다릅니다. 원장님 사진 8장 실측:

| 내안각÷동공 | 0.537 | 0.542 | 0.576 | 0.579 | 0.645 | 0.676 | 0.682 | 0.701 |
|---|---|---|---|---|---|---|---|---|
| **내안각이 오는 눈금** | 41.3 | 41.2 | 40.5 | 40.4 | 39.0 | 38.3 | 38.2 | 37.7 |

**37.7 ~ 41.3 · 폭 3.6 눈금.** 즉 40~48 룰이 고객마다 통째로 밀렸습니다.
게다가 눈금 표시가 `Math.round(v × 100)` — **화면 좌표 그대로**였습니다.

**고침 — 두 겹으로 막습니다** (원장님 선택: 「둘 다 하기」)

① **자동 정렬의 자를 내안각으로** (`INNER_FRAC = 0.263`)
   `autoAlign` 이 배율을 **내안각 간격**으로 맞추고, 좌우 중심도 **내안각 중점**(얼굴 축)에
   둡니다. 위아래는 그대로 동공 높이 → `CENTER_Y`(v1.97.2에서 맞춘 값)를 건드리지 않습니다.
   0.263 = 반간격 0.1315 × 2 → **1 눈금 = 화면 1%**, 룰의 13.15 와 딱 맞습니다.

② **세로선 눈금을 얼굴 기준으로** (`faceRef()` / `dispV()`)
   ```
   왼쪽 내안각 = 40 · 센터 = 53.15 · 오른쪽 내안각 = 66.3
   눈금 = 40 + (v − 왼쪽내안각) / (센터 − 왼쪽내안각) × 13.15
   ```
   원장님 선택에 따라 **세로선 전부**(이너·아우터·센터·아치선)에 적용합니다.
   랜드마크가 있으면 매번 실측, 없으면 배치 때 저장한 `S.faceRef` 를 씁니다.
   자가 아예 없으면 예전처럼 화면 % 로 돌아갑니다.

**왜 ②가 필요한가 (①만으로는 부족).** 센터(`centerX()`)는 기기 폭·도크 폭에 따라 조금씩
움직이고, `fitBrowsInFrame`(1-40)이 눈썹 꼬리가 잘릴 때 **배율을 낮춥니다**. 그러면 화면
좌표는 또 밀립니다. ②는 얼굴에 붙어 있어 그때도 **40 은 40** 입니다.

⛔ `EYE_FRAC` 기준 정렬로, 또는 `Math.round(g[k] * 100)` 표시로 되돌리지 마세요.
**회귀 153** 이 둘 다 잡습니다 (동공을 좁게·넓게 바꿔도 눈금 40/53/66 고정).

⚠️ 가로선(앞머리·아치·꼬리 높이)은 그대로 화면 % 입니다 — 룰이 붙어 있지 않고,
원장님이 익숙한 숫자를 유지하기 위해서입니다.


### 1-65. ⭐⭐⭐ 눈 앞꼬리 **자동 인식** (v2.0.1 · 원장님 지시 2026-08-29)

> 「넣지마라. **시스템 내부에서 눈금자 이용하여 눈 앞꼬리를 자동으로 인식하라.**
>   그게 AI가 하는 일이다. 나는 너가 똑똑해지라고, 내 경험을 배우라고 지시한 것이다」
>
> (화면에 눈금자를 그려 원장님이 짚게 하는 안은 **반려**되었습니다 — 사람이 짚을 일이 아니라
>  시스템이 스스로 찾아야 하는 일입니다.)

**원장님의 정의를 픽셀로 옮긴 것** (1-64 의 정의):
> **눈꺼풀 틈의 세로 높이는 코 쪽으로 갈수록 줄어 앞꼬리에서 0 이 된다.**

```
① 동공에서 코 쪽으로, 열마다 「피부보다 CANTHUS_DARK(30) 어두운 픽셀 수」 = 틈 높이
② 틈이 가장 넓은 열(눈 한가운데)을 찾고, 거기서 코 쪽으로 걸어간다
③ 높이가 최대의 CANTHUS_AP(0.30) 아래로 CANTHUS_RUN(3) 열 연속 떨어지는 자리 = 앞꼬리
```

| 상수 | 값 | 왜 |
|---|---|---|
| `CANTHUS_BAND` | 16px | 눈 높이 위아래로만 본다 (눈썹·볼을 안 본다) |
| `CANTHUS_DARK` | 30 | 이보다 낮추면 코 그늘까지 눈으로 센다 |
| `CANTHUS_AP` | 0.30 | 틈이 최대의 30% 아래 = 닫힘 |
| `CANTHUS_RUN` | 3 | 점 하나·속눈썹 한 올에 안 끌리게 |

⛔ 「제일 어두운 열의 끝」으로 되돌리지 마세요 — 코 그늘·눈물샘을 눈으로 오인해
앞꼬리를 코 쪽으로 10px 넘게 밀어냅니다 (실측). **회귀 154** 가 잡습니다.

**성적.** 원장님이 확인해 주신 사진 4장 8점에서 **평균 0.58 · 최대 0.94 눈금**.
제가 손으로 짚던 방식은 1.2~3.3 눈금 흔들렸습니다 (1-64).

**쓰이는 자리.** 랜드마크가 있으면 그대로 랜드마크(133/362)를 씁니다 — 이미 정확합니다.
랜드마크가 **없는 길**(얼굴 인식 실패 · 눈 확대 사진)에서 예전에는 앞꼬리를
**비율(`R_INNER` 0.52)로 짐작**했는데, 이제 `alignFromPupils` 끝에서 사진을 직접 읽어
`S.faceRef` · `S.innerAnchor` 를 바로잡습니다. 못 찾으면 조용히 짐작값으로 남습니다.

**⚠️ 아직 남은 것 — 씨앗(동공) 찾기.** 검출기는 **눈 한가운데를 알려 주면** 잘 찾습니다.
앱에서는 랜드마크나 정렬된 동공 위치가 그 씨앗이라 문제없습니다. 그러나 컨테이너처럼
얼굴 인식이 없는 곳에서 **씨앗을 스스로 찾는 것**은 아직 안 됩니다 (어두운 덩어리로 찾으면
아이라인·속눈썹·머리카락에 끌립니다 — 실측 8장 중 1장만 성공). `findPupilsFallback` 의
같은 약점입니다. **다음 과제.**


### 1-66. ⭐⭐⭐ 앞머리 판독 룰 (v2.1.0 · 원장님 지시 2026-08-29)

> 「눈 윗부분에서 올라가면 **피부색이 이어지다가** 어느 한 지점에서 **검은색**으로 보이는
>   지점이 앞머리다」

`frontDecide` — 이너 자리에서 세로로: 눈 위 7% 에서 출발(속눈썹 방어) → 피부가 5줄 이상
이어진 뒤 → 9줄 창에 7줄 어두운 「두꺼운 검은 것」의 첫 줄 = 앞머리. 「검은색」 문턱은
그 열의 피부↔최암부 **중간(상대값만)**. 실패하면 기존 밴드 판독 유지 (조용한 안전판).

| 방어 | 막는 것 |
|---|---|
| 상대 문턱만 | 조명 어두운 사진 (절대값 섞으면 8장 중 6장 실패) |
| 두께 창 9/7 | 쌍꺼풀 주름 · 아이라인 (검지만 얇다) |
| 피부가 먼저 (5줄) | 출발점에 붙은 눈 화장 덩어리 |

케이스 8장 중 5장 성공(전부 아랫선 정확) · 3장 안전 포기. **회귀 155**. 자세한 것은 판독-룰 2-7.

**넘버링 대체값 (v2.1.1)** — 원장님: 「빨간 선은 눈으로부터 올라와 대체값이 필요할 때 사용할
넘버링」. 앞머리 = **눈 위 11.7 눈금**(확정 5장 중앙값 · 이너와 같은 자). 시작 배치의 동공
비율(0.78) 짐작을 이것으로 폐지했고, 판독 실패 시 밴드 값이 7~16 눈금 밖이면 11.7 로
대체합니다. **회귀 156**.

**쌍꺼풀·주름 쉐도우 방어 (v2.1.2)** — 두꺼운 쉐도우는 두께 창을 통과하므로, 열의 후보를
**전부 모은 뒤** 눈 위 7~16 눈금 안의 가장 아래 후보를 고릅니다 (쉐도우 5눈금 vs 눈썹
11눈금 → 눈썹). **회귀 157**. ⚠️ v2.2.3 — 원장님이 동공 십자로 0 오차를 짚어 주셔서 8장 0 을 동공 실측으로
바로잡고 전부 재환산: 정답 4번=**10.2**·7번=**7.3**(구눈금 14·9), 확정 6건 중앙값 → **보통값
9.8 · 하한 6**. 예전 11.6·7 은 손 0 이 낮았던 값 — 되돌리지 마세요. 2번 정답 재요청 중.

**하한 절대 규칙 (v2.1.3)** — 원장님: 「어느 넘버 이하는 앞머리로 측정하지 않는다」.
7 눈금 미만은 후보 자격이 없고, 어떤 경로로 왔든 최종 앞머리가 7 미만이면 `frontFloor()` 가
**보통값 11.7 로 대체**합니다. 상한은 집행 ✗ (확대 사진의 16 초과는 정당 — 모양 C).
합성 픽스처는 h1 을 눈 위치(이미지 y=250)에 맞춰 실제 얼굴과 같은 눈금으로 잽니다. **회귀 158**.

**넘버링 0 동일화 (v2.2.2)** — 원장님: 「0 자리가 사진마다 다 다르다. 동일화 프롬포트 정해라」.
**0 = 동공 중심**: 랜드마크 홍채 실측 → 저장된 동공 높이(`S.eyeZero`) → 최후에 h1.
⛔ h1 을 0 으로 쓰면 원장님이 눈 선을 옮길 때 넘버링 전체가 밀립니다. **회귀 159**.

**앞두께 (v2.3.0 · 원장님 확정)** — 「검은선에서 **피부색이 나오는 지점**」. 짙은 검정이
끝나도 연한 문턱(FT_SOFT 25%)을 넘는 동안 계속, 피부가 4줄 연속이면 끝. v2.2.0 의 「짙은
검정 끝」은 5장 중 4장에서 1~2 눈금 낮았음 (원장님 핑크 표시). 확정 5건(4.8·5.7·7·6·7)
→ 두께 보통값 **6.0** · 범위 3~9 (`ftGuard`). 성적 평균 0.86 눈금. 앞머리 보통값은 케이스 2
최종 확정(**11.0** — 원장님 「12 아니고 11」)을 더해 **10.2**(7건 중앙값 · 4번=10.2, 7번=7.3
확인 사진으로 재확인 완료). **회귀 155**.

### 1-67. ⭐⭐⭐ 이너 맥시멈 45 (v2.4.0 · 원장님 지시 2026-08-29)

> 「**이너 45를 맥시멈으로 설정해라**」

이너의 답은 **40~45** 를 벗어날 수 없습니다 (`INNER_F_SOFT` 클램프). 탐색·맨살 기준은
48 쪽을 계속 씁니다 (48 너머가 맨살 골짜기). 캡 전 실제 시작점은 `fRaw` 로 보존해
**앞머리 훑는 열이 그 자리를 씁니다** — 캡 자리로 열까지 옮기면 케이스 2·3·8 의 앞머리가
밀립니다 (실측으로 확인 후 fRaw 방식으로 고정, 8장 모두 이전과 동일). 예비 경로(밴드
half)의 클램프도 45 기준. **회귀 160** (+ 90 의 이너 기대값에 캡 반영).

### 1-68. ⭐⭐⭐ 앞두께 우선순위 (v2.4.0 · 원장님 지시 2026-08-29)

> 「**두께 보통값 6.0 이 중요한 게 아니다.** ① 반드시 사진에서 픽셀 검증으로 — 검은색에서
> 피부색이 나오는 부분의 **검은 마지막 끝부분**이 앞두께다. ② 명확하지 않을 때는 색상의
> **퍼센트지가 낮아지는 부분**을 선택해야 한다」

① 픽셀 검증(피부 4줄 연속 = 명확 → 피부 직전 검은 끝) → ② 불명확(피부 복귀 없음, 또는
두께가 3~9 밖)이면 어둡기 퍼센트가 **가장 크게 낮아지는 자리**(`FT_P2_MIN` 0.15) →
③ 그래도 밖이면 `ftGuard` 가 보통값 6.0 — **최후 안전판일 뿐**. 확정 8장에서 ② 는
발동하지 않았고 8장 모두 v2.3.1 과 동일 (순수 안전망). **회귀 161**.


### 1-69. ⭐⭐⭐ 아치엣지·아치두께 판독 룰 — **앞머리와 같은 방법** (v2.5.0 · 원장님 지시 2026-08-29)

> 「앞머리, 앞두께 자동 위치조정 프로그래밍과 **같은 방법으로** 아치엣지 아치두께 고도화해라」

앞머리(1-66)·앞두께(1-68)가 쓰던 세로 픽셀 판독을 **공용 함수(`darkBlobsUp`)로 꺼내**
아치가 **같은 함수**를 쓰게 했습니다. ⛔ 규칙을 두 군데서 키우지 마세요 — 한쪽만 고치면
두 판독이 서로 다른 말을 하게 됩니다(명령서 §0-X).

| | 앞머리·앞두께 | **아치엣지·아치두께** |
|---|---|---|
| 훑는 열 | 이너(v2) 자리에서 눈썹 몸통 쪽 5열 | **산꼭대기(밴드 봉우리)** 좌우 5열 (`ARCH_SPAN` ±1.6%) |
| 아랫끝 | 앞머리 | **아치두께** |
| 윗끝 | 앞두께 | **아치엣지** |
| 후보 자격 | 눈 위 7~16 눈금 | 눈 위 **6~26 눈금** · **앞머리보다 위**(해부학) |
| 못 읽으면 | 넘버링 대체값(10.2 / 6.0) | **표준값**(앞머리에서 3칸·5칸) — v2.6.1 · 아래 1-70 |

**공통 규칙(그대로 물려받음)** — ① 피부가 5줄 이어진 뒤의 것만 후보(눈화장 방어) ② 창 9줄 중
7줄이 어두워야 눈썹(쌍꺼풀 주름 방어) ③ 문턱은 **상대값만**(어두운 조명 방어) ④ 윗끝은
우선순위 ①피부 복귀 → ②퍼센트 하락(1-68).

**아치에만 있는 것 두 가지**

| 규칙 | 왜 |
|---|---|
| **훑는 자리 = 산꼭대기** | ⛔ 아치선(v6)에서 재지 마세요. v6 는 「꺾임점」(1-34)이라 봉우리보다 바깥이고, 거기서 재면 산이 아닌 자리의 눈썹을 잽니다. **자를 그리는 기둥(v6)과 재는 자리(peak)는 다릅니다** |
| **창 천장에 닿으면 포기**(`ceil`) | 크게 확대한 사진에서 눈썹이 창 위로 빠져나가면 윗끝이 창 천장에 **못박힙니다**(판독-룰 3장). 그때는 값을 내지 않고 밴드를 남깁니다 |

**못 읽었을 때.** v2.5.0 까지는 **밴드를 그대로 남겼습니다**. v2.6.0 부터는 원장님이 정해 주신
**표준값**(앞머리에서 3칸 · 거기서 5칸)이 들어갑니다 — 다만 **실패 이유에 따라 갈립니다**. 1-70 을 보세요.

**남는 안전판.** 아치두께 마지노선(1-45 · `min(꼬리, 앞머리) − 3px`)은 그대로입니다 —
판독이 성공해도 마지막에 한 번 더 걸립니다.

회귀 **162** 가 잠급니다(쉐도우·창 천장·해부학 순서 방어 · 돌연변이 검사로 무의미하지 않음을 확인).

### 1-70. ⭐⭐⭐ 아치 표준값 — 판독이 실패했을 때 (v2.6.0 · 원장님 지시 2026-08-29)

> 「1번 판독 실패시 표준값 : 아치두께는 앞머리 측정값에서 3칸 위로,
>   아치엣지는 아치두께 위치에서 5칸 위로 측정한다」  ← **원장님 재확인 5칸 확정 (v2.6.1)**

**왜 필요했나.** v2.5.0 에는 아치 표준값이 없어서, 픽셀 판독이 포기하면 **밴드값이 그대로**
남았습니다. 실제 고객 사진 5장을 넘버링으로 표기해 원장님께 판정을 받은 결과:

| 사진 | 아치엣지 | 아치두께 | 원장님 판정 |
|---|---|---|---|
| 1번 | 23.1 (밴드) | 16.0 (밴드) | **틀림** — 20 / 15 여야 한다 |
| 2번 | 18.7 (판독) | 13.2 (판독) | 맞음 |
| 3번 | 23.8 (판독) | 18.5 (판독) | 맞음 |
| 4번 | 17.0 (판독) | 11.8 (판독) | 맞음 |
| 5번 | 14.3 (판독) | 8.5 (판독) | 아치엣지 맞음 · **아치두께는 10.5** 여야 한다 |

1번은 산꼭대기가 **관자놀이 머리카락** 위에 잡혀 판독이 포기했고, 그 자리에 남은 밴드값이
눈썹 위로 **4.9 눈금** 떠 있었습니다. 이제 그 경우 표준값이 들어갑니다 —
표준값을 넣으니 1번은 **아치두께 15.4 · 아치엣지 20.4** 가 되었습니다 — 원장님 정답 15 / 20 과 맞습니다.

**⚠️ 표준값은 아무 때나 쓰지 않습니다 — 실패 이유로 갈립니다.**

| 실패 이유 | 뜻 | 하는 일 |
|---|---|---|
| `seen = 0` | 산꼭대기에 「두꺼운 검은 것」이 **아예 없다** — 테두리만 그린 드로잉, 저대비 맨 눈썹 | **밴드값 그대로** (밴드가 읽은 것이 유일한 증거) |
| `seen > 0` | 뭔가 보이는데 **열들이 서로 다른 말**을 한다 — 머리카락·그늘 | **표준값** |

⛔ 이 갈림을 지우지 마세요. 지우면 원장님이 **드로잉을 그려 놓은 경우**까지 표준값이 덮어
버립니다 — 실제로 지워 보니 회귀 **89**(테두리만 그린 드로잉) 아치두께 159(기대 140),
**94**(맨 눈썹) 156(기대 140) 으로 떨어졌습니다. 드로잉이 있으면 드로잉이 정답입니다.

**⚠️ 아직 확인 못 한 것 (원장님 확인 필요)**

- (해결) 처음 지시는 6칸이었고 그러면 21.4 가 나왔습니다. **원장님 재확인으로 5칸 확정**
  (v2.6.1) → 1번 20.4 (정답 20). 판독이 성공한 3장의 간격도 5.2~5.5 였습니다.
- 5번 아치두께 8.5 → 10.5 는 **아직 못 고쳤습니다**. 이건 표준값 문제가 아니라 판독이
  눈썹 아래 **성긴 잔털**까지 눈썹으로 세는 문제입니다. 같은 문턱으로 2·3·4번이 맞았으므로
  문턱을 함부로 낮추면 맞던 것이 틀어집니다 — 사례를 더 받아서 고쳐야 합니다.

**회귀 163** 이 산수(3칸·5칸)와 `seen` 세기를 잡고, **회귀 89·94** 가 갈림을 잡습니다.

---

### 1-71. ⭐⭐⭐ 아치엣지만 잡힌 경우의 아치두께 (v2.7.0 · 원장님 지시 2026-08-29)

> 「5번 사례의 경우, 판별룰 추가 — **아치엣지가 잡힌다 + 아치두께 안 잡히는 경우
>   = 아치엣지에서 5칸 아래 위치한다 대체값**」

**「아치두께가 안 잡힌다」를 좁게 정했습니다.** 아랫끝이 **넘버링 하한(6 눈금) 아래로 새어
내려간 열** — 눈꺼풀·속눈썹까지 읽었다는 뜻이라 그 아랫끝은 두께가 아닙니다. 그때 윗끝이
멀쩡하면(6~26 눈금, 창 천장 아님) **윗끝은 살리고 두께만 아치엣지에서 5칸 아래**로 놓습니다.

```
아치엣지 = 살아남은 윗끝들의 중앙값
아치두께 = 아치엣지 + 5 눈금 (아래로)
```

**⛔ 해부학 순서 위반(아치두께가 앞머리보다 아래)은 이 길로 보내지 마세요.** 그건 덩어리
자체가 눈썹이 아닐 수 있다는 신호라 윗끝도 못 믿습니다. 실제로 그 가드를 빼 보니
**회귀 162ⓒ** 가 바로 떨어졌습니다(쌍꺼풀 쉐도우의 윗끝이 아치엣지로 살아났습니다).

**⚠️ 5번 사진은 이 룰로 고쳐지지 않습니다.** 5번은 아랫끝이 눈 위 **8.5 눈금**으로 하한(6)
위에 있어 「잡힌」 것으로 판정됩니다 — 못 잡은 게 아니라 **성긴 잔털까지 세어 낮게 잡힌**
것입니다. 원장님 정답 10.5 를 내려면 「잡혔지만 너무 아래면 버린다」는 상한이 필요한데,
그 상한(아치엣지−5칸)을 걸면 원장님이 맞다고 하신 2·3·4번이 각각 13.2→13.7 · 18.5→18.8 ·
11.8→12.0 으로 움직입니다. **사례가 더 필요합니다.**

**회귀 164** 가 잡습니다(대체값 산수 · 아랫끝이 멀쩡하면 읽은 값이 이김). 돌연변이 확인:
5칸→3칸 → 164 실패 · 해부학 가드 제거 → 162 실패.

---

## 2. 📐 잠금된 기본값 (변경 시 사용자 재학습 필요)


### 가로 라인 (H)

> 버튼 이름은 **언어에 따라 바뀝니다** (v1.19.0 · 1-12). 아래는 영어 이름 / 한국어는 괄호 안.

| 버튼 | key | 색상 | 기본 위치 | 기본 표시 | 선 길이(너비 비율) |
| --- | --- | --- | --- | --- | --- |
| Eye (눈) | `h1` | **`#3A3F4A` · op 0.5** | **0.60** | ✅ | 0 → 1 (좌우 관통 · 수평 기준선) |
| Front (앞머리) | `front` | `#14161B` | 0.40 | ✅ | 0.195→0.345, 0.655→0.805 (**앞두께와 동일**) |
| F.T (앞두께) | `frontThickness` | `#14161B` | 0.35 | ✅ | 0.195→0.345, 0.655→0.805 (**앞머리와 동일**) |
| Arch (아치) | `h2` | `#2E8BFF` | 0.34 | ✅ | **−0.02→0.13, 0.87→1.02** (눈썹 **산** 자리 · A.T 와 동일) |
| A.T (아치두께) | `archThickness` | `#2E8BFF` | 0.38 | ✅ | **−0.02→0.13, 0.87→1.02** (**아치와 동일**) |
| Tail (꼬리) | `h3` | `#A855F7` | 0.43 | ✅ | **−0.15→−0.02, 1.02→1.15** (아우터 **바깥**) |

### 세로 라인 (V)

| 버튼 | key | 색상 | 기본 위치 | 기본 표시 | 대칭 |
| --- | --- | --- | --- | --- | --- |
| Center (센터) | `v1` | `#111111` | 0.50 | ✅ | 기준선 |
| Inner (이너) | `v2` → `v3` | `#111111` | 0.35 | ✅ | Center 기준 |
| Outer (아우터) | `v4` → `v5` | `#0066FF` | 0.15 | ✅ | Center 기준 |

### 그 외

| 항목 | 값 |
| --- | --- |
| 눈 아몬드 가이드 | **기본 꺼짐** (`eyeGuideVisible: false`) — 자동 줌이 충분히 크므로 시야를 가리지 않게 함.<br>**v1.28.0 에서 `눈가이드` 버튼을 없앴습니다**(쓰이지 않음). 렌더 코드와 상태값은 그대로 두어 옛 프리셋이 켜 두었다면 계속 그려집니다 |
| Base Structure | 기본 꺼짐, **`innerAngle 0.10`** (위에서 10% 지점 — 피봇을 맨 위로) / **`outerAngle 0.125`** (= **아래 45°**)<br>피봇이 위에 있고 팔이 아래로 45° 벌어져야 **V 사선**을 눈썹 위에서 확인할 수 있습니다 (v1.19.0) |
| 줌 범위 | 0.5 ~ **20** 배 (v1.25.0 — 작은 사진도 목표 눈 크기까지) |
| 회전(밸런스) 범위 | ±30° |
| V Angle 범위 | **±60°** (`V_ANGLE_MAX`) — v1.19.0 에서 40→60. 기본 −45° 를 담으려면 40 으로는 부족합니다.<br>이 값을 바꾸면 **저장된 프리셋의 각도가 달라집니다** — 바꿀 때 내장 프리셋 값도 같이 보정하세요 |
| 자동 정렬 기준 | 동공 간 거리 = **캔버스 너비의 44%** (`EYE_FRAC 0.44`) — 눈썹이 크게 보이도록 |
| **자동 정렬 기준점** | **가로 = `centerX() = workRight() / 2`** (v1.17.0 — 고정값 아님) / **세로 = `CENTER_Y 0.60`**<br>가로는 **메인 작업 영역의 한가운데** (1-11 참고). 세로는 위쪽에 **V 사선·눈썹 라인 공간**을 두려고 아래로 내립니다 (v1.14.0 +5% → v1.19.0 다시 +5%)<br>**`DEFAULT_GUIDE.h1` 도 같은 값(0.60)이어야 합니다** — 어긋나면 사진과 빨간 눈 기준선이 따로 놉니다<br>`alignFromPupils()` · `autoAlign()` 두 곳에서 함께 씁니다 — **한쪽만 바꾸면 사진과 Center/Eye 선이 어긋납니다** |
| 인체 계측 비율 | 내안각 `R_INNER 0.52` / 외안각 `R_OUTER 1.50` (동공 반거리=1.0) |
| 라인 드래그·탭 인식 반경 | 28 px (`HIT_PX`) |

---

## 3. 🧭 레이아웃 원칙 (사용성 — 함부로 바꾸지 말 것)

**가로 모드 · 양손 조작이 이 앱의 핵심 설계입니다.**

```
[ 왼손 ]          [    사진    ]          [ 오른손 ]
 라인 선택 레일      가이드 표시             위치·보정 조절
 #lineRail          #stage                 .topbar + .panels
```

- 왼손 엄지로 조절할 바를 고르고, 오른손으로 그 바를 움직입니다.
- 세로 모드에서는 라인 버튼이 캔버스 하단 오버레이로 자동 이동합니다 (`placeLineBars()`).
- 라인 버튼을 오른쪽으로 옮기면 **양손 조작이 무너집니다.** 옮기지 마세요.
- **가로선 라벨 배지는 화면에 표시하지 않습니다** (v1.1.1). 선 색상이 왼쪽 레일 버튼 색과 1:1 로 대응하므로
  배지 없이도 구분되고, 시술 중에 얼굴을 가리지 않는 쪽이 낫습니다. 세로선 라벨만 남깁니다.

### 화면 배분 (v1.11.0) — 사진이 화면의 90% 이상

**상단 바와 우측 조절 패널은 제거되었습니다.** 모든 컨트롤은 캔버스 위 오버레이입니다.

| 영역 | 위치 · 크기 | 내용 |
| --- | --- | --- |
| 좌측 라인 레일 `#lineRail` | **62px** 고정 | 가로선 버튼 → 세로선 버튼 → **V Center Pivot · V Angle** (`#railExtra`) |
| **사진 캔버스** `#stage` | **나머지 전부** (아이패드 가로에서 **95%**) | 사진 + 가이드 + 오버레이 도크 |
| 오른쪽 도크 `.rdock` | 오른쪽에서 `--redge`(5%+8px) · 세로 중앙 | **`초기화`(짙은 빨강)** …`.rgap` 으로 띄어서… **`사진변경`** → **`사진저장`** → **위아래 드래그 바** |
| 오른쪽아래 `.bdock` | 오른쪽에서 `--redge`(5%+8px) · 맨 아래 | 위에서부터 **사진보정 4버튼** → **`.barrow`: `↷ 다시 실행` · `↶ 되돌리기` · 좌우 드래그 바** |
| 왼쪽아래 `.ldock` | 왼쪽 끝 · 맨 아래 | **`프리셋` + 즐겨찾기 ★ 버튼**(`#favRow`) |
| **가운데아래 `.cdock`** | **`사진잠금` 의 가로 중심 = 캔버스 한가운데** | **`사진잠금` 하나** |
| 위쪽 칩 줄 `.ov-tl` | 캔버스 왼쪽 위 | `모든 라인 숨김` · `여러라인` · (`전체라인`) |
| **밸런스 `.ov-bal`** | 위쪽 · **중앙에서 약간 왼쪽**(`left:42%`) | `밸런스` · (켜면) **`왼쪽` · `오른쪽`** |

### 버튼 자리 — 원장님이 직접 정하신 것입니다 (v1.28.0 · v1.28.1 조정)

> 회귀 테스트 40 · 83번이 이 자리를 지킵니다. **옮기려면 먼저 물어보세요.**

| 버튼 | 자리 | 왜 여기인가 |
| --- | --- | --- |
| `사진변경` · `사진저장` | **오른쪽 도크**, 초기화 아래 · 위아래 바 위 (v1.29.0) | 사진을 넣고 빼는 동작이라 조절 컨트롤과 같은 손(오른손) 위쪽에 둡니다. **크기는 서로 같고 `사진변경` 은 회색** — 액센트를 주면 위계가 싸웁니다 |
| `↷ 다시 실행` · `↶ 되돌리기` | **좌우 드래그 바 왼쪽, 같은 행** (v1.29.0) | **되돌리기를 바에 가장 가깝게** — 제일 자주 누르므로 손이 짧게 움직입니다 |
| `밸런스` (+ `왼쪽`/`오른쪽`) | **위쪽 · 중앙에서 약간 왼쪽** (v1.29.0) | 왼쪽 칩 줄(선을 다루는 것)과 떨어뜨려 **판정 기능임을 눈으로 구분**합니다. 기준 쪽은 토글 하나가 아니라 **두 버튼** — 지금 어느 쪽이 기준인지 눌러 보지 않아도 보입니다 |
| `사진저장` · `사진잠금` · `사진변경` | **화면 아래 가운데.** ⚠️ v1.51.0 부터 잠금의 **가로 중심 = 센터 세로선(`g.v1`)** 입니다 (원장님 지시: 「사진잠금이 센터 라인과 동일 선상에 있도록」). v1.50.0 까지는 `left:50%`(캔버스 정중앙)이었는데, 얼굴 중심축은 화면 중앙과 달라 늘 어긋나 보였습니다. `alignCenterDock()` 이 매 렌더마다 맞추고, 좌·우 도크와 겹치지 않게 클램프합니다 | 잠금은 이 앱의 **모드 스위치**(1-8)라 손이 늘 같은 자리를 찾아가야 합니다. `.cdock` 은 `left:50%` 에서 잠금 절반만큼 되돌리는 방식이라 버튼 수가 바뀌어도 중심이 유지됩니다 |
| `프리셋` + 즐겨찾기 ★ | **왼쪽 아래** | 왼손 엄지 자리. 시술 중 디자인을 바꿔 끼우는 동작입니다.<br>**프리셋 버튼 크기는 그대로, 즐겨찾기는 낮고 길쭉하게**(높이 34/30px) — 크기 차이로 "프리셋에 딸린 목록"임을 표현합니다 |
| **`초기화`** | **오른쪽 도크 맨 위** · `.rgap` 으로 아래 버튼과 띄우고 **`--danger-d`(짙은 빨강)** | ⚠️ **아래로 되돌리지 마세요.** 시술 중 손은 화면 아래와 왼쪽 레일에 머물러서, 아래에 두면 오발 위험이 가장 큽니다.<br>**아래 버튼과 반드시 띄워 두세요.**<br>색은 밸런스 표시(`--danger`)보다 **한 단계 짙게** 씁니다. 같은 빨강이면 "고쳐야 할 곳"과 "전부 지우는 버튼"이 같은 무게로 보입니다 |
| `눈가이드` | **삭제됨 (v1.28.0)** | 자동 줌이 충분해 쓰이지 않았습니다. 아몬드 가이드 **렌더 코드(`g.eyeGuideVisible`)는 남아 있으므로** 옛 프리셋이 켜 두었다면 그대로 그려집니다 — 지우지 마세요 |

- 오른쪽 도크는 버튼이 3개(`초기화`·`사진변경`·`사진저장`)라 **세로 여유가 필요합니다**
  (`height:56%` · compact `66%`). 줄이면 위아래 드래그 바가 짧아져 미세조정이 예민해집니다.
- **좌우 드래그 바는 v1.29.0 에서 짧아졌습니다** (compact 300 → 186px). 되돌리기·다시실행이
  바 왼쪽에 들어오고 가운데 `사진잠금` 은 정중앙을 지켜야 해서 남는 폭이 그만큼입니다.
  더 길게 하려면 되돌리기·다시실행을 **보정 버튼 행**으로 올리는 방법이 있습니다(원장님 확인 필요).
- 세로 폴백에서는 폭이 좁아 가운데 도크가 왼쪽 도크 위로 올라타므로 **한 줄 위로** 올립니다
  (`body:not(.land) .cdock{bottom:… + 112px}`). 겹치면 버튼이 아예 눌리지 않습니다.
- 좌우 드래그 바(`.posctl.axis-h`)는 **가운데 도크를 넘지 않는 선까지만** 넓힙니다
  (`min(46vw,430px)` · compact `min(35vw,300px)`). 넓히면 `사진잠금` 위로 올라탑니다 — 회귀 83번.
- 즐겨찾기 버튼은 **`max-width` 로 폭을 묶습니다.** 프리셋 이름은 원장님이 직접 지으시므로
  길어질 수 있고, 그대로 두면 왼쪽 묶음이 가운데까지 밀고 들어옵니다.
- 세로선 라벨 회피 목록(`renderGuides()` 의 `blocks`)에 **`btnReset` 도 넣어야 합니다.**
  빼면 `이너` 라벨이 초기화 글자 위에 겹쳐 둘 다 안 읽힙니다.

- **캔버스 배경은 중성 다크 `#1f2228`** (`--stage`). 흰 배경은 피부톤 판단을 방해합니다.

**컨트롤 영역 스크림 (v1.16.0) — 사진이 주인공**

컨트롤이 놓이는 **아래·오른쪽 가장자리에 어두운 그라데이션**(`.scrim-b` / `.scrim-r`)을 깝니다.

- **불투명 띠로 만들지 마세요.** 사진 영역을 영구히 잃습니다. 가장자리만 진하고 안쪽으로 갈수록 투명해지는
  그라데이션이라 확대해도 작업 영역이 줄지 않습니다.
- **DOM 순서: `#photo` → `.scrim` ×2 → `#guides` → `#touch`.**
  스크림이 가이드 SVG 보다 **뒤(아래)** 에 있어야 선이 그대로 밝게 보입니다. 순서를 바꾸면 선이 흐려집니다.
- `pointer-events:none` 필수 — 빼면 사진·선 드래그가 막힙니다 (회귀 테스트 47번).
- 크기는 도크에 맞춰 변수로: `.scrim-b{height:calc(var(--bdock) + 30px)}` / `.scrim-r{width:calc(var(--redge) + 62px)}`
- **아래·오른쪽 컨트롤에는 `backdrop-filter` 를 쓰지 마세요.** 스크림이 이미 배경을 어둡게 하므로 불필요하고,
  확대된 사진 위 블러는 비용이 크며 실제로 **버튼이 빈 사각형으로 렌더되는 문제**가 있었습니다.
  블러는 사진 위에 그대로 떠 있는 `.chip`(all line · AI 안내)과 `.linebar` 에만 남깁니다.
- 어두운 스크림 위에서 버튼이 묻히지 않도록 버튼 배경은 `rgba(54,59,69,.92)` 정도의 **중간 회색**입니다.
- 캔버스 위 칩·버튼은 **어두운 반투명 + 흰 테두리** — 밝은 피부/어두운 머리 어디에 놓여도 읽힙니다.
  반투명 흰색 배경으로 바꾸면 밝은 사진에서 사라집니다.

  > ⚠️ **v1.49.0 까지 코드가 이 규칙과 어긋나 있었습니다.** `.chip` 배경이 `var(--glass)` =
  > `rgba(255,255,255,.035)` — 거의 투명한 흰색이라, 원장님 지적(2026-08-22) 「맨위에 버튼들
  > 사진이 밝고 고객 이마가 넓은경우 버튼이 아예 안보인다」 그대로 **흰 바탕에 흰 글씨**가 됐습니다.
  > v1.50.0 에서 `rgba(10,13,20,.68)` + 테두리 `rgba(255,255,255,.36)` 로 되돌렸습니다.
  > **경고 칩(`.chip.warn`)과 `초기화`도 같은 이유로 어두운 판 위**에 올립니다 — 붉은 반투명은
  > 밝은 이마 위에서 똑같이 사라집니다. 다만 **초기화는 채움으로 만들지 마세요**: 눌리면 전부
  > 지워지므로 읽히기만 하고 시선을 끌면 안 됩니다. 회귀 105.
- **가로선 라벨 배지는 표시하지 않습니다** (v1.1.1). 선 색상이 왼쪽 레일 버튼 색과 1:1 로 대응합니다.
  세로선 라벨(Center/Inner/Outer)만 남깁니다.
- AI 안내 칩은 **왼쪽 위**(`all line` 바로 아래)입니다 — 아래는 좌·우 도크가 모두 차지합니다.
- **삭제된 것 (v1.11.0) — 되살리지 마세요**: 상단 바, 우측 패널, `가이드 값` 목록,
  `동공정렬` 버튼, `화면가로` 버튼, 별도 사진 보정 슬라이더(`phSlider`), 두 번째 잠금 버튼(`btnLock2`).

---

## 4. 🔁 변경 절차 (이 순서를 지키세요)

1. **현재 상태 백업** — 아래 5번 참고 (또는 이미 GitHub에 커밋돼 있으면 생략 가능)
2. 코드 수정 (`index.html` / `app.js`)
3. **회귀 테스트 실행** — 아래 6번. 전 항목 통과해야 함
4. 실제 기기(아이패드 가로)에서 한 번 눈으로 확인
5. **BASELINE.md 에 규칙 추가** + `sw.js` 의 `VERSION` 올리기 (안 올리면 폰에 옛 화면이 남습니다)
6. **백업 zip + SHA-256 만들기 ← 배포보다 먼저** (아래 경고 참고)
7. GitHub에 파일 교체 커밋 → 1분 뒤 라이브 반영
8. 잘 동작하면 **새 태그(릴리스) 생성** — 다음 복구 지점이 됩니다
9. 백업·대시보드를 맥 `_출력` 폴더에 저장

> ⚠️ **백업은 반드시 배포보다 먼저 만드세요.** (v1.23.1 에서 순서를 바꾼 이유)
> 예전에는 배포 → 태그 → 백업 순서였는데, **중간에 세션·네트워크가 끊기면
> "배포는 됐는데 백업이 없는" 상태**가 남습니다. 실제로 v1.16.0 · v1.20.1 이 그렇게 빠졌습니다.
> 테스트가 통과한 시점의 코드가 곧 백업 대상이므로, 배포를 기다릴 이유가 없습니다.

> 한 번에 여러 가지를 바꾸지 마세요. **하나 고치고 → 테스트 → 커밋**을 반복하면
> 문제가 생겼을 때 어디서 깨졌는지 바로 알 수 있습니다.

**배포 체크리스트 (하나라도 빠지면 다음 사람이 헤맵니다)**

- [ ] 회귀 테스트 전 항목 통과
- [ ] `BASELINE.md` 기준 버전 · 규칙 · 테스트 표 갱신
- [ ] `sw.js` `VERSION` 증가
- [ ] `README.md` 가 지금 기능과 맞는지 (기능이 늘었으면 4번 표 갱신)
- [ ] **`클로드-시작-명령서.md` 도 같이 갱신했는지** ← ⚠️ 아래 이유 참고
- [ ] **백업 zip + SHA-256**
- [ ] GitHub 커밋
- [ ] 릴리스 태그
- [ ] 맥 `_출력` 저장 · 대시보드 갱신

> ⚠️ **`클로드-시작-명령서.md` 는 반드시 저장소 안에 있어야 합니다.**
> 원장님은 새 창에서 파일을 첨부하지 않으십니다. 아래 한 줄만 말씀하십니다.
>
> 「github.com/leahkshop/perfect-brow 클론해서 클로드-시작-명령서.md 읽고 그대로 해라」
>
> 즉 **저장소에 올라간 명령서가 곧 다음 사람이 받는 지시 전부**입니다.
> 규칙을 바꿔 놓고 명령서를 안 올리면, 새 창은 **옛 규칙으로 조용히 작업합니다.**
> 로컬 파일만 고치고 커밋을 빠뜨리는 것이 이 시스템의 가장 위험한 구멍입니다.

---

## 5. 💾 백업 · 복구

### 복구 지점 3중화

| 순위 | 방법 | 복구 범위 |
| --- | --- | --- |
| 1 | **GitHub 태그(릴리스)** — `v1.0.0` 등 | 검증된 특정 버전으로 통째 복구 |
| 2 | **GitHub 커밋 히스토리** | 어떤 시점으로든 복구 |
| 3 | **로컬 백업 zip** (`_출력/백업/`) | 인터넷/계정 문제 시에도 복구 |

### GitHub에서 되돌리는 법 (가장 쉬움)

1. `github.com/leahkshop/perfect-brow` → **Releases** → 되돌릴 버전의 `Source code (zip)` 다운로드
2. 압축 풀고 → 저장소 → `Add file` → `Upload files` → 파일 전체 덮어쓰기 → Commit
3. 1분 뒤 라이브 주소가 그 버전으로 돌아옵니다

### 특정 파일 하나만 되돌리기

저장소에서 파일 클릭 → 우측 상단 **History** → 원하는 시점 → `⋯` → `View file` → 내용 복사 → 현재 파일에 붙여넣고 커밋

### 로컬 백업 만들기 (맥 터미널)

```bash
cd ~/Downloads/perfect-brow-source/_출력
mkdir -p 백업
zip -r "백업/perfect-brow-$(date +%Y%m%d-%H%M).zip" perfect-brow-web
```

---

## 6. ✅ 회귀 테스트 (변경 후 반드시 실행)

`regression-test.mjs` 가 실제 브라우저를 띄워 핵심 기능을 자동 검증합니다.

```bash
# 최초 1회만
npm install playwright
npx playwright install chromium

# 매 변경 후
node regression-test.mjs
```

**검증 항목 (하나라도 FAIL 이면 커밋하지 마세요)**

| # | 항목 | 통과 기준 |
| --- | --- | --- |
| 1 | 페이지 로드 · JS 오류 | 콘솔 에러 0건 |
| 2 | 라인 직접 드래그 | 60px 끌면 60±3px 이동 |
| 3 | Inner 좌우 대칭 | `(v2+v3)/2 == v1`, 오차 < 0.001 |
| 4 | Outer 좌우 대칭 | `(v4+v5)/2 == v1`, 오차 < 0.001 |
| 5 | Center 이동 시 동반 이동 | v2~v5 가 같은 양만큼 이동 |
| 6 | 동공정렬 | 6° 기울인 이미지 → 회전 -6.0±0.3°, `v1 = centerX()` · `h1 = 0.55` |
| 7 | 줌/회전 슬라이더 | 값이 정의된 범위 안에서 반응 |
| 8 | 프리셋 저장 | 새로고침 후에도 유지 |
| 9 | 이미지 저장 | PNG 다운로드 성공 |
| 10 | 가로 레이아웃 | 좌측 레일 + V 버튼, 두 도크 잘림·겹침 없음, 사진 90% 이상 |
| 11 | 세로 폴백 레이아웃 (`off`) | 라인 버튼이 캔버스 하단으로 이동 |
| 12 | 좌표계 규약 | 모든 라인 값이 0~1 범위 |
| 13 | 세로 기기에서 가로 강제 | `rot90`+`land` 적용, 캔버스가 가로 |
| 14 | 회전 상태 드래그 정확도 | 회전 중에도 60px 끌면 60±3px (CTM 사용 금지) |
| 15 | 가로 강제 해제 | 기기 방향으로 복귀 |
| 16 | 사진 잠금 | 사진 고정 + 보정 버튼 반투명 잠김 + 가로 바가 선 조절로 복귀 |
| 17 | 잠금 중 선 조절 · 가로바 축 고정 | 대각선으로 끌어도 위아래 성분만 반영 |
| 18 | 세로바 축 고정 · 대칭 유지 | 좌우 성분만 반영, `(v2+v3)/2 = v1` |
| 19 | 방향 버튼 | 가로바 `▼▲` / 세로바 `◀▶` |
| 20 | 잠금 해제 | 보정 버튼이 다시 밝아지고 눌림 |
| 21 | **잠금 중** 빈 곳 드래그 → 선택된 가로바 | 대각선으로 끌어도 세로 성분만 따라옴, 사진 불변 |
| 22 | **잠금 중** 빈 곳 드래그 → 대칭 세로바 | 좌우 성분만, `v2`/`v3` 가 반대로 대칭 이동 |
| 23 | 두 손가락 드래그 | 사진만 이동, 선은 불변 |
| 24 | 세로 조절자 = 오른쪽(여백 5%) · **세로 중앙** | 세로 배치, `▲` 위 / `▼` 아래, 우측 여백 4~10% |
| 25 | 가로 조절자 — 아래·오른쪽 정렬 | 가로 배치, `◀` 왼쪽 / `▶` 오른쪽 |
| 26 | 조절자 방향 = 선의 이동 방향 | 위로 밀면 위로, 오른쪽으로 밀면 오른쪽 |
| 27 | 선 탭 → 선택만 (값 불변) | 3px 데드존으로 밀리지 않음 |
| 28 | 다른 선 탭 → 전환 | 선택 + 조절자 축이 함께 바뀜 |
| 29 | **기본값에서 세로 기기 → 항상 가로** | `rot90`+`land`+레일 표시, `pb_orient` 저장값 없음(자동 해제 안 함) |
| 30 | 기기가 실제 가로면 가짜 회전 해제 | `rot90` 꺼짐, 설정값은 그대로 유지 |
| 31 | 세로로 되돌아가면 즉시 가로 복귀 | `rot90`+`land` 재적용 |
| 32 | 두 조절자 겹침 없음 · 캔버스 안 | 세로/가로 조절자가 서로 겹치지 않고 둘 다 캔버스 안 |
| 33 | **앱 실행 직후 두 조절자 모두 표시** | `#posCtlV`·`#posCtlH` 둘 다 보임 (기본 대상 `h1` / `v1`) |
| 34 | **강제 가로 — 손 제스처 축이 화면과 일치** | 화면상 아래로 끌면 가로선이 아래로, 오른쪽으로 끌면 세로선이 오른쪽으로 |
| 35 | **잠금 해제 · 한 손가락 사선 드래그 = 사진 자유 이동** | Δ(70,−55)px 를 그대로 따라오고 선은 불변 |
| 36 | **가로 바 하나로 선 조절 ↔ 사진 보정 겸용** | 보정 버튼 클릭 → 사진 반응·선 불변, 재클릭 → 선 조절 복귀 |
| 37 | 세로선 선택 → 가로 바가 선 조절로 자동 복귀 | `hMode` 가 `line` 으로 |
| 38 | 오른쪽 아래 도크 | 사진보정 버튼 위 / 좌우 바가 **오른쪽에서 5% 떨어져** 맨 아래 |
| 39 | **되돌리기·다시실행 = 좌우 바 왼쪽** (v1.29.0) | 되돌리기가 바에 더 가깝게 · 삭제 버튼 정리 |
| 40 | 메뉴 행 = 왼쪽 아래 · 초기화 띄어서 같은 행 | id 순서까지 검사, 좌우 바와 겹치지 않음 (가로 전용) |
| 41 | **되돌리기 — 직전 작업만, 다시 누르면 그 전 작업** | 2단계 쌓고 2번 되돌리면 처음 값, 빈 스택이면 버튼 잠김 |
| 42 | 드래그 1회 = 되돌리기 1단계 | 탭만 하면 기록되지 않음 |
| 43 | 라인 버튼 1탭 선택 · 다시 탭 숨김 | 선택은 유지, 표시만 토글 |
| 44 | **자동 정렬 = 작업 영역 가로 한가운데 · 세로 55%** | 기대값을 `rightDock.offsetLeft` 에서 계산해 비교 |
| 45 | 세로 조절자 값 라벨 = 바 **오른쪽** · 캔버스 안 | 여백(`--redge`) 안에 들어옴 (가로 전용) |
| 46 | 세로선 라벨 = 캔버스 맨 위(갭 6px) | `all line` 칩과 겹치지 않음 (가로 전용) |
| 47 | **컨트롤 영역 스크림** | 터치 통과 · 가이드 선이 스크림 위 · 도크를 덮음 (가로 전용) |
| 48 | **가로 자 길이 = 눈썹 구간** | 이너·아우터를 좁히면 자도 짧아짐 · 바를 넘지 않음 (가로 전용) |
| 49 | **여러라인 — 선택 누적 · 다시 누르면 해제** | 해제해도 숨겨지지 않음 |
| 50 | **여러라인 — 선택된 선들이 함께 이동** | 비선택 선은 불변 |
| 51 | 선택된 라인 강조 | 선택 시 선 굵기 증가 |
| 52 | 여러라인 해제 → 한 개만 선택 | `selSet` 비워짐 |
| 53 | 버튼 이름 | `모든 라인 숨김` + 옆에 `여러라인` |
| 54 | **화면의 선 1탭 = 선택** | 숨기지 않음 (레일 버튼과 동일) |
| 55 | **화면의 선 다시 탭 = 숨김** | v1.18.0 에서 캔버스 탭만 숨김이 안 되던 버그 |
| 56 | 여러라인 모드의 화면 탭 | 선택 해제만 · 숨기지 않음 / 숨김은 되돌리기 1단계 |
| 57 | **다시 실행 (redo)** | 되돌린 작업을 앞으로 · 새 작업 시 갈래 폐기 |
| 58 | **전체라인** | 여러라인 OFF 면 `hidden` · 보이는 선 전부 선택/해제 |
| 59 | 전체라인 동시 이동 | 화면의 모든 가로선이 같은 양만큼 |
| 60 | **한/영 전환** | 라인 버튼 이름이 바뀜 · 현재 언어만 `.on` |
| 61 | 기본값 | 눈 0.60 · V 피봇 0.10 · V 앵글 −45° |
| 62 | **아이콘** | 이모지 0개 · 8개 버튼 전부 SVG |
| 63 | **왼쪽 레일 담백** | 레일 안 요소에 `box-shadow`·`gradient` 없음 |
| 64 | **라인 버튼 색 띠** | `--dot` = 선 색 / 버튼 배경은 선 색이 아님 |
| 65 | **잠금 아이콘 모양 전환** | 잠김/열림 SVG 가 서로 다름 |
| 66 | **모달 대비** | 다크 바탕 + 밝은 글자, 대비 ≥ 7:1 (흰 바탕 금지) |
| 67 | **액센트 채움 버튼 대비** | 시안 위 어두운 글자, 대비 ≥ 7:1 |
| 68 | **모달 버튼 위계** | 채운 버튼은 주 동작 하나뿐 |
| 69 | **두께 선 실측** | 눈썹 아래 윤곽에서 측정 (고정 오프셋 아님) |
| 70 | **비대칭 얼굴 (데칼코마니)** | 기준쪽 이너·아우터 정확히 · 오차는 반대쪽이 안음 · 대칭 유지 |
| 71 | **꼬리 잘림 방지** | 자동 정렬 후 양쪽 눈썹 꼬리가 화면 안 |
| 72 | **선 켤 때 AI 배치** | 측정 위치로 스냅 · 랜드마크 없으면 그대로 |
| 73 | **자 정렬** | 짝끼리 동일 · 아치는 바깥 · 꼬리·눈 얇은 실선 없음 |
| 74 | **프리셋 얼굴 맞춤** | 넓은/좁은 얼굴에 맞춰 환산 · 대칭 유지 |
| 75 | 프리셋 적용중 표시 | 초기화하면 해제 |
| 76 | 옛 프리셋 하위호환 | 기준틀 없으면 환산 없이 그대로 |
| 77 | **밸런스 — 차이 검출** | 6px 차이를 잡고 반대쪽만 빨갛게 |
| 78 | 밸런스 — 기준 쪽 전환 | 부호가 뒤집히고 빨간 표시도 반대로 |
| 79 | 밸런스 — 좌우 같으면 표시 없음 | 건너뛴 것도 없어야 진짜 통과 |
| 80 | **밸런스 — 못 읽으면 건너뜀** | 오판해서 빨갛게 칠하지 않음 |
| 81 | **홈은 세로 · 편집기는 가로** | 사진 넣기 전에는 `rot90` 없음, 사진을 넣으면 `rot90` 적용 |
| 82 | **사진 선택 중 딤** | 방향은 그대로(`rot90` 유지) · 딤만 켜졌다 꺼짐 |
| 83 | **버튼 자리 (v1.29.0)** | 오른쪽 도크=초기화→사진변경→사진저장→위아래 바 · 잠금 단독 정중앙 · 밸런스는 중앙보다 왼쪽 · 즐겨찾기가 프리셋보다 낮음 · 도크끼리 안 겹침 · 라벨이 위쪽 버튼과 안 겹침 |
| 84 | **즐겨찾기 노출** | 지정한 개수만큼만 버튼 · 누르면 그 프리셋 적용 · 지워진 id 는 걸러냄 |
| 85 | **별표 3개 제한** | 4번째는 거부(토스트) · 다시 누르면 해제 · 삭제하면 즐겨찾기에서도 빠짐 |
| 86 | **허용 오차가 얼굴 크기를 따라감** | 같은 6px 차이라도 이너가 좁으면 잡고 넓으면 통과 (px 고정 아님) |
| 87 | **드로잉 자동 맞춤 (얼굴 인식 성공)** | 눈썹 모양에서 앞머리·앞두께·아치·아치두께·꼬리·이너·아우터를 읽음 · V 피봇은 건드리지 않음 |
| 88 | **드로잉 자동 맞춤 (인식 실패 예비 경로)** | 랜드마크가 없어도 그린 선을 찾아 같은 결과 |
| 89 | **테두리만 그린 드로잉** | 위선·아래선을 한 덩어리로 봐 두께가 맞음 |
| 90 | **사진 확대·이동 불변** | 확대하거나 옮겨도 같은 드로잉 위에 붙음 |
| 91 | **드로잉 모양이 바뀌어도** | 아치가 안쪽으로 옮겨간 눈썹에서도 선이 따라감 |
| 92 | **눈썹 아래 주름이 있어도** | 쌍꺼풀 선에 앞두께가 끌려가지 않음 |
| 93 | **세로선 묶음 (v1.32.0)** | 아치 자는 아치선을 따라감 · 아우터를 따라가지 않음 · 아우터=보라 · 아치선이 더 얇음 |
| 94 | **맨 눈썹 (v1.33.0)** | 드로잉이 없어도 저대비 2차 패스가 털을 읽어 배치 |
| 95 | **세로선 길이·굵기 (v1.33.0)** | 아치선·아우터는 눈까지 안 내려옴 · 이너는 길게 · 이너보다 얇음 |
| 96 | **시작 배치 (v1.34.0)** | 사진 입력 = 초기화와 동일한 랜드마크 배치 · 드로잉 판독은 버튼에서만 |

**초기화 × 사진잠금 (v1.37.0)** — 원장님 지시(2026-08-21): 「초기화 버튼은 사진이 잠금이
되어있을경우 잠금이된 사진을 제외하고 나머지를 초기화해라. 사진 잠금 없을경우 사진도 함께
초기화 한다」. 잠금 중 초기화는 `placeLines()`(사진 변환은 그대로, 선만 랜드마크 배치)를
씁니다 — ⚠️ 여기서 `autoAlign()` 을 부르면 사진이 움직여 잠금 약속이 깨집니다. 회귀 99.
| 97 | **꼬리 2단계 (v1.41.0)** | 보이는 진한 꼬리 = 잉크 끝 · 안 보이면 = 콧볼–외안각 연장선 · 약한 대비 금지 |
| 98 | **판독 기준쪽 (v1.37.0)** | 기본은 밸런스에서 고른 쪽 · 반대쪽이 1.5배 이상 짙으면 자동 전환 |
| 99 | **초기화 × 사진잠금 (v1.37.0)** | 잠금 중엔 사진(위치·배율·회전·잠금) 유지, 선만 초기화 · 잠금 없으면 사진까지 초기화 |

**버튼 배치 (v1.43.0/v1.44.0 · 원장님 지시 2026-08-21)** — 가운데 도크 = **사진저장 ·
사진잠금 · 사진변경** 한 행(잠금이 캔버스 중앙). 되돌리기·다시실행 = **위아래 드래그 바
위**(오른쪽 도크, 되돌리기가 바에 가깝게). 좌우 드래그 바 폭 = **줌~밸런스 버튼 행과
동일**. 초기화 = 더 크게(58px) · 도크 위로 올림 · **붉은 배경 + 흰
글자**. 선 값 HUD(네모칸)는 띄우지 않는다 — 바 라벨 숫자로 충분. 회귀 39·83.
**v1.45.0 (원장님 지시 2026-08-21)** — 오른쪽 도크 순서 = **초기화 → 위아래 조절 바 →
다시실행 → 되돌리기**. 초기화 = **프리셋 버튼과 같은 크기**(50×68 / compact 46×52),
색은 진빨강 대신 **앱 컨셉 코랄**(--danger 글자 + 유리 배경). 사진저장 = **줌(켜짐) 버튼과
같은 그라데이션 채움**. 되돌리기·다시실행 = 더 크게(46px / compact 38px) + 쓸 수 있을 때
**--grad-deep 채움 + 흰 글자**(행동 버튼). 위아래 바 = 조금 짧게(rdock 56%→48%, compact
66%→58%). 회귀 39(순서)·83(크기·색) 갱신.
⚠️ **v1.44.1 (원장님 확인 「오른쪽 아래 좌우 바는 안길어 졌다」)** — v1.44.0의 width:100%는
compact 규칙 `width:min(22vw,186px)` 에 밀려 iPhone에서 무효였음. compact도 100%로 통일하고,
`.barrow{width:0;min-width:100%}` 로 바의 고유 폭이 도크 폭 계산에 끼지 않게 해야
**바 폭 = 줌~밸런스 행 폭(양끝 일치)** 가 된다. width:100%만 주면 슬라이더 고유 폭 때문에
바가 행보다 길어진다. 회귀 38이 폭·왼쪽 끝 일치(±2px)를 잡는다.
| 100 | **홈 화면 버전 표시 (v1.39.2)** | `APP_VERSION` 이 홈 화면에 보임 — iOS PWA 캐시 확인용. 릴리스 때 sw.js VERSION 과 함께 올린다 |
| 101 | **가이드 플로우 (v1.81.0 · v1.83.0 이너 포함)** | **이너**→앞머리→앞두께→아치엣지→아치두께→꼬리아우터→꼬리높이 · 인사 뒤 첫 스텝 · 꼬리 높이 뒤 종료 · 끄면 즉시 종료 |
| 102 | **이너 묶음 두 색 (v1.48.0)** | 연한 = `#C9D1D6` 불투명(알파 아님) · 강조 = `#5EEAD4` + 굵어짐 · 선 색 = 레일 버튼 띠 색 |
| 103 | **가이드 — 밝은 선은 하나 (v1.49.0)** | 다음 차례로 넘어가면 이전 선이 옅어지고 밝은 선은 정확히 1개 · 조절 바도 함께 이동 · 플로우 밖 선을 고르면 추천을 내리고, 안의 선을 고르면 재개 |
| 104 | **아치·꼬리 연한 색 (v1.49.0)** | 고유색 `#2E8BFF`·`#A855F7` 은 그대로 · 연한 상태만 `#A9CFF2`·`#D0B8F0` 불투명 (알파로 되돌리면 피부와 밝기가 같아져 안 보임) |
| 105 | **밝은 사진에서도 읽히는가 (v1.50.0)** | 캔버스 위 칩·경고 칩·초기화가 어두운 판 위 · 사진잠금=채움 · 사진저장=채움 아님 · 가이드는 켜졌을 때만 채움 |
| 106 | **일하는 곳만 굵게 (v1.51.0)** | 자는 세로선 바깥 절반만 굵고 안쪽은 얇은 회색 · 세로선은 두 자 사이(눈썹 속)를 비우고 바깥만 굵게 · 꼬리 자 길이는 아치 자의 절반 · 사진잠금 중심 = 센터선 |
| 107 | **프리셋 (v1.51.0)** | 내장 기본 3종 없음 · 사용자가 저장한 것만 · 프리셋 버튼 자체는 유지 |
| 108 | **지시등 (v1.55.0)** | 차례인 선은 CSS 로 4초에 한 번 느리게 깜빡임(JS 타이머 없음) · 잡으면 짙은 회색 심 + 살구색 테두리 · 저장본은 언제나 선명 |
| 109 | **설정 (v1.81.0)** | 색상표 **8색**(흰색) · **세로선 3색 목록** · 굵기·테두리 굵기 **슬라이더** · 테두리 색 없음/흰색/검정/먹색 · 선택 강조 · 탭 3개 |
| 110 | **조합 순환 버튼 (v1.58.0)** | 가이드 오른쪽 · 클릭마다 다음 추천 조합 · 한 바퀴 돌면 내 세트로 복귀 |
| 111 | **설정 시트 가로 (v1.59.0)** | 아이폰 세로 잠금(`rot90`)에서도 시트가 가로로 뜬다 |
| 112 | **원장님 확정 기본 세팅 (v1.60.0 · v1.81.0 갱신)** | `LOOK_DEF` 15개 값 고정 (세로선 이너 민트 / 아치선·아우터 먹색 · 테두리 없음) |
| 113 | **꼬리·아우터 사선 (v1.61.0)** | 한 손짓으로 꼬리 끝 점을 놓는다 (거울 쪽 부호 반전 · 다른 자는 그대로) |
| 114 | **미세 이동·프롬프트·십자 (v1.81.0)** | 화살표 한 칸 ≈1px · ▶=위로 · 프롬프트가 **끄면 실제로 사라진다** · 십자 모서리는 **꼬리 두 스텝에만**(안쪽+위) · 아치에는 없다 |
| 115 | **머리카락 방어 — 인식 성공 (v1.66.0)** | 관자놀이 머리카락이 있어도 랜드마크 경로가 눈썹만 읽는다 |
| 116 | **머리카락 방어 — 예비 경로 (v1.66.0)** | 얼굴 인식이 실패해도 머리카락을 눈썹으로 읽지 않는다 (아우터 오차 47px 재발 방지) |
| 117 | **아치는 혼자 움직인다 (v1.81.0)** | 아치·아치두께·아치선이 전부 따로 (v1.67.0 동반 폐지) |
| 118 | **가로 길이 슬라이더 (v1.68.0 · v1.81.0)** | 아주 짧게(8%)까지 · 끌면 **눈썹 위 미리보기**의 자와 실제 자가 같이 줄었다 늘어남 (`pv-ruler` 로 찾는다) · 십자 표식 테두리는 설정을 따름 |
| 119 | **교정 프롬프트 (v1.69.0)** | 드로잉 맞춤 뒤 가이드가 ① 부터 다시 시작 · 프롬프트에 ①~⑥ 번호 · 좁은 폰에서도 안 잘림 |
| 120 | **크게 확대한 사진 (v1.69.0)** | 눈썹이 지금 선보다 훨씬 위에 있어도 예비 경로가 찾아냄 |
| 120b | **탐색창에 갇힘 방지 (v1.69.0)** | 앞머리·아치·꼬리가 한 값(창 천장)으로 뭉치지 않음 |
| 121 | **판정 기준 (v1.70.0)** | 꼬리=끝의 아랫선(윗선 아님) · 아치선=꺾임점(산꼭대기 아님) · 앞머리=90° 꼭지점 |
| 122 | **얇은 털 추적 금지 (v1.71.0)** | 아우터는 진한 눈썹의 끝에 선다 · 잔털까지 따라가지 않는다 |
| 123 | **검은 드로잉의 끝 (v1.72.0)** | 넓고 옅은 번짐이 있어도 아우터는 진한 곳이 끝나는 자리에 선다 |
| 124 | **앞머리·앞두께 위아래 (v1.73.0)** | 앞두께가 위 · 앞머리가 아래 (원장님 확정) |
| 125 | **이너 = 색이 시작하는 선 (v1.74.0)** | 앞머리가 얇아져도 색이 있는 곳까지 |
| 126 | **꼬리 = 색이 끝나는 곳 (v1.74.0)** | 얇아진 꼬리는 끝까지 · 잔털은 제외 |
| 127 | **아랫선이 창 바닥에 안 박힌다 (v1.75.0)** | 눈썹 아래 그늘이 있어도 눈썹 아랫선 |
| 128 | **그늘이 창 바닥 위에서 끝나도 (v1.76.0)** | 조건 없이 색이 살아 있는 곳까지 |
| 129 | **윗선도 색이 있는 곳까지 (v1.77.0)** | 눈썹 위 옅은 번짐에 앞두께가 안 끌림 |
| 130 | **자는 드로잉 위에 (v1.78.0)** | 이너·아우터 자가 맨살로 뜨지 않음 |
| 131 | **못박음 검사 (v1.79.0 · v1.81.0 확장)** | 까다로운 사진 9장 · 창 경계·그늘에 안 박힘 + **아치두께 ≤ 앞머리 ≤ 꼬리** |
| 132 | **가이드 OFF/ON/잡은 선 (v1.80.0)** | 끄면 전부 고유색 · 켜면 한 줄씩 · 잡은 선만 잡은 색 |
| 133 | **아치두께 마지노선 (v1.81.0)** | 꼬리·앞머리 아래로 내려가지 않는다 (눈꺼풀 그늘 방어) |
| 134 | **선 상태 (v1.81.0)** | 전체라인 인사 1회 깜빡임 · 선택하면 굵고 밝게 · 움직인 선은 잡은 선 색으로 남음 |
| 135 | **가이드 순서 편집 (v1.81.0)** | ▲▼ 로 순서 변경 · 이름을 눌러 켜고 끔 · 번호가 새 순서를 따라감 · 저장 |
| 136 | **설정 시트 배치 (v1.83.0)** | 조합 카드는 미리보기 밑 작은 카드 · 굵기·투명도·길이 각 한 줄 · 테두리는 별도 배경 블록 · 스크롤 없음 |
| 137 | **고르면 되살아난다 (v1.83.0)** | 죽은 선도 고르면 고유색 · 더 굵고 더 밝게 · 고른 순간 한 번 반짝 · 잡는 동안은 잡은 선 색 |
| 138 | **고른 선 말고는 물러난다 (v1.84.0)** | 가이드 꺼짐에서만 · 나머지는 자기 색 그대로 0.55 · 색은 바뀌지 않는다 |
| 139 | **한 번 탭에 선이 사라지지 않는다 (v1.85.0)** | 숨김은 같은 버튼 연속 두 번만 · 자동 선택은 근거가 아님 · 숨기면 HUD + 버튼에 표시 |
| 140 | **숨은 선 되살리기 (v1.86.0)** | 드로잉 맞춤이 가이드 순서의 선을 전부 다시 보이게 · 차례인 선은 숨길 수 없음 |
| 141 | **초기화셋팅 (v1.87.0)** | 4초 인사 · 사진 로드/초기화 버튼/가이드 껐다 켜기 네 곳에서 작동 |
| 142 | **잡는 범위 섬세 교정 (v1.88.0)** | 교차점은 가로 자 · 아치선은 아치두께 아래 구간만 · 눈 선 9px · 인사 조기 종료 |
| 143 | **안내 (v1.90.0)** | 중앙 위 한 자리 · 토글로 끄고 켬 · 가이드 켜짐 중 유지 · AI/눈썹정렬 두 줄 |
| 144 | **기본 언어 영어 (v1.92.0)** | 링크 미리보기(OG)·설치 설명 영어 · og:image 절대 주소 · 한국어는 눌러 저장 |
| 145 | **도크 자동 맞춤 · 무조건 가로 (v1.93.0)** | 667~844px 겹침 없음 · pb_orient=off 도 가로 · dvh 마지막 · rect 금지 |
| 146 | **놓은 선 (v1.95.0)** | doneC/doneW/doneOp 로 그림 · 잡은 선과 분리 · 설정 컨트롤 |
| 147 | **서브 라인 (v1.95.0)** | 자→이너 연결선 굵기·투명도가 설정(subW/subOp)을 따름 |
| 148 | **배경 한 번 탭 (v1.95.0)** | 단계 확인·다음으로 · 안내도 이동 · 인사 중엔 안 넘어감 |
| 149 | **예비 동공 정렬 (v1.97.0)** | 인식 실패 사진도 동공 간격 44%·기준점 정렬 · SVG 픽스처는 그대로 |
| 150 | **레일 정돈 · 위아래 바 30% 단축 (v1.98.0)** | 버튼 21px · 붙여서 중앙 · 아래 끝 고정 · 바 왼쪽 16px |
| 151 | **이너 판독 룰 (v1.99.0)** | 눈꺼풀 그늘을 안 따라감 · 40~48 · 못 읽으면 43 |
| 152 | **이너 판독이 조용히 꺼지지 않는다 (v1.99.2)** | 화면 위 밴드도 측정 · 자만 있으면 언제나 답 |
| 153 | **눈 앞꼬리 = 언제나 40 (v2.0.0)** | 동공 간격이 달라도 내안각 눈금이 안 흔들림 |
| 154 | **눈 앞꼬리 자동 인식 (v2.0.1)** | 눈꺼풀 틈이 닫히는 자리 · 코 그늘에 안 끌림 |
| 155 | **앞머리·앞두께 판독 (v2.1.0 · v2.3.0)** | 피부 다음 「두꺼운 검은 것」의 아랫끝·윗끝 (주름·눈화장 아님) |
| 156 | **앞머리 넘버링 (v2.1.1)** | 판독 없는 배치는 눈 위 보통값 눈금 · 이너와 같은 자 |
| 157 | **쌍꺼풀·주름 쉐도우 방어 (v2.1.2)** | 두꺼운 쉐도우가 있어도 넘버링이 눈썹을 고른다 |
| 158 | **앞머리 하한 (v2.1.3)** | 눈 위 7 눈금 미만은 앞머리가 아니다 → 보통값 대체 |
| 159 | **넘버링 0 동일화 (v2.2.2)** | 0 = 동공 중심 실측 · h1 을 옮겨도 안 흔들림 |
| 160 | **이너 맥시멈 45 (v2.4.0)** | 답 클램프·예비 경로 모두 45 기준 (fRaw 는 캡 전 값 보존) |
| 161 | **앞두께 우선순위② (v2.4.0)** | 피부 복귀가 불명확하면 퍼센트가 낮아지는 자리(검정 끝) |
| 162 | **아치엣지·아치두께 판독 (v2.5.0)** | 앞머리와 **같은 방법** · 쉐도우·창 천장·해부학 순서 방어 |
| 163 | **아치 표준값 (v2.6.1)** | 판독 실패 시 앞머리에서 3칸·5칸 · **실패 이유(seen)로 갈린다** |
| 164 | **아치엣지만 잡힘 (v2.7.0)** | 아랫끝이 하한 아래로 샌 열 → 아치두께 = 아치엣지 5칸 아래 |

---

## 7. 🚀 고도화 로드맵

### 다음 단계 (웹에서 바로 가능)

- [ ] 시술 전/후 사진 비교 (좌우 분할 뷰)
- [ ] 프리셋에 사진 보정값(줌/회전)까지 포함할지 결정
- [ ] 고객별 기록 저장 (이름 + 가이드 설정 + 날짜)
- [ ] 눈썹 두께를 mm 단위로 표시 (동공 간 거리 평균 63mm 기준 환산)
- [ ] 라인 세트를 시술 유형별로 프리셋화 (콤보/쉐도우/엠보)

### 앱스토어 · 구글플레이

```bash
npm i @capacitor/core @capacitor/ios @capacitor/android
npm i -D @capacitor/cli
npx cap init "Perfect Brow" com.perfectbrow.app --web-dir=.
npx cap add ios && npx cap add android
npx cap open ios
```

- 웹 파일이 앱 안에 통째로 들어갑니다. **원격 URL을 웹뷰로 띄우면 애플이 반려합니다.**
- **네이티브로 감싸면 방향 문제가 완전히 해결됩니다** — `Info.plist` 의 `UISupportedInterfaceOrientations`
  를 가로 두 개만 남기면 폰 회전 잠금과 무관하게 **앱도 사진 선택 시트도 함께 가로**로 뜹니다.
  (안드로이드는 `AndroidManifest.xml` 의 `android:screenOrientation="landscape"`)
- 심사 통과율을 높이려면 **Camera 플러그인(현장 촬영)** 과 **사진 앨범 저장**을 네이티브로 붙이세요.
  (App Store Guideline 4.2 — 단순 웹사이트 껍데기는 반려)
- 비용: Apple $99/년, Google Play $25 1회

---

## 8. 📁 파일별 책임

| 파일 | 책임 | 수정 빈도 |
| --- | --- | --- |
| `index.html` | 화면 구조 + 전체 CSS (레이아웃/반응형) | 레이아웃 바꿀 때 |
| `app.js` | 전 로직. 파일 상단 주석에 1~9 섹션 구분 있음 | 기능 바꿀 때 |
| `manifest.webmanifest` | 앱 이름·아이콘·가로 고정·전체화면 | 거의 안 바꿈 |
| `sw.js` | 오프라인 캐시. **파일을 추가하면 `SHELL_FILES` 에도 추가** | 파일 추가 시 |
| `icon-*.png` | 앱 아이콘 | 브랜딩 바꿀 때 |
| `regression-test.mjs` | 자동 검증 (174항목) | 기능 추가 시 항목 추가 |

> ⏱ **테스트는 작업 중인 부분만 돌린다** (원장님 지시 2026-08-29 · 명령서 §0-Y).
> 174항목 전체는 한 번에 3분이라 고치는 동안 매번 돌리면 원장님이 기다리신다.
> **고치는 중 = 그 선의 항목만 · 커밋 직전 = 전 항목 한 번 · 돌연변이 검사 = 원장님이
> 작업 안 하실 때 몰아서.** 커밋 직전 전 항목 확인은 건너뛰지 마라.
>
> ⚡ **v2.9.0 — 이제 말이 아니라 스위치입니다** (`PB_ONLY`). 명령서 §0-Y-1 ·
> 판독-룰 「부분 테스트 스위치」.
> `PB_ONLY=아치선 node regression-test.mjs` → **30초** (전체 196초).
> 테스트를 추가·이동하면 `if (RUN(n))` 번호와 `BLOCK_TESTS` 표를 **다시 뽑아야** 한다.
| `BASELINE.md` | 이 문서 | 기준이 바뀔 때 |

### `app.js` 섹션 지도

```
1. i18n          한국어/영어 문구        → I18N
2. SPEC/DEFAULTS 라인 정의·기본값        → H_SPECS, V_SPECS, DEFAULT_GUIDE
3. state         앱 상태                → S
4. render        사진 변환 + SVG 그리기   → renderPhoto, renderGuides
5. gesture       드래그·핀치·회전        → hitTest, pointerdown/move
6. panels        위치 조절·사진 보정      → posConfig, photoConfig
7. presets       저장/불러오기           → savePreset, applyPreset
8. faceAI        얼굴 인식·동공정렬       → autoAlign, alignFromPupils
9. export        PNG 저장               → exportImage
```

---

## 9. ⚠️ 알려진 제약

| 제약 | 설명 |
| --- | --- |
| AI 모델 최초 다운로드 | 첫 사용 시 인터넷 필요 (약 10MB). 이후 캐시되어 오프라인 동작 |
| 아이폰 홈화면 설치 | **사파리에서만** 가능 (크롬 불가) — iOS 정책 |
| 아이폰 가장자리 제스처 | 화면 맨 오른쪽(강제 가로에서는 기기 아래쪽 = 홈 인디케이터)은 시스템이 먼저 가져갑니다 → 오른쪽 컨트롤을 `--redge` 만큼 띄워 회피 |
| 세로 모드 | **지원 중지 (v1.8.0).** 가로 전용. `⟳ 화면가로` 를 끄면 세로 폴백 레이아웃이 나오지만 유지보수 대상이 아님 |
| 진짜 방향 잠금 | `screen.orientation.lock()` — **안드로이드 설치앱(PWA)만 동작.** iOS 사파리는 미구현(웹앱이 폰 방향을 바꿀 방법이 아예 없음). iOS 에서는 CSS 90° 회전으로 대체 |
| 시스템 UI 방향 | **iOS 사진 선택 시트는 늘 기기 방향(세로)** 입니다. OS 가 그리는 UI 라 웹앱이 손댈 수 없음.<br>v1.27.0 대응: **홈은 세로**로 두어 첫 사진을 편하게 넣게 하고, **작업 중 `사진변경` 은 가로를 유지한 채 화면만 어둡게** 합니다. 근본 해결은 네이티브(Capacitor) 빌드 |
| 프리셋 저장 위치 | 브라우저 localStorage. **브라우저 데이터 삭제 시 사라짐** (기기 간 동기화 없음) |
| 저장소 공개 | GitHub Pages 무료 플랜은 Public 저장소만 지원 |
