# 판독 룰 — 눈썹 포인트를 **일률적으로** 잡기 위한 규칙

> 원장님 지시 2026-08-25
> 「왜 어떤 사진은 잘 잡고 어떤 사진은 에러가 나는지 확인하고 **일률적으로 눈썹 포인트를
> 잘 잡도록 해결**. 이런 에러 사항이 생기지 않도록 **룰 저장**해서 앞으로도 파일을
> 업로드할 때 저장할 수 있도록 해라. **못박음 에러 없도록** 깃, 백업, 커밋 등 md 로 저장」

이 문서는 **매 배포마다 함께 올립니다.** `클로드-시작-명령서.md` 가 첫 줄에서 이 파일을
읽으라고 지시합니다. 지우거나 빠뜨리면 같은 실수가 그대로 재발합니다.

---

## 0. 한 줄 요약

> **모든 자는 「색이 있는 곳」까지만 간다. 창 경계도, 그늘도, 번짐도 색이 아니다.**

2026-08-25 하루에 나온 실패 **다섯 건이 전부 이 한 문장을 어긴 것**이었습니다.

---

## 1. 왜 어떤 사진은 잘 잡고 어떤 사진은 틀렸나

판독은 세로 열마다 **어두운 덩어리**를 찾습니다. 그 덩어리가 눈썹만 담고 있으면 잘 맞고,
**눈썹이 아닌 것이 붙어 있으면** 틀립니다. 붙는 것은 네 종류뿐입니다.

| 붙는 것 | 어디에 | 증상 | 사진 조건 |
|---|---|---|---|
| **눈꺼풀·쌍꺼풀 그늘** | 아래 | 앞머리·아치두께가 눈 근처로 내려감 | 조명이 어둡거나 눈이 깊은 얼굴 |
| **탐색창 바닥** | 아래 | 앞머리 = 아치두께 (같은 값) | 랜드마크 경로 · 눈과 눈썹이 가까움 |
| **파우더 번짐** | 위 | 앞두께가 눈썹 윗선 위로 뜸 | 파우더·옴브레 눈썹 |
| **관자놀이 머리카락** | 바깥 | 아우터·아치선이 눈썹 밖으로 밀림 | 머리가 눈썹까지 내려온 사진 |

**⚠️ 컨테이너에서 재현이 안 되는 이유.** 작업 컨테이너는 MediaPipe CDN 이 막혀 **예비
경로(fallbackBox)** 로만 돕니다. 예비 창은 바닥이 눈에서 멀어 「창 바닥 못박음」이 잘
안 납니다. 원장님 폰은 **랜드마크 경로(browBoxes)** 이고 창 바닥이 훨씬 눈에 가깝습니다.
→ **새 판독 규칙의 회귀 검사는 반드시 `runDraw(true, …)`(랜드마크 켬)로도 돌리세요.**

---

## 2. 규칙 (코드에 들어가 있는 것)

### 2-1. 양 끝 — 색이 시작하는 곳 ~ 색이 끝나는 곳 (`growEnd`)

`trimOutside` 는 **잉크(두께 × 진하기)** 로 양 끝을 자릅니다. 눈썹은 앞머리도 꼬리도
끝으로 갈수록 **얇아지므로** 잉크가 먼저 떨어집니다 — 색은 남았는데 잘립니다.
다듬기 뒤에 **진하기 기준**으로 다시 이어 붙입니다.

| | 문턱 (진하기 ÷ 중앙값) | 왜 |
|---|---|---|
| 이너 `INNER_DARK` | **0.25** | 코 방향에는 머리카락이 없다 |
| 아우터 `OUTER_DARK` | **0.5** | 관자놀이에는 머리카락·번짐·잔털이 있다 |

**⛔ `OUTER_DARK` 를 0.4 아래로 내리지 마세요** — 「얇은털 따라가는것 금지」(원장님
2026-08-24)가 깨집니다. 회귀 **122·123·126** 이 지킵니다.

### 2-2. 위아래 — 색이 살아 있는 곳까지 (`coreTop` / `coreBot`)

덩어리마다 **제일 진한 곳**에서 위아래로 걸어 나가며 진하기가 문턱 아래로 떨어지는
자리를 함께 잽니다. 그 값으로 위아래를 **항상** 잘라 냅니다 (조건 없음).

```
윗선   = max(덩어리의 시작, coreTop)
아랫선 = min(덩어리의 끝,  coreBot)
```

| | 문턱 | 왜 |
|---|---|---|
| 아랫선 `CORE_DROP` | **0.45** | 아래는 눈꺼풀·그늘이 눈썹만큼 진해서 세게 잘라야 한다 |
| 윗선 `CORE_UP` | **0.1** | 위는 번짐뿐이라 살짝만 자르면 된다 |

**⛔ `CORE_UP` 을 올리지 마세요.** 0.45 로 두고 원장님 사진을 재현하니 앞두께가
89 → 98 로, 원장님이 찍어 주신 87.7 에서 10px 멀어졌습니다.

**⚠️ 예외 하나 — 테두리로 그린 드로잉(`outline`).** 그때 판독 결과는 **위·아래 두 줄을
묶은 것**이라, 씨앗 한 줄의 핵심으로 자르면 아랫줄이 통째로 날아갑니다. 회귀 **89** 가
잡습니다 (실제로 예외를 빼고 돌렸더니 앞머리 149 vs 178 로 즉시 실패).

### 2-3. 바깥 — 머리카락 방어 (`trimOutside`)

창 천장에 닿은 열 · 끊어진 작은 조각 · 잉크가 옅은 바깥 열을 **끝에서부터** 버립니다.
자세한 것은 `BASELINE.md` 1-31.

### 2-4. 아치두께는 **꼬리보다 아래로 내려가지 않는다** (v1.81.0)

원장님 지시 2026-08-27 — 「**절대로** : 아치두께는 절대로 꼬리가 측정된 위치 밑으로 내려오지 않는다.
눈썹중 꼬리가 가장 낮은곳에 위치한다. 아치두께는 앞머리와 같은선 위치 하거나 높은곳에 위치한다」

```
아치두께  ≤  앞머리  ≤  꼬리        (y 는 아래로 갈수록 큼)
```

이 순서를 넘었다면 읽은 것은 눈썹이 아니라 **눈꺼풀 그늘**입니다. `autoFromDrawing()` 끝에서
`min(꼬리, 앞머리) − 3px` 로 상한을 겁니다.

**⚠️ 상한에 딱 붙이지 않습니다.** 붙이면 아치두께 = 앞머리가 되어 못박음 검사(회귀 131)가
「사진에서 읽은 값이 아니다」로 잡습니다 — 상한이 걸렸다는 것 자체가 판독이 흔들렸다는 뜻이므로
조금 위에 세웁니다. 회귀 **133** · **131**.

### 2-5. 자는 드로잉 위에 (`segPx`)

이너·아우터는 눈썹의 **양 끝**입니다. 자를 그 위에 가운데 맞추면 절반이 눈썹 밖 맨살로
뜹니다. 자는 세로선에서 **눈썹 쪽으로만** 뻗습니다 (길이는 그대로).
→ 세로선과 가로선이 만나는 자리가 그대로 **90° 꼭지점**이 됩니다.

### 2-6. ⭐⭐⭐ 이너 — **40 ~ 48 사이에서 「드로잉이 시작되는 선」** (v1.99.0)

> ⭐ **v2.4.0 — 맥시멈은 45 입니다** (원장님 확정 2026-08-29: 「**이너 45를 맥시멈으로
> 설정해라**」). 탐색과 맨살 기준은 48 쪽을 계속 쓰지만(48 너머가 맨살 골짜기라서),
> 드로잉이 45 를 넘게 그려져 있어도 **답은 45 로 자릅니다** (`INNER_F_SOFT` 클램프).
> 캡 전 실제 시작점은 `fRaw` 로 보존해 앞머리 훑는 열(2-7)이 씁니다 — 캡 자리로 열까지
> 옮기면 케이스 2·3·8 의 앞머리가 밀립니다 (실측). 회귀 **160**.

> 원장님 지시 2026-08-29 — **그대로 옮깁니다. 프롬프트로 읽으세요.**
>
> 「40이 눈 앞꼬리, 밸런스 기본이 되는 선. 48이 맥시멈, 각 나라마다 맥시멈 자리가 있다.
> 보통은 45가 맥시멈, 최대 48까지 늘어날 수 있다. **40에서 48까지 들어오면서 드로잉이
> 시작되는 굵은 혹은 검정색의 라인을 이너라인으로 선택한다.** 만약 이너라인을 선택할 수
> 없을 경우 40에서 45의 중간인 **43** 을 잡아낸다. 그러니 42이거나 43이거나 44일 수 있다.
> 42,43,44 중에 근접하게 검정 선이 있을 때 자동으로 선택해라. 자동으로 선택할 수 없을
> 경우 중간 43을 선택한다」
>
> 「이너라인을 선택하는 룰은 **가장 높은 값을 선택하는 게 아니다.** 사실 46은 거의 맨살,
> 45도 맨살 낮은 점수, 그리고 44에서 조금 더 높은 점수로 띄는 값이 이너라인의 선택지라고
> 볼 수 있다. **맨살에 쉐도우와 확연히 조금 더 짙은 선이 시작하는 부분, 맨살에서 「이곳에
> 선이 있다」라고 판단되는 점수가 확 띄는 지점**이 이너라인으로 선택될 수 있다」
>
> 「앱이 자기 안내문에 「콧방울·내안각 선에 맞추세요」 는 **잘못된 설명**, **「드로잉 앞부분에
> 맞추세요」** 가 맞다」 → 안내문 `tip_v2` 를 바꿨습니다.

#### ① 숫자 40·43·45·48 은 화면 눈금이 아니라 **얼굴 비율**입니다

원장님이 부르시는 숫자는 자동 정렬된 화면에서 이너 슬라이더가 가리키는 값(= `v2 × 100`)
입니다. 사진 프레임이 달라지면 그 숫자는 흔들리므로, 앱은 같은 뜻을 **얼굴에 붙은 자**로
저장합니다 — 「내안각 → 센터」 거리를 1.0 으로 본 비율 `f`:

```
f = (이너 − 내안각) / (센터 − 내안각)
```

기준 사진에서 내안각 = 40, 센터 = 53.15 이므로 **1 눈금 = 7.6%** 입니다.

| 원장님 숫자 | 뜻 | 상수 | `f` |
|---|---|---|---|
| **40** | 눈 앞꼬리(내안각) · 밸런스 기본선 · **하한** | `INNER_F_LO` | 0.000 |
| **43** | 40~45 의 중간 — **읽을 수 없을 때의 답** | `INNER_F_MID` | 0.228 |
| **44** | 기준 사진의 정답 (케이스 1) | — | 0.304 |
| **45** | 보통 맥시멈 | `INNER_F_SOFT` | 0.380 |
| **48** | 절대 맥시멈 — 이보다 안쪽으로 못 들어간다 | `INNER_F_HARD` | 0.608 |

**⛔ 이 상수를 화면 눈금(0.40 등)으로 되돌리지 마세요** — 프레임이 바뀌면 전부 틀립니다.

#### ② 왜 `growEnd`(2-1) 로는 안 되는가 — 재현으로 확인한 진짜 원인

`growEnd` 는 열마다 `columnRuns` 가 **스스로 찾은 제일 진한 덩어리**의 진하기를 봅니다.
눈썹 앞머리 안쪽에는 **눈꺼풀·눈두덩 그늘**이 이어져 있어, 눈썹이 끝난 뒤에도 그 덩어리의
진하기가 중앙값의 30% 대로 계속 남습니다.

| | 값 |
|---|---|
| 밴드(눈썹) 자체가 끝나는 자리 | **앱 44.0** ← 정답 |
| `growEnd` 가 걸어간 자리 | **앱 48.3** ← 미간 맨살 |
| 밴드 진하기 중앙값 | 34.2 → `INNER_DARK` 문턱 = 0.25 × 34.2 = **8.5** |
| 앱 42 ~ 49.5 열들의 진하기 | 10 ~ 13 — **전부 8.5 를 넘는다** |
| 그 열들의 두께 | **78 ~ 111px** — 진짜 눈썹(약 40px)의 **2~3배** |

**두께가 결정적 단서였습니다.** 진하기만으로는 눈썹과 그늘이 구분되지 않습니다.

#### ③ 새 판독 — 두께를 눈썹으로 **고정한 창**으로 다시 잰다 (`innerProfile`)

1. 밴드 **안쪽 40%** 열로 중심선(1차식)과 두께(중앙값)를 잡는다
2. 그 창을 **그대로 끌고** 안쪽으로 걸어가며 열마다 잉크를 잰다
3. 잉크 = 창에서 **제일 어두운 `INNER_CORE`(35%) 만의 평균**
   → ⚠️ 창 전체 평균을 쓰면 안 됩니다. 눈썹은 안쪽으로 갈수록 얇아지므로 진짜 선이
     맨살에 희석되어 「없다」가 됩니다 (정답 44 자리가 맨살 46 과 5%p 차이로 무너졌습니다)
4. **맨살 기준** = `f` 0.380 ~ 0.858 표본의 **낮은 15% 분위** (= 골짜기)
   → ⛔ 48 **너머**에서 재지 마세요. 미간·콧대 그늘과 반대쪽 눈썹이 있어 기준이 부풀고,
     진짜 선이 문턱을 못 넘습니다 (**1차 시도 실패: 이너 42**)
   → ⛔ 평균·중앙값도 쓰지 마세요. 드로잉을 45 너머까지 그린 사진에서 드로잉이 기준을 끌어올립니다
5. **눈썹 잉크 기준** = 밴드 가운데 50% 열의 중앙값 (= 100%)
6. **48 에서 40 방향으로 걸어 나오며** 처음 두 열 연속(`INNER_RUN`)으로
   `올라온 정도 ≥ INNER_RISE(0.12)` **그리고** `맨살의 INNER_MULT(1.28)배 이상` 인 자리를 찾는다
7. **답은 그 열이 아니라 바로 안쪽(맨살 쪽) 열** — 원장님의 「맨살에서 선이 있다고 판단되는
   지점」은 선의 **안쪽 경계**입니다. 통과한 열을 그대로 쓰면 한 눈금 바깥으로 밀립니다
8. 못 찾으면 **43** (케이스가 3개 이상이면 케이스 중앙값 · §7)

#### ④ 기준 사진 실측 (컨테이너 재현 · 눈썹 잉크 = 100%)

| 앱 눈금 | 잉크 | 올라온 정도 | 맨살 대비 | 판정 |
|---|---|---|---|---|
| 42.0 | 59% | 0.420 | 2.02배 | 선 |
| 43.0 | 45% | 0.219 | 1.53배 | 선 |
| **43.5** | 39% | **0.138** | **1.33배** | ← **처음 문턱을 넘는다** |
| **44.0** | 32% | 0.045 | 1.11배 | ← **그 바로 안쪽 = 정답 44** |
| 44.5 ~ 46 | 32~34% | ~0.05 | ~1.1배 | 맨살 |
| 46.5 ~ 47 | 28% | ~0 | 1.0배 | **맨살 바닥** |
| 48 이후 | 다시 올라감 | — | — | 미간·콧대 그늘 (기준으로 쓰면 안 됨) |

**결과: 앱 44.2 → 원장님 정답 44.** (고치기 전 48)

**⛔ `INNER_RISE` 를 0.10 아래로 내리지 마세요** — 45·46(맨살)의 잔물결까지 선으로 봅니다.
**⛔ `INNER_MULT` 를 2.0 같은 큰 값으로 올리지 마세요** — 어두운 핵심 평균이라 맨살도 0 이
아닙니다. 2.2 로 뒀을 때 **42 로 밀렸습니다**.

회귀 **151** 이 지킵니다 (눈꺼풀 그늘이 이어진 합성 사진 + 경계 상수 잠금 + 43 대체값).

### 2-7. ⭐⭐⭐ 앞머리 — **피부가 이어지다 처음 만나는 「두꺼운 검은 것」** (v2.1.0)

> 원장님 지시 2026-08-29 — **그대로 옮깁니다. 프롬프트로 읽으세요.**
>
> 「눈 윗부분부터 올라가서 이너 부분에 닿는 첫 검은 선.
>   눈 윗부분에서 올라가면 **피부색이 이어지다가 어느 한 지점에서 검은색으로 보이는
>   지점**이 앞머리다」

구현 (`frontDecide`) — 이너 세로선 자리에서 **세로로** 훑습니다:

```
① 눈(h1) 위로 FRONT_LASH_GAP(7%) 띄운 곳에서 시작 — 속눈썹·아이라인을 건너뛴다
② 이너에서 눈썹 몸통 방향으로 FRONT_COLS(5) 열, 열마다 아래→위로 걷는다
③ 「검은색」 = 그 열의 피부 ↔ 제일 어두운 값의 중간 (상대값만!)
④ 창 9줄 중 7줄이 어두워야 눈썹 — 얇은 검은 선(주름·아이라인)은 두께로 거른다
⑤ 검은 창 바로 앞(아래)에 피부가 FRONT_SKIN(5)줄 이상 이어져 있어야 인정
⑥ 열들의 중앙값 = 앞머리. 3열 미만이면 포기 → 기존 밴드 판독 유지 (조용한 안전판)
```

**만들면서 겪은 실패 셋 — 각각이 규칙 하나가 됐습니다:**

| 실패 | 증상 | 규칙 |
|---|---|---|
| 절대 문턱(피부−40)을 섞음 | 조명 어두운 사진에서 눈썹 절반이 문턱 위 → 8장 중 6장 실패 | ③ **상대값만** |
| 문턱만으로 거름 | 쌍꺼풀 주름(5px 검은 선)에 앞머리가 내려앉음 | ④ **두께 창** |
| 아무 데서나 출발 | 짙은 눈화장 사진 2장에서 섀도 덩어리를 앞머리로 읽음 | ⑤ **피부가 먼저** — 원장님 문장의 「피부색이 이어지다가」가 바로 이것 |

**실측 (케이스 8장):** 5장 판독 성공 — 전부 눈썹 앞부분 아랫선에 정확히 섬 (눈으로 확인).
3장(옅은 반영구·성근 눈썹·그늘 연결)은 포기 → 기존 밴드 판독 유지. 회귀 **155** 가 지킵니다.

⛔ ③④⑤ 어느 하나도 빼지 마세요 — 각각 실제로 겪은 실패의 방어입니다.

#### 앞머리 넘버링 — 대체값 (v2.1.1)

> 원장님 지시 2026-08-29 (폰 스크린샷의 빨간 선): 「빨간 선은 눈으로부터 올라와
> **대체값이 필요할 때 사용할 넘버링**, 파란색이 옳바른 (자리)」

이너의 43 과 같은 구조를 **세로**로 만들었습니다. 자도 같은 자입니다 —
「내안각→센터」의 1/13.15 이 1 눈금이고, **눈(동공 높이)에서 위로** 몇 눈금인지로 셉니다.

| | 값 | 근거 |
|---|---|---|
| 대체값 `FRONT_T_MID` | **눈 위 10.2 눈금** | 확정 7건 7.3·7.9·9.4·10.2·10.9·**11.0**·13.3 의 중앙값 (2·4·7 최종 확인 · v2.3.1) |
| 하한 `FRONT_T_LO` | **6** | 실측 최소 7.3 아래 여유. ⚠️ 예전 7·11.6 은 손 0 이 평균 1.3 눈금 낮았던 값 |
| 상한 `FRONT_T_HI` | 16 | 이보다 멀면 이마·머리카락을 읽은 것 |

쓰이는 자리 둘:
1. **판독 없는 시작 배치**(`placeLinesFromEyes`) — 동공 비율(0.78) 짐작을 폐지하고 넘버링으로
2. **판독 실패 시** — 밴드 값이 7~16 눈금 밖이면 눈꺼풀·이마를 읽은 것 → 11.7 로 대체.
   범위 안이면 밴드 값을 믿습니다 (테두리 드로잉처럼 밴드가 맞는 경우를 지키기 위해)

회귀 **156** 이 잠급니다.

#### 쌍꺼풀·주름 쉐도우 방어 — 후보를 전부 모아 넘버링으로 고른다 (v2.1.2)

> 원장님 지시 2026-08-29: 「2,4,7 판독 틀렸어. 그래 **쌍꺼풀, 주름 쉐도우를 방어**하도록
> 룰 추가하고, 판독이 어려울 경우 제시할 수 있는 **대체값·보통값**을 적용할 수 있도록
> 넘버링도 추가」

두꺼운 쉐도우는 두께 창(④)을 **통과합니다** — 얇지 않으니까요. 그래서 넘버링으로 거릅니다:

```
열을 끝까지 걸어 「두꺼운 검은 것」 후보를 전부 모은 뒤
  ① 눈 위 7~16 눈금 안의 가장 아래 후보 = 앞머리
  ② 그 범위에 없으면, 16 눈금을 넘지 않는 가장 아래 후보
  ③ 자(anchor)가 없으면 예전처럼 첫 후보
```

쌍꺼풀 주름·눈두덩 쉐도우는 눈 위 **7 눈금 안쪽**에 삽니다. 쉐도우(5눈금)와 눈썹(11눈금)이
둘 다 후보로 잡혀도 **눈썹을 고릅니다.** 회귀 **157** 이 잠급니다.

**앞머리 정답과 0 바로잡기 (2026-08-29):** 원장님 정답 4번=14·7번=9 는 **제 손 0(눈대중)**
기준이었습니다. 원장님이 4번에 동공 십자를 그려 0 오차(3.8 눈금)를 짚어 주셔서, 8장 전부
0 을 **검정 동공의 어둡기-가중 무게중심**으로 재실측하고 정답을 환산했습니다:

| 케이스 | 구눈금 답 | 손0 오차 | **바로잡은 눈금** |
|---|---|---|---|
| 4 | 14 | 3.8 | **10.2** |
| 7 | 9 | 1.7 | **7.3** |

**케이스 2·4·7 최종 확정 (2026-08-29, 확인 사진으로 재확인):**
2번 앞머리 **11**(12 아님) · 앞두께 **17** (두께 6.0) — 앱 판독 11.1, 차이 **0.1** ✓ ·
4번 **10.2** ✓ · 7번 **7.3** ✓.
확정 7건: 7.3 · 7.9 · 9.4 · 10.2 · 10.9 · 11.0 · 13.3 → **보통값 10.2 · 하한 6**.

#### ⭐⭐⭐ 하한은 **절대 규칙** (v2.1.3)

> 원장님 지시 2026-08-29 (실제 폰에서 앞머리가 눈꺼풀에 내려앉은 것을 보시고):
> 「**어느 넘버 이하는 앞머리로 측정하지 않는다**가 있어야 한다.
>   판독이 애매한 경우에도 위 사진과 같은 **말도 안 되는 위치**에 있으면 안 된다」

v2.1.2 에는 구멍이 있었습니다 — 범위(7~16) 안에 후보가 없으면 **낮은 후보라도** 썼습니다.
그 구멍으로 폰에서 앞머리가 눈꺼풀에 섰습니다. 이제:

1. **7 눈금 미만은 후보 자격이 없습니다** — 범위 안에 아무것도 없으면 그 열은 포기
2. **최종 하한 집행**(`frontFloor`) — 판독·밴드·이전 값 어떤 경로로 왔든, 마지막 앞머리가
   눈 위 7 눈금 미만이면 **보통값 11.7 로 대체**합니다
3. 상한(16)은 집행하지 않습니다 — 크게 확대한 사진에서는 눈썹이 정당하게 16 을 넘습니다.
   위쪽 오독은 후보 선택(≤16)이 막습니다

회귀 **158** 이 잠급니다 (3눈금→11.7 대체 · 10눈금 유지 · 18눈금 유지).

### 2-8. ⭐⭐⭐ 앞두께 — **「검은선에서 피부색이 나오는 지점」** (v2.3.0 · 원장님 확정)

> 원장님 2026-08-29: 「선으로 앞머리부터 올려서 **검은선에서 피부색이 나오는 지점**이
> 앞두께 지점이다」 · 「앞두께 다 틀린데」(5장 핑크 표시) · 「흰선 안 맞아, 17.5가 맞지」

**v2.2.0 의 실수** — 「짙은 검정이 끝나는 곳」으로 만들었더니 5장 중 4장이 1~2 눈금
**낮게** 잡혔습니다. 옅어진 윗털·파우더 그라데이션도 눈썹입니다. **피부가 나와야 끝**입니다.

판독: 앞머리 덩어리에서 계속 올라가되, 짙은 문턱이 끝나도 **연한 문턱**(`FT_SOFT` —
피부↔최암부 차의 25%)을 넘는 동안 계속 올라가고, **피부가 `FT_SKIN`(4)줄 연속**되면 끝.

**넘버링(두께 가드)** — 원장님 확정 5건으로 재확정:

| | 값 | 근거 |
|---|---|---|
| 보통값 `FT_T_MID` | **6.0 눈금** | **원장님 확정** 1번 4.8 · 2번 6.0 · 3번 7 · 6번 6 · 8번 7 의 중앙값 |
| 최소 `FT_T_MIN` | 3 | 확정 최소 4.8 아래 여유 |
| 최대 `FT_T_MAX` | 9 | 이보다 두꺼우면 이마 그늘·머리카락까지 읽은 것 |

새 룰 성적 (확정 정답 대비): 1번 0.8 · 2번 0.4 · 3번 2.1 · 6번 1.0 · 8번 **0.0** —
평균 오차 1.6 → **0.86 눈금**. 남은 과제: 3번(결 눈썹)의 성근 윗털은 연한 문턱도 못 따라감.
`ftGuard()` 가 마지막에 3~9 범위를 검사 — 밖이면 6.0 으로 대체. 회귀 **155**.

#### ⭐⭐⭐ v2.4.0 — 앞두께 **우선순위** (원장님 확정 2026-08-29)

> 「**두께 보통값 6.0 이 중요한 게 아니다.** ① 반드시 사진에서 **픽셀 검증**으로 —
> 앞머리에서 위로 올라가며 검은색에서 피부색이 나오는 부분의 **검은 마지막 끝부분**이
> 앞두께다. ② 앞두께 피부색이 나온 부분이 **명확하지 않을 때** 색상의 **퍼센트지가
> 낮아지는 부분**을 선택해야 한다」

| 우선순위 | 언제 | 무엇을 고르나 |
|---|---|---|
| **①** 픽셀 검증 | 피부가 `FT_SKIN`(4)줄 연속으로 나와 **명확**할 때 | 피부 직전의 **검은 마지막 끝부분** (기존 걷기의 `last`) |
| **②** 퍼센트 하락 | 피부 복귀를 못 찾았거나, ①의 두께가 3~9 눈금 밖일 때 | 열의 어둡기 퍼센트(피부 0%·최암부 100%, 3줄 평활)가 **가장 크게 낮아지는 자리** (`FT_P2_MIN` 0.15 이상 하락) |
| **③** 보통값 6.0 | ①②가 모두 실패해 두께가 여전히 3~9 밖일 때 (`ftGuard`) | **최후 안전판** — 말도 안 되는 자리 방지용일 뿐 |

확정 8장 실측: ① 이 전부 명확해 ② 는 발동하지 않았고 **8장 모두 v2.3.1 과 동일**
(순수 안전망 추가 — 성적 저하 없음). 회귀 **161** 이 ② 를 합성 사진으로 잡습니다:
그라데이션이 길어 피부-복귀 두께가 12.7 눈금이 되는 사진에서 ② 가 검정 끝을 고른다.
⛔ ② 를 빼고 바로 6.0 으로 가지 마세요.

### 2-9. ⭐⭐⭐ 아치엣지·아치두께 — **앞머리와 같은 방법** (v2.5.0 · 원장님 지시 2026-08-29)

> 「앞머리, 앞두께 자동 위치조정 프로그래밍과 **같은 방법으로** 아치엣지 아치두께 고도화해라」

2-7(앞머리)·2-8(앞두께)의 세로 판독을 **공용 함수 `darkBlobsUp`** 으로 꺼내, 아치가 그 함수를
그대로 씁니다. ⛔ 같은 규칙을 두 군데서 키우지 마세요.

```
① 눈(0=동공 중심) 위 7% 에서 출발 — 속눈썹·아이라인 건너뜀
② 산꼭대기 좌우 5열, 열마다 아래 → 위로 걷는다        ← 앞머리는 이너 자리 5열
③ 「검은색」 = 그 열의 피부 ↔ 최암부의 중간 (상대값만)
④ 창 9줄 중 7줄이 어두워야 눈썹 (얇은 주름 방어)
⑤ 검은 창 **바로 앞에 피부가 5줄** 이어져야 인정 (눈화장 방어)
⑥ 후보를 전부 모은 뒤 — 눈 위 6~26 눈금 · **앞머리보다 위**인 가장 아래 후보
⑦ 아랫끝 = 아치두께 · 윗끝 = 아치엣지 (윗끝은 2-8 의 우선순위 ①피부복귀 → ②퍼센트하락)
⑧ 3열 미만이거나 창 천장에 닿으면(ceil) 포기 → **밴드 판독 그대로**
```

**아치에만 있는 두 규칙 — 둘 다 이 프로젝트가 이미 겪은 실패의 방어입니다**

| 규칙 | 막는 것 |
|---|---|
| 훑는 자리는 **산꼭대기**(밴드 봉우리) | 아치선(v6, v3.0.0부터 → 2-11)은 산꼭대기에서 **바깥으로 최소 3칸 이상** 떨어진 자리라 봉우리 바깥 — 거기서 재면 산이 아닌 자리를 잰다 |
| 윗끝이 **창 천장에 닿으면 포기** | 크게 확대한 사진에서 눈썹이 창 위로 나가면 윗끝이 창 천장에 **못박힌다**(3장) |

### ⭐⭐⭐ 2-10. 아치 **표준값** — 못 읽었을 때 (v2.6.1 · 원장님 지시·확정 2026-08-29)

> 「1번 판독 실패시 표준값 : 아치두께는 앞머리 측정값에서 **3칸 위로**,
>   아치엣지는 아치두께 위치에서 **6칸 위로** 측정한다」

v2.5.0 까지는 못 읽으면 **밴드값이 그대로** 남았습니다. 실제 고객 사진 5장을 넘버링으로 표기해
원장님 판정을 받으니, 판독이 성공한 3장(2·3·4번)은 맞았고 **판독이 포기한 1번만 틀렸습니다** —
밴드값이 눈썹 위로 4.9 눈금 떠 있었습니다. 그래서 표준값이 생겼습니다.

```
아치두께 = 앞머리 − 3 눈금
아치엣지 = 아치두께 − 5 눈금      (= 앞머리 − 8 눈금)
```

**⚠️ 아무 때나 쓰지 않습니다 — 실패 이유로 갈립니다.**

| 실패 이유 | 뜻 | 하는 일 |
|---|---|---|
| 산꼭대기에 두꺼운 검은 것이 **아예 없음** | 테두리만 그린 드로잉 · 저대비 맨 눈썹 | **밴드값 그대로** (드로잉이 정답) |
| 뭔가 보이는데 **열들이 서로 다른 말** | 머리카락 · 그늘 | **표준값** |

⛔ 이 갈림을 지우면 원장님이 그려 놓은 **드로잉까지 표준값이 덮습니다** — 실제로 지워 보니
회귀 89·94 의 아치두께가 140 → 159·156 으로 떨어졌습니다.

**⚠️ 검증 상태 — 아직 확인 못 한 것 두 가지**

1. (해결) 처음 지시 6칸 → 21.4. **원장님 재확인으로 5칸 확정** → 1번 20.4 (정답 20).
2. **5번 아치두께 8.5 → 10.5 는 아직 못 고쳤습니다.** 표준값 문제가 아니라, 판독이 눈썹 아래
   **성긴 잔털**까지 눈썹으로 세는 문제입니다. 같은 문턱으로 2·3·4번이 맞았으므로 문턱을
   낮추면 맞던 것이 틀어집니다 — 사례를 더 받아 고쳐야 합니다.

회귀 **163** 이 산수와 `seen` 세기를, 회귀 **89·94** 가 갈림을 잡습니다(돌연변이 검사 완료).

#### ⭐⭐⭐ v2.7.0 — 아치엣지만 잡힌 경우 (원장님 지시 2026-08-29)

> 「아치엣지가 잡힌다 + 아치두께 안 잡히는 경우 = **아치엣지에서 5칸 아래** 위치한다 대체값」

「아치두께가 안 잡힌다」 = **아랫끝이 넘버링 하한(6 눈금) 아래로 새어 내려갔다** (눈꺼풀·
속눈썹까지 읽음). 그때 윗끝이 멀쩡하면 윗끝은 쓰고 두께만 `아치엣지 + 5 눈금(아래)`.

⛔ **해부학 순서 위반은 이 길로 보내지 마세요** — 회귀 162ⓒ 가 잡습니다.

⚠️ **5번 사진은 이걸로 안 고쳐집니다.** 5번 아랫끝은 8.5 눈금으로 하한 위라 「잡힌」 것으로
판정됩니다 — 못 잡은 게 아니라 잔털까지 세어 **낮게 잡힌** 것입니다. 상한을 걸면 맞다고 하신
2·3·4번이 0.2~0.5 씩 움직입니다. 사례가 더 필요합니다. 회귀 **164**.

### ⭐⭐⭐ 2-11. 아치선(v6) — **「꺾임점」 폐지, 아치엣지-피부 경계로 교체** (v3.0.0 · 원장님 지시·확정 2026-08-30)

> 「파란색이 내가 판단하는 아치선이다. 아치엣지가로를 아치선이 맞닿을때 생기는 피부색이
>  생기는 위치가 아치선인데, 그것이 판단불가시 각 눈위치의 눈꼬리 윗쪽으로 위치하는것이
>  표준값이 되겠다」
> 「아치가 찾아지지 않을경우 절대 하면 안되는 위치: 3개의 칸 내부로 들어오지 않는다.
>  무조건 3개이후부터 찾거나, 찾아지지 않을경우 3개칸이후 있는 눈꼬리 윗부분으로 정한다」

1-34·2-9 의 「꺾임점」(`KINK_DROP`, 낙차의 15%) 은 **폐지**되었습니다. 실제 사진 4장에 원장님이
파란 세로선으로 직접 표시해 확인한 결과, 꺾임점이 잡던 자리와 다른 자리를 가리켰습니다.

```
① 기준 = 산꼭대기 자체의 높이 (smoothTop(pk)) — archDecide 의 h2 가 아니라
   band(seq/smoothTop) 안에서 자기 자신과만 비교한다 (h2 와 1~2px 어긋나면 다음 열을
   「벌써 피부」로 잘못 읽는다)
② 여유(ARCHV_EPS) = 산꼭대기~꼬리 낙차의 5% (최소 1.5px) — 꺾임점(15%)보다 훨씬 작다.
   「다 꺾였다」가 아니라 「막 피부가 비치기 시작했다」를 잡기 때문
③ 버팀(ARCHV_HOLD=5) — 그 문턱을 5열 연속 넘어야 확정. **연속 열의 높이가 계속
   늘어나야 한다**(작은 흔들림은 tol=0.5 봐줌) — 결 때문에 잠깐 튀었다 도로 가라앉는
   「가짜 상승」에 낚이지 않기 위해
④ 3~4칸 마지노선 (v3.1.0) — 산꼭대기에서 실제 화면 눈금 거리로 최소 3~4칸(눈썹 길이에
   따라 선형, 아래 참고) 안쪽은 후보 자격이 아예 없다. ②③이 다 뚫려도 이게 마지막 방어선
⑤ 그래도 못 찾으면 → eyeArchRange().corner(눈꼬리) 표준값 그대로. **v3.1.0부터 ④ 밖으로
   억지로 밀지 않는다** — 눈꼬리는 랜드마크 실측값이라 그 자체가 신뢰할 자리
```

**v3.1.0 — 마지노선(④)이 눈썹 길이에 따라 3~4칸으로 늘어난다** (원장님 지시 2026-08-30:
「사진이 늘어나거나 눈썹이 길어지면 3, 3.5, 최대 4칸까지 마지노선. 4칸 전에 눈꼬리가
3·3.5·4에 걸리면 눈꼬리를 마지노선 쪽으로 더 밀어내지 않는다」).

```
browSpanUnits = |산꼭대기 x − 꼬리쪽 끝 x| / frontTickPx()   ← 눈금 단위라 확대율 무관
browSpanUnits ≤ 12칸  → 마지노선 3칸
browSpanUnits ≥ 20칸  → 마지노선 4칸
그 사이               → 3~4칸 선형 보간
```

⚠️ **12·20 경계는 아직 미검증**입니다. 실제 사진 4장은 전부 10.4~14.75칸이라 마지노선이
거의 3칸 그대로였습니다(회귀 174/174 재확인, 4장 값 변화 없음) — 실제로 긴 눈썹 사진을
받아 20칸 근처가 맞는지 확인해야 합니다. 사례가 더 필요합니다.

⛔ **눈꼬리 표준값(⑤)을 마지노선 밖으로 강제 이동시키지 마세요** — v3.0.0에서 한 번
넣었다가 원장님 지시로 v3.1.0에서 도로 뺐습니다. 마지노선은 ①의 픽셀 판독 후보를
거르는 자이지, 랜드마크로 실측한 눈꼬리까지 밀어낼 이유가 없습니다.

---

**⛔⛔ v3.2.0 — 열 순서(seq)의 방향은 「센터에 가까운 끝이 앞머리」** (실기기 사진 2026-08-30)

위 ①~⑤가 전부 맞아도, **열 순서가 거꾸로 서면** 탐색이 꼬리 쪽이 아니라 앞머리 쪽으로
걸어가 아치선이 눈썹 몸통 한가운데에 섭니다. 원장님 실기기에서 실제로 났습니다 —
아치선이 산꼭대기보다 안쪽 num ≈ 35.

```
예전:  seq = pts[0].x > cx ? pts : reverse(pts)      ← 끝점 하나만 봄
지금:  seq = seqOrient(pts, cx)                       ← 센터에 가까운 끝이 seq[0]
       |pts[0].x − cx| < |pts[끝].x − cx| ? pts : reverse(pts)
```

화면 오른쪽 눈썹에서 읽힌 열이 **센터를 하나라도 넘으면**(미간 잔털·그늘) 예전 식은
`pts[0].x < cx` 가 되어 판정이 뒤집혔습니다. 이 사진은 미간 잔털이 센터에서 **3.3칸**
거리까지 읽혔습니다. 예비 경로(`fallbackBox`)는 상자를 센터에서 자르지만 **랜드마크
경로(`browBoxes`)에는 그 잘라내기가 없어** 실기기에서만 났습니다.

⛔ 끝점 하나(`pts[0].x > cx`)로 되돌리지 마세요 — 회귀 **175** 가 잡습니다.
`growEnd` 도 같은 잣대를 씁니다. 두 곳이 다른 잣대를 쓰면 또 갈립니다.

**⭐ v3.2.0 — ⑤ 다음의 최후 표준값(⑥) 신설.** 픽셀 판독도 실패하고 눈꼬리 랜드마크도
없으면 예전에는 **아무것도 안 해서** 임시로 놓아 둔 **산꼭대기 값(0칸)** 이 그대로
남았습니다 — 「3개의 칸 내부로 들어오지 않는다」를 가장 크게 어기는 자리입니다.
이제 **산꼭대기 + 마지노선(3~4칸)** 으로 내보냅니다. 실측값이 아예 없을 때의 표준값이라
⑤(눈꼬리 실측)를 밀지 않는다는 원칙과 충돌하지 않습니다. 회귀 **176** 이 잠급니다.

**⭐ `S.archRead`** — 무엇을 보고 그 자리에 섰는지 남깁니다(`from`: pixel/corner/floor ·
`pkX` · `floorUnits` · `browSpanUnits`). 이너의 `S.innerRead` 와 같은 방식. ⛔ 지우지 마세요.

**⛔⛔ v3.3.0 — v3.2.0 으로도 실기기 증상은 안 고쳐졌습니다. 뒤집힘은 원인이 아니었습니다.**

원장님이 폰에서 v3.2.0 을 확인(홈 화면 표기)하고 「AI 눈썹정렬 적용됨」이 뜬 상태로 보내 주신
화면을 원본과 화소 정합해 재니 아치선 **34.46** — v3.1.0 과 사실상 동일. 이너 43.88 · 꼬리 17.37 은
컨테이너(44.44 / 16.99)와 맞는데 아치선만 10칸 어긋납니다. 두 번째 사진에서도 34 가 나왔습니다.
그 사진 윤곽으로 「뒤집혔을 때」를 계산하면 29.9 여야 하므로 **뒤집힘 가설은 기각**입니다
(첫 사진에서 35.08 이 맞은 것은 우연 — 랜드마크 눈썹 산 자리와 겹쳤습니다).

남은 단서: 아치엣지가 `archDecide` 결과로 보이므로 `autoFromDrawing` 은 v6 블록까지 도달했고,
그렇다면 **실기기의 `pk`(산꼭대기)가 num 37~38** — 실제(26.4)와 11칸 어긋납니다. 즉
**랜드마크 경로에서 열의 윗선(top) 판독이 망가진 것**으로 좁혀집니다. ⏸ 컨테이너는 MediaPipe 가
막혀 재현 불가 — 폰에서 갈래를 읽으려고 알림에 `[v6:pixel pk38]` 꼬리표를 붙였습니다.

❌ **v3.3.0 에서 시도했다가 접은 것 — 「꼬리~이너 15~40% 띠」 안전판** (원장님 지시:
「아치선 시스템을 삭제·잠금하고 초록을 만든 방법을 새 룰로」). 어떤 경로로 놓였든 그 띠를
벗어나면 표준값 26%로 놓는 안. **회귀 91 이 잡았습니다** — 모양 B(아치가 안쪽 x260)의 확정값
234 는 이 띠로 **52%** 라 178 로 끌려갔습니다. 15~40% 는 모양 A 의 성질이지 보편 규칙이
아닙니다. ⛔ 띠를 넓혀 통과시키지 마세요.

**⭐⭐ v3.3.1 — 넘버링 상한선 (원장님 확정 2026-08-30)**

```
픽셀 판독(①~④) 결과의 넘버링이 15~31 을 벗어나면 → 버린다
  → ⑤ 눈꼬리 랜드마크 (실측이라 표준값보다 정확)
  → ⑥ 표준값 넘버링 23 (확정 4장 중앙값 22.83 ≒ 원장님 23)
  → 눈금 자 없으면 산꼭대기 + 마지노선
```

근거: 확정 4장 17.65~25.46 · 실기기 오류값 34~35.4 · 회귀 91 모양 B 확정 지점 30.46
(그래서 상한이 30 이 아니라 31). ⛔ 34 이상으로 넓히지 마세요. 회귀 177 이 잠급니다.
접은 「눈 구간」·「15~40% 띠」와 달리 **넘버링 절대값**이라 모양 B 를 다치지 않습니다.

**⭐ v3.3.1 — 점선 진단(`showArchDots`)**: 눈썹정렬 시 읽은 윗선을 파란 점(산꼭대기 주황·
아치선 초록)으로 8초 표시. 실기기 점선 vs 컨테이너 점선 비교용. 저장본에는 안 찍힘.

❌ **시도했다가 접은 것 — 눈 구간으로 픽셀 판독 검증하기** (원장님 질문 2026-08-30:
「눈꼬리 랜드마크 설정하는것도 좋은 방법이니?」). `eyeArchRange().a`(눈동자 바깥 끝)보다
안쪽으로 들어온 픽셀 판독을 버리는 안. **회귀 91 이 바로 잡았습니다** — 모양 B(아치가
안쪽 x260에 있는 눈썹)의 정상 판독 234 를 「너무 안쪽」으로 오판해 눈꼬리 150 으로
버렸습니다. 눈 구간은 사람마다 눈썹-눈 배치가 달라 판독을 감시할 만큼 촘촘한 자가
못 됩니다. ⛔ 문턱만 느슨하게 해서 되살리지 마세요.

**왜 seq/smoothTop 을 다시 쓰는가** — 2-9 의 `darkBlobsUp` 으로 새로 훑으면 테두리만 그린
드로잉(회귀 89)·저대비 맨눈썹(회귀 94)의 속 빈 부분을 잘못 읽습니다. seq/smoothTop 은
`readDrawing`이 이미 outline·low-contrast 를 검증해 놓은 자료라 그대로 재사용합니다.

**실측 (4장, 2026-08-30 확인)**

| 케이스 | 아치선(v6) | 상태 |
|---|---|---|
| A(구5번) | 25.46 | 원장님 확정 — 「3번째 칸으로 조정한 것 인정」 |
| B(구3번) | 17.65 | 원장님 확정 — 맞음 |
| C(구2번) | 20.56 | 원장님 확정 — 맞음 |
| D(구4번) | 25.1 | 원장님 확정 — 맞음 |

⛔ **꺾임점(KINK_DROP)을 되살리지 마세요** — 회귀 **121**이 새 규칙 기준(바깥 15~40% 밴드,
같은 수치 그대로 유지)으로 이미 바뀌었습니다. 1·2·3(`eyeArchRange`, 2-10 위 인용문의
①②③)은 여전히 **④가 못 찾았을 때만** 쓰는 대체 자리입니다.

---

## 7. 드로잉 케이스 보관함 (`INNER_CASES`)

> 원장님 지시 2026-08-29 — 「이제 **드로잉 케이스마다 너가 저장**하고, 여러 케이스에서
> **가장 잘 사용되는 것**, 판독을 자세히 측정할 수 없을 때 **가장 비슷하다고 생각되는 지점**을
> 선택하도록 룰을 만들어야 한다」

**⛔ 사진은 저장하지 않습니다.** 이 저장소는 **공개**이고 고객 얼굴이기 때문입니다.
남기는 것은 **숫자뿐**입니다 — 얼굴 비율 `f` 와 그때의 점수.

### ⚠️ 0. 먼저 — **내안각(40)의 정의** (원장님 확인 2026-08-29)

> 「위·아래 눈꺼풀이 만나는 **코쪽 끝점**」 = 눈물샘(caruncle)의 **안쪽 끝**.
> 눈물샘이 **시작하는** 자리가 아닙니다.

원장님 지적: 「너는 눈앞꼬리를 40으로 잡는데, 각 사진마다 눈 앞꼬리를 **41 혹은 43** 으로
잡으니 대체값이 항상 변하게 되고 그러면 더 형편없는 대체값이 된다. **너의 눈앞꼬리
넘버링을 먼저 고도화 해라**」

실제로 재 보니 두 자리는 사진마다 **6~14px** 차이가 났고, 1 눈금이 4~15px 이므로
눈금 전체가 **최대 3칸** 어긋나 있었습니다. 이 한 가지 때문에 그 전에 기록한 케이스
숫자가 전부 틀렸습니다 — **2026-08-29 에 8건을 전부 다시 쟀습니다.**

앱은 MediaPipe 랜드마크 **133 / 362** 를 씁니다 — 그 점이 바로 이 정의의 자리라서,
앱 안에서는 처음부터 맞고 있었습니다. 틀린 것은 **컨테이너에서 손으로 잰 제 측정**이었습니다.
⛔ 새 케이스를 잴 때는 반드시 **눈 안쪽 끝을 확대해서** 이 점을 찍으세요.

### ⚠️ 0-2. **눈 앞꼬리는 언제나 40** (v2.0.0 · 원장님 지시 2026-08-29)

> 「눈 앞꼬리를 항상 40으로 잡아야 하는거 아니니?」

그렇지 않았습니다. 자동 정렬이 **동공 간격** 기준이라 사람마다 내안각이
**37.7~41.3 눈금**(폭 3.6)에서 흔들렸습니다. 두 겹으로 고쳤습니다:

1. **자동 정렬의 자 = 내안각 간격** (`INNER_FRAC 0.263`) — 1 눈금 = 화면 1%
2. **세로선 눈금 = 얼굴 기준** — 왼쪽 내안각 40 · 센터 53.15 · 오른쪽 내안각 66.3

이제 어떤 고객·어떤 기기에서도 **40 = 눈 앞꼬리, 48 = 하드 맥시멈**이 똑같이 맞습니다.
자세한 것은 `BASELINE.md` **1-64**. 회귀 **153** 이 지킵니다.

⚠️ **아래 케이스 표의 숫자는 이 고침 전에 잰 것입니다.** 앱 판독 열은 v2.0.0 에서
다시 재야 합니다 — 정답 열(원장님이 짚어 주신 자리)은 얼굴에 붙은 값이라 그대로입니다.

### ⚠️ 0-4. **넘버링의 0 자리 동일화** (v2.2.2)

> 원장님 지시 2026-08-29: 「너의 0 자리도 앞머리 고도화와 마찬가지 오류로 **사진마다
> 다 다르다.** 그러니 보통값 11.7도 이상한 자리가 된다. **0 자리 동일화 프롬포트 정해라**」

정의: **0 = 동공(눈동자) 중심의 높이.** 눈대중이 아니라 시스템 측정값입니다:

```
① 랜드마크가 있으면 홍채 중심(468~477)의 평균 — 매번 실측
② 없으면 배치 때 저장한 동공 높이 (S.eyeZero — 정렬이 맞춘 자리)
③ 그것도 없으면 눈 가로선(h1) — 최후의 수단
```

⛔ **h1 을 0 으로 쓰지 마세요** — h1 은 원장님이 드래그로 옮길 수 있는 선이라, 옮기는
순간 하한 7·보통값 11.6·두께 4.7 이 전부 따라 밀립니다. 0 은 얼굴에 붙어 있어야 합니다.
⛔ **넘버링 사진을 만들 때도 동공 중심을 측정해서 0 을 놓으세요** — 손으로 찍은 0 이
사진 3장에서 눈 위·눈 중앙·눈 아래로 제각각이었던 것이 이 규칙이 생긴 이유입니다.
측정법: 눈 상자 안 **가장 어두운 6% 픽셀의 어둡기-가중 무게중심** = 검정 동공 중심.
원장님이 4번에 그려 주신 십자와 일치함을 확인했습니다 (2026-08-29).
회귀 **159** 가 잠급니다.

### ⚠️ 0-3. **눈 앞꼬리는 시스템이 스스로 찾습니다** (v2.0.1)

> 원장님 지시 2026-08-29: 「시스템 내부에서 눈금자 이용하여 눈 앞꼬리를 **자동으로
> 인식**하라. 그게 AI가 하는 일이다」

원리 — 0장의 정의를 픽셀로 옮긴 것: **눈꺼풀 틈의 세로 높이가 코 쪽에서 0 이 되는 자리.**
동공에서 코 쪽으로 걸어가며 틈 높이가 최대의 30% 아래로 3열 연속 떨어지면 거기가 앞꼬리입니다.
원장님 확인 사진 4장 8점 기준 **평균 0.58 · 최대 0.94 눈금**. 자세한 것은 `BASELINE.md` **1-65**.

⛔ **손으로 짚지 마세요.** 제가 손으로 짚던 값이 1.2~3.3 눈금 흔들려 케이스 표 전체가
틀어졌습니다 (원장님 지적: 「너가 잡은 눈 앞꼬리가 40이 달라서 넘버링이 달라진다」).

⚠️ 아직 **씨앗(눈 한가운데)** 을 스스로 찾는 것은 안 됩니다. 앱에서는 랜드마크·정렬된 동공이
씨앗이라 문제없지만, 얼굴 인식이 없는 곳에서는 아이라인·속눈썹에 끌립니다. 다음 과제입니다.

### 케이스 표 — **확정** (2026-08-29 · 원장님 「맞다 이대로 넣어라」)

| # | 눈썹 종류 | 원장님 정답 | 앱(v2.0.1) | `f` | 차이 | 확인 |
|---|---|---|---|---|---|---|
| 1 | 드로잉(굵은 선) | **45.4** | 45.1 | 0.411 | 0.3 ✅ | |
| 2 | 짙은 파우더 반영구 | **47.3** | 48.0 | 0.555 | 0.7 ✅ | |
| 3 | 자연 결 눈썹 | **46.0** | 46.7 | 0.456 | 0.7 ✅ | |
| 4 | 옅은 반영구 | **42.6** | 40.4 | 0.198 | 2.2 ⚠️ | |
| 5 | 옅은 자연 ① | **41.8** | 43.0 | 0.137 | 1.2 ⚠️ | ⭐ |
| 6 | 옅은 자연 ② | **42.4** | 42.2 | 0.183 | 0.2 ✅ | ⭐ |
| 7 | 옅은 자연 ③ | **41.9** | 47.2 | 0.144 | **5.3 ❌** | ⭐ |
| 8 | 옅은 자연 ④ | **45.1** | 45.4 | 0.388 | 0.3 ✅ | ⭐ |

**평균 오차 1.36 눈금 · 8건 중 5건이 1 눈금 이내.**

⭐ = 원장님이 **앞꼬리 자리까지 확대해서 확인**해 주신 케이스입니다
(「빨간 선이 맞다」 → 「맞다 이대로 넣어라」). **검출기 검증의 정답지**로 씁니다 —
자동 인식(0-3)은 이 4장 8점에서 평균 0.58 · 최대 0.94 눈금입니다.

⚠️ `f` 는 **얼굴에 붙은 값**이라 앱을 고쳐도 그대로입니다. 앱을 고치면 **app 열만** 다시 잽니다.

### ❌ 시도했다가 접은 것 — 「털의 결(texture)」로 읽기

원장님 힌트를 따라, 평균 어둡기 대신 **창 안 세로 밝기차의 합**(털은 결이 있고 그늘은
매끈하다)으로 프로파일을 다시 재 봤습니다. 결과가 반으로 갈렸습니다:

| 케이스 | 정답 | 결 프로파일 |
|---|---|---|
| 1 | 45.4 | 44 → 51%, **45 → 30%** — 계단이 **정답 자리에 정확히** 생김 ✅ |
| 8 | 45.1 | 44 → 43%, **45 → 31%** — 역시 정답 자리 ✅ |
| 4 | 42.6 | 41→18 · 42→45 · 43→20 · 45→65 — **들쭉날쭉, 계단 없음** ❌ |
| 7 | 41.9 | 39~50 전 구간 50~100% — **떨어지지 않음** ❌ |

**왜 안 되는가.** 케이스 7은 눈꺼풀 주름이 깊은 눈이라 **주름에도 결이 있습니다.**
케이스 4는 옅은 반영구라 털 결 자체가 약합니다. 즉 결은 **잘 되는 사진에서만 잘 됩니다** —
이미 어둡기로도 맞히던 1·8 에서만 통했고, 정작 못 맞히던 4·7 은 그대로였습니다.
→ **넣지 않았습니다.** 표본 2장에 맞춰 넣으면 나머지가 깨집니다.
⛔ 다음에 다시 시도한다면 **케이스 4·7 부터** 확인하세요. 이 표가 그 증거입니다.

### 규칙 — 대체값은 언제나 43

> 원장님 2026-08-29: 「44가 맞다 하지만 **판독 안될 경우 43도 괜찮아 보인다**」
> 다만 대체값 재검토는 **보류**입니다 — 원장님: 「너의 눈 앞꼬리 판단 오류로 인해 43
> 대체값을 적용할 수 없다 … 너의 눈앞꼬리 넘버링을 먼저 고도화 해라」
> → 넘버링은 위 0장에서 고쳤습니다. 대체값을 44로 올릴지는 **다음 케이스로 다시 판단**합니다.

- 못 읽으면 **항상 43**(`INNER_F_MID`). 케이스가 몇 개든 바뀌지 않습니다.
- ⛔ **케이스 중앙값을 대체값으로 쓰지 마세요.** `INNER_CASES` 는 **기록·검증표**입니다.

**⛔ 사진은 저장하지 않습니다.** 이 저장소는 **공개**이고 고객 얼굴이기 때문입니다.
남기는 것은 **숫자뿐**입니다 — 얼굴 비율 `f` 와 그때의 점수.

| # | 날짜 | 눈썹 종류 | 원장님 정답 | 앱 판독 | `f` | 차이 |
|---|---|---|---|---|---|---|
| 1 | 08-29 | 드로잉(굵은 선) | **44** | 44.2 | 0.304 | **0.2** ✅ |
| 2 | 08-29 | 짙은 파우더 반영구 | **48** | 48.0 | 0.608 | **0.0** ✅ |
| 3 | 08-29 | 자연 결 눈썹 | **47** | 47.6 | 0.532 | **0.6** ✅ |
| 4 | 08-29 | 옅은 반영구 | **44** | 판독 실패 → 43 | 0.304 | **1.0** ⚠️ |

**이 표가 알려 준 것**

- **48 이 드문 값이 아닙니다.** 짙은 반영구는 잉크가 하드 맥시멈까지 실제로 갑니다
  (케이스 2). 「보통 42~44」는 케이스 1 하나만 보고 세운 인상이었습니다 —
  `INNER_F_SOFT`(45) 를 상한처럼 쓰지 마세요. **하한 40 · 상한 48 이 진짜 경계입니다.**
- **옅은 반영구는 아직 못 읽습니다** (케이스 4). 판독은 대비 18 로 「제일 진한 심지」만
  잡아 창이 18px 로 얇아지고, 그 창이 중심선을 조금만 벗어나면 눈썹을 놓칩니다.
  창을 1.4배로 넓혀 봤더니 **케이스 2가 깨졌습니다** → 되돌렸습니다. 다음 과제입니다.

**규칙 — 대체값은 언제나 43**

> 원장님 재확인 2026-08-29 (케이스 4를 보시고): 「44가 맞다 하지만 **판독 안될 경우
> 43도 괜찮아 보인다**」

- 못 읽으면 **항상 43**(`INNER_F_MID`). 케이스가 몇 개든 바뀌지 않습니다.
- ⛔ **케이스 중앙값을 대체값으로 쓰지 마세요.** 지금 4건의 중앙값은 **47** 인데,
  그것은 「짙은 반영구 손님이 많다」는 뜻이지 「못 읽었을 때 47 에 세우라」가 아닙니다.
  못 읽은 사진에 47 을 세우면 미간 맨살에 섭니다. 회귀 **151** 이 잡습니다.
- `INNER_CASES` 는 **기록·검증표**입니다 — 룰을 고칠 때 이 4건이 다 맞는지 봅니다.

**⛔ 사진은 저장하지 않습니다.** 고객 얼굴이고 저장소가 공개입니다. **숫자만** 남깁니다.

**원장님께 부탁드릴 것** — 케이스를 늘리려면 사진과 **그 사진의 정답 이너 눈금**이
함께 필요합니다. 특히 **옅은 반영구·연한 드로잉** 사진이 더 있으면 케이스 4의
판독 실패를 고칠 수 있습니다.

---

## 3. 못박음 검사 — 이 실수를 다시 못 하게 하는 잣대

이 세션의 실패는 전부 **한 가지 모양**이었습니다. 판독이 사진이 아니라 **창 경계나
그늘 끝에 못박혀서**, 서로 달라야 할 자 둘이 **같은 값**이 됩니다.

```
앞머리 212 = 아치두께 212   ← 탐색창 바닥
앞머리 199 = 아치두께 199   ← 그늘 끝
```

**서로 달라야 할 두 값이 같으면, 그것은 사진에서 읽은 값이 아닙니다.**
이 한 문장이 원인을 30초 만에 찾아 줍니다.

### 회귀 131 — 까다로운 사진 9장 일률 검사

| 검사 | 뜻 |
|---|---|
| ① 앞두께 < 앞머리 | 윗선이 아랫선보다 위 |
| ② 아치 < 아치두께 | 같은 규칙 |
| ③ \|앞머리 − 아치두께\| > 2 | 둘이 같으면 못박힌 것 |
| ④ 가로선 다섯 개가 모두 다르다 | 두 개라도 겹치면 의심 |
| ⑤ **아치두께 ≤ 앞머리 ≤ 꼬리** (v1.81.0) | 눈썹의 해부학 순서. 넘었으면 눈꺼풀 그늘을 읽은 것 (2-4) |

사진 9장: 그늘(창바닥) · 그늘(창바닥 위) · 위 번짐 · 얇아지는 꼬리 · 얇아지는 앞머리 ·
잔털 꼬리 · 옅은 번짐 · 모양 A · 모양 B.

**⛔ 새 판독 규칙을 넣을 때는 이 검사를 먼저 돌리세요.** 개별 검사는 통과해도 여기서 걸립니다.

---

## 4. 원장님 표시 대비 현재 정확도 (기준 사진)

| | 앱 | 원장님 표시 | 차이 |
|---|---|---|---|
| 앞두께 | 89 | 87.7 | 1.3px |
| 앞머리 | 136 | 137.2 | 1.2px |
| 이너 | 316 | 318.6 | 2.6px |
| 아우터 | 93 | 93.1 | 0.1px |

**이 표가 나빠지는 변경은 넣지 마세요.** 새 규칙을 넣을 때마다 이 사진을 다시 재현해
네 값을 확인합니다.

**⚠️ v1.99.0 — 이너 행은 새 룰(2-6)이 정합니다.** 위 316/318.6 은 `growEnd` 시절의 값이라
그대로 비교하면 안 됩니다. 새 기준은 **2026-08-29 사진에서 앱 44.2 vs 원장님 44** 입니다
(고치기 전 48). 08-25 사진을 다시 받으면 이 행을 새 룰로 재측정해 갱신하세요.

---

## 5. 배포 절차 — 못박음 없이, 빠짐 없이

원장님 지시: 「못박음 에러 없도록 **깃, 백업, 커밋 등 md 로 저장**」

승인된 변경 뒤에는 **묻지 않고** 아래 순서로 진행합니다.

| # | 단계 | 확인 |
|---|---|---|
> ⏱ 고치는 동안은 **그 부분만** 돌린다 — `PB_ONLY=아치선 node regression-test.mjs`
> (명령서 §0-Y·§0-Y-1 · 아래 「부분 테스트 스위치」 절). 아래 1번은 **커밋 직전 한 번**이다.

| 1 | 회귀 테스트 | `PB_CHROME=/opt/pw-browsers/chromium-1194/chrome-linux/chrome node regression-test.mjs` → **전 항목 통과** (지금 174항목) |
| 2 | 못박음 검사 | 회귀 **131** 통과 (위 3장) |
| 3 | 기준 사진 재현 | 4-장 표의 네 값 확인 |
| 4 | BASELINE 갱신 | 규칙 + **왜** 를 함께 적는다 |
| 5 | 판독 룰 갱신 | 이 파일(`판독-룰.md`)에 새 실패 모양을 추가 |
| 6 | 버전 올리기 | `app.js APP_VERSION` **과** `sw.js VERSION` 을 **함께** |
| 7 | 백업 | zip + SHA-256 을 **배포 전에** 만들어 대화창으로 전달 |
| 8 | 커밋 | 로컬 커밋 (푸시는 불가 — 아래 참고) |
| 9 | 업로드 | GitHub 웹에 **7개 파일** 업로드 → 커밋 |
| 10 | 릴리스 | 한국어 릴리스 노트로 태그 발행 |
| 11 | 라이브 확인 | `WebFetch https://leahkshop.github.io/perfect-brow/sw.js?cb=…` → `VERSION` 일치 |
| 12 | 재동기화 | `git fetch origin && git reset --hard origin/main` → 회귀 다시 한 번 |

### 올리는 파일 7개 (고정)

```
app.js               sw.js
BASELINE.md          README.md
regression-test.mjs  클로드-시작-명령서.md
판독-룰.md            ← 이 파일. 빠뜨리지 마세요.
```

`index.html` · 아이콘 · manifest 는 **바뀐 버전에서만** 추가로 올립니다.
⚠️ v1.81.0 은 `index.html` 도 바뀌었습니다 (설정 시트 구조 · 미리보기 크기 · 순서 편집 탭 ·
`pbBlink1` · `.guidetip .chip[hidden]` 버그 수정) → **8개 파일**을 올립니다.

### ⛔ 절대 하지 말 것

- **`git push` 시도** — 컨테이너에 GitHub 인증이 없어 프록시가 403 을 돌려줍니다.
  배포는 **오직** 브라우저 자동화(`mcp__claude-in-chrome__*`)로 GitHub 웹에 업로드합니다.
- **`npx playwright install`** — 브라우저는 `/opt/pw-browsers` 에 이미 있습니다.
- **API 키·비밀번호·토큰을 저장소에 올리는 것** — 이 저장소는 **공개**입니다.
  `perfect-brow-source/.project-config.json` 은 **절대** 올리지 마세요.
- **회귀 실패 상태로 커밋** — 실패 항목이 하나라도 있으면 커밋하지 않습니다.

---

## 6. 실패 기록 (같은 실수를 두 번 하지 않기 위해)

| 날짜 | 증상 | 진짜 원인 | 고친 곳 |
|---|---|---|---|
| 08-24 | 아우터·아치선이 관자놀이 밖 | 머리카락을 눈썹으로 읽음 | `trimOutside` (1-31) |
| 08-25 | 꼬리가 잔털까지 감 | 문턱을 낮춰 추적함 | 추적 폐지 (1-34) |
| 08-25 | 꼬리가 옅은 번짐까지 감 | **잉크**로 판정 | **진하기**로 판정 (1-34) |
| 08-25 | 앞머리·앞두께가 거꾸로 | 이름만 뒤바뀜 | 이름 교환 (1-35) |
| 08-25 | 이너 13px 짧음 | 잉크로 끝을 자름 | `growEnd` (1-36) |
| 08-25 | 아우터 11px 짧음 | 같은 원인 | `growEnd` 바깥쪽 (1-36) |
| 08-25 | 앞머리 = 아치두께 = 창 바닥 | 덩어리가 창 바닥에서 잘림 | `coreBot` (1-37) |
| 08-25 | 아치두께만 눈꺼풀 위 | 구제 조건이 「바닥에 닿음」뿐 | 조건 제거 (1-37) |
| 08-25 | 앞두께가 눈썹 위로 뜸 | 파우더 번짐이 붙음 | `coreTop` (1-37) |
| 08-25 | 자가 눈썹 밖 맨살에 뜸 | 자를 세로선에 가운데 맞춤 | `segPx` (1-38) |
| 08-27 | 아치두께가 꼬리보다 아래 | 산 아래 그늘을 아치두께로 읽음 | 상한 (1-45 · 2-4) |
| 08-27 | 가이드를 꺼도 안내 칩이 남음 | `.chip{display:block}` 이 `[hidden]` 을 이김 | `.guidetip .chip[hidden]` (1-41) |
| 08-29 | **이너가 미간 맨살(48)에 섬** | `growEnd` 가 눈꺼풀 그늘을 눈썹으로 알고 걸어감 | 이너 전용 판독 `innerDecide` (2-6 · 1-62) |
| 08-29 | 안내문이 「콧방울·내안각에 맞추세요」 | 이너의 뜻이 드로잉 앞부분인데 설명이 달랐음 | `tip_v2` 교체 (2-6) |
| 08-29 | 아치만 **밴드 분위수**로 읽고 있었다 | 앞머리는 픽셀 검증인데 아치는 옛 방식이라 방어가 서로 달랐음 | 공용 판독 `darkBlobsUp` + `archDecide` (2-9 · 1-69) |

**공통점 — 열 건 중 여덟 건이 「눈썹이 아닌 것이 덩어리에 붙었다」입니다.**
새 증상을 만나면 먼저 이것부터 의심하세요.

---

## 부분 테스트 스위치 — `PB_ONLY` (v2.9.0)

명령서 §0-Y 「작업 중인 부분만 돌린다」를 **러너가 직접 집행**합니다.

```bash
export PB_CHROME=/opt/pw-browsers/chromium-1194/chrome-linux/chrome

PB_ONLY=아치선  node regression-test.mjs   # 이름에 그 낱말이 든 항목만
PB_ONLY=arch    node regression-test.mjs   # 별칭 (arch inner outer front tail brow num layout balance)
PB_ONLY=121     node regression-test.mjs   # 항목 번호
PB_ONLY=162-164 node regression-test.mjs   # 범위
node regression-test.mjs                   # 전부 — 커밋 직전 한 번
```

전 항목 180초 → `arch` 76초 → `121` 30초.

### 어떻게 잘라내나

러너 위쪽에 **`BLOCK_TESTS`** 표가 있습니다. 「블록 n 번 안에 이런 이름의 검사들이 있다」는
목록이고, 각 테스트 블록의 여는 중괄호는 `if (RUN(n)) {` 로 감싸여 있습니다.
`RUN(n)` 은 그 블록의 이름들이 `PB_ONLY` 에 걸리는지 보고 **블록째** 건너뜁니다
(브라우저 창을 아예 안 여니까 빠릅니다).

- 검사가 하나도 없는 준비용 블록은 **늘 돌아갑니다** (`RUN` 이 true 를 돌려줍니다).
- 한 블록 안 형제 항목은 **같이** 돕니다. 같은 판독 경로를 공유하므로 오히려 안전합니다.
- 부분 실행이면 요약에 **「부분 실행 · n건 건너뜀」** 이 찍힙니다. 그 줄이 있는 결과를
  「전 항목 통과」라고 보고하면 안 됩니다.

### ⚠️ 테스트를 추가·삭제·이동한 뒤에는 표를 다시 뽑는다

블록 번호와 `BLOCK_TESTS` 가 어긋나면 엉뚱한 블록을 건너뜁니다. 아래를 돌려 확인하세요.

```bash
python3 - <<'PY'
import re, json
L = open('regression-test.mjs').read().split('\n')
idx = [i for i,l in enumerate(L) if re.match(r'^\s*if \(RUN\(\d+\)\) ', l)]
nums = [int(re.search(r'RUN\((\d+)\)', L[i]).group(1)) for i in idx]
tbl = json.loads(re.search(r'const BLOCK_TESTS = (\[.*?\]);\n', open('regression-test.mjs').read(), re.S).group(1))
print("블록", len(idx), "· 표", len(tbl), "· 번호 연속:", nums == list(range(len(nums))))
PY
```

`번호 연속: False` 이거나 개수가 다르면, 블록 여는 줄에서 `if (RUN(n)) ` 를 전부 떼고
표를 지운 다음 **다시 삽입**하세요 (v2.9.0 을 만든 스크립트와 같은 방식:
최상위 `{` 와 들여쓰기 2칸 `{` 를 블록으로 보고, 블록 안 `check("…")` 첫 인자를 이름으로 모읍니다).
